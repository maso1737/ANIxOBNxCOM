# SPEC_17 — 操作文法の統一（INPUT GRAMMAR）

**状態: ANIMATOR / econte / manga-plate 実装済み（2026-08-24）。OBAN は未着手。**

道具の切替を「ボタンを探して押す」から「手が覚えている押し方」へ寄せるための共通文法。
**正準は `animator.html`。** 他ツールは追ってここに揃える。

---

## 1. なぜ揃えるのか

同じ意味の操作がツールごとに違う入口を持っていると、手が覚え直しになる。
現状 **Wクリックのボタン切替だけが各ツールにバラバラに実装されていて**、
キー側の文法（2連打・長押し・修飾キー）が ANIMATOR にしか無い。

揃える対象は「**モードの切替**」であって、機能そのものではない。
各ツール固有の機能は無理に共通化しない。

---

## 2. 文法（ANIMATOR で確定した形）

### 2-1. ツールキーの押し方（3通り＋バネ）

| 押し方 | 意味 | ANIMATOR の実装 |
|---|---|---|
| 単押し | そのツールを選択 | `SHORTCUT_ACTIONS` の `run()` |
| **2連打**（320ms以内） | そのツールボタンの **Wクリックと同じ** | `KEY_GESTURES[id].dbl` |
| **長押し**（500ms） | そのツールボタンの **長押しと同じ** | `KEY_GESTURES[id].hold` |
| **Shift+キー** | 長押しと同じ動作へ**待ち時間ゼロで直行** | `KEY_GESTURES[id].shift` |
| **押しっぱなし→離す**（250ms以上） | 押している間だけそのツール／離すと**元のツールへ戻る** | `KEY_GESTURES[id].spring` |

**キー名ではなくアクションid（`fill`/`pen`/`erase`）に紐づける。**
キーは再割当できるので、キー名で書くと設定を変えた瞬間に壊れる。

実装上の落とし穴（ANIMATOR で踏んだもの）:

- **`e.repeat` を捨てる。** オートリピートを連打や再実行と数えると、
  押しっぱなしで全消去が連射されて Undo が何手も積まれる
- **単押しの動作は遅らせない。** 1回目で即ツール選択 → 2回目で切替。
  「2連打かどうか待つ」設計にするとツール選択がもたつく
- **2連打・長押しが成立したら基準時刻を0に落とす。** 3連打が2連打の続きにならないように
- `keyup` と `blur` でタイマーとバネ状態を必ず消す
- **ボタン側が click の時刻差で見ている操作（ANIMATORのERASE全消去）は `dbl` に登録しない。**
  キー2連打＝ボタンを2回clickするだけで成立するので、登録すると二重発火する

### 2-2. 現在の割り当て（ANIMATOR）

| キー | 単押し | 2連打 | 長押し | Shift+ |
|---|---|---|---|---|
| `G` | FILL選択 | 投げ縄塗り ⇔ バケツ | 投げ縄透明消し ⇔ バケツ | 投げ縄透明消しへ直行 |
| `P` | PEN選択 | 筆圧ペン ON/OFF | （バネ＝押している間だけPEN） | — |
| `E` | ERASE選択 | このコマを全消去 | （バネ＝押している間だけERASE） | — |

ボタン側（Wクリック／長押し）は**同じ関数を呼ぶ**。挙動を二本立てにしない。

### 2-2b. 現在の割り当て（econte・2026-08-24）

| キー | 単押し | 2連打 | 長押し | Shift+ | バネ |
|---|---|---|---|---|---|
| `G` | LASSO選択 | **— (割り当てない)** | 投げ縄 塗り ⇔ 透明消し | 同左へ直行 | ✓ |
| `B` / `P` | PEN選択 | 筆圧ペン ON/OFF | — | — | ✓ |
| `E` | ERASE選択 | いまの描く先を CLEAR | — | — | ✓ |
| `A` | SEL選択（SINGLE限定） | WARP 3×3 ON/OFF | — | — | — |

econte 側の**意図的な違い**（理由つき）:

- **`G` の2連打には何も割り当てない。** econte は FILL を廃止して**常時投げ縄**（SPEC_16 §5-B2）なので、
  ANIMATOR の「2連打＝投げ縄塗り ⇔ バケツ」に当たる相手が居ない。
  **空振りする押し方を作らない**方を選んだ。投げ縄消しは **長押し と Shift+G**（ANIMATOR と同じ場所）
- **`E` には `dbl` を登録してよい。** ANIMATOR が登録しないのは、あちらのボタンが click の時刻差で
  全消去を見ていて二重発火するため。econte のボタンは `onDoubleActivate`＝**ネイティブ dblclick** なので、
  キーの2連打では発火しない
- **`P` は ANIMATOR 合わせの別名。** econte は昔から `B` なので**両方効かせる**
  （手が覚え直しにならないのが目的なので、片方を取り上げない）
- **キーは再割当できない**（B/E/G/A 固定）ので、表は `PAINT_KEYS`（キー→ツールid）と
  `KEY_GESTURES`（ツールid→押し方）の2枚に分けてある。再割当を入れるときは `PAINT_KEYS` を
  keymap 参照へ差し替えるだけで済む

### 2-2c. 現在の割り当て（manga-plate・2026-08-24）

| キー | 単押し | 2連打 | 長押し | Shift+ | バネ |
|---|---|---|---|---|---|
| `V` | SEL選択 | コマ ⇔ 素材 | — | — | — |
| `D` | DIV選択 | **— (割り当てない)** | — | — | ✓ |
| `P` | EDIT選択（コマ線編集） | **— (割り当てない)** | — | — | ✓ |
| `T` | 文字選択 | 縦書き ⇔ 横書き | — | — | — |
| `B` | ブラシ選択 | 筆圧ペン ON/OFF | — | — | ✓ |
| `G` / `W` | 投げ縄選択 | **— (割り当てない)** | 塗り ⇔ 透明消し | 同左へ直行 | ✓ |

manga-plate 側の**意図的な違い**（理由つき）:

- **EDIT（`P`）の「ホワイト」モードを廃止した。** 元は「前のコマの上に白をかぶせる」ための逃げ道だったが、
  ◆Items が**コマ欄**になって「枠の上」へ置けば同じことができる（SPEC_09 v2-9）。
  投げ縄ホワイトと**同じ機能が2つの入口を持っている**状態だったので、投げ縄（`G`）へ一本化。
  結果 `P` は2連打の相手が居なくなったので、**空振りする押し方を作らない**（econte の `G` と同じ判断）
- **`G` の2連打も割り当てない。** manga-plate の投げ縄は econte と同じく**バケツを持たない＝常時投げ縄**。
  ANIMATOR の「2連打＝投げ縄塗り ⇔ バケツ」に当たる相手が居ない
- **`W` は残す。** manga-plate は昔から `W`＝投げ縄ホワイト。`G` を足して ANIMATOR / econte に揃えるが、
  **片方を取り上げない**（econte が `B` と `P` を両方効かせているのと同じ理由）
- **`G` を空けるために GUIDE を `Ctrl+@` へ移した**（Photoshop のグリッド `Ctrl+'` と同じ位置。
  USレイアウトの `'` も受ける）。GUIDE はレールの押しやすい所にボタンがあるので、キーは補助でよい
- **`D` の2連打も無し。** 分割の向きは `1`/`2`/`3` で直接選べる＝2状態のトグルではない
- キーは再割当できないので、表は `TOOL_KEYS`（キー→ツールid）と `KEY_GESTURES`（ツールid→押し方）
  の2枚に分けてある（econte と同じ形）

### 2-3. 囲う操作（投げ縄）

| 始め方 | 動き |
|---|---|
| **ドラッグで始める** | フリーハンド。離した所で確定 |
| **クリックで始める** | **多角形**。クリックのたびに頂点、次の一辺はラバーバンドで見せる |

多角形の確定は **Enter / 始点をクリック / Wクリック** の3通り。
**Esc でいつでも捨てられる**（塗らない・Undoも積まない）。ツールを変えても捨てる。

> ⚠ **Alt+クリックは使わない。** スポイトの正準割り当て（`animator-brush-ops`）と衝突する。
> 「クリックで始めたら多角形」なら修飾キーが要らない。

### 2-4. 効いたことを見せる

キー操作はボタンの点灯が視界の外なので、**切替後の状態名をステージ上部に一瞬出す**
（ANIMATOR: `stageToast()`・0.9秒）。モード切替は原則すべて通す。
ANIMATOR で通しているもの: 塗りモード / 筆圧 / 全消去 / 多角形開始・取消 / MIRROR / FLIP / ONION / インク。

**manga-plate は既存の `#toast` を使い回している。** あれは元からステージ（`#main`）の中・上部中央に
出るので、位置は ANIMATOR の `#stage-toast` と同じ。**時間だけ 0.9 秒に縮めた `stageToast()`** を足して、
モード切替はそちらへ通す（通常の `toast()` は 2.6 秒のまま＝「やったことの結果」用に残す）。

### 2-5. 点灯で状態を見せる（モードバッジ）

ツールボタンの**角に出す小さな丸**。**色そのものが状態の名前**なので、ラベルを読まなくていい。
ANIMATOR が正準で、econte・manga-plate は同じ色・同じ位置・同じ大きさ（6px・グロー付き）。

| 位置 | 色 | 意味 | クラス |
|---|---|---|---|
| 左上 | 紫 `#B98CFF` | **投げ縄で囲う道具である** | `.lasso-on` |
| 右上 | 水色 `#5AE9FF` | **透明消し** | `.fe-on` |
| 右上 | 金 `#FFD27A` | **筆圧ペン ON** | `.pp-on` |

左上と右上に分けてあるのは、**投げ縄＋透明消しが同時に立つ**ため（重ねると片方が見えない）。
ANIMATOR は FILL がバケツにも化けるので `.lasso-on` を出し入れするが、
**econte と manga-plate は常時投げ縄なので `.lasso-on` は点けっぱなし**にする。

---

## 3. 追って揃える先（未着手）

**いま「Wクリックのボタン切替」だけで、キー側の文法が無いところ。**

| ツール | 対象 | メモ |
|---|---|---|
| ~~`econte.html`~~ | ~~投げ縄（塗り⇔透明消し・長押し）~~ | **2026-08-24 実装済み。§2-2b を見る。**<br>`G`の2連打だけは相手が居ないので割り当てない（長押し / Shift+G が投げ縄消し） |
| ~~`manga-plate.html`~~ | ~~SEL の **コマ ⇔ 素材**、EDIT の **線 ⇔ ホワイト**~~ | **2026-08-24 実装済み。§2-2c を見る。**<br>EDIT のホワイトは**廃止**（◆Items の「枠の上」で同じことができるため）。投げ縄は `G`（`W` も残す）・GUIDE は `Ctrl+@` へ |
| `oban-builder.html` | PLACE の Wクリック=フレーム内へ 等 | 選択・配置系。ツール切替とは意味が違うので、載せるかは要検討 |
| `composer.html` | キーの Wクリック送り（LINEAR→EASE→HOLD） | タイムライン上の操作。ツールキーの文法とは別系統。**無理に揃えない** |
| 各ツール共通 | `stageToast` 相当の告知 | **econte は `stageToast()` を実装済み**（`.stage-toast`・0.9秒・ステージ上部中央）。ステージが2つ（STUDIOプレビュー / EDIT）あるので id ではなく class で持ち、**出ているほうへ出す**。ステージが見えていないときは既存の `toast()` へ落とす |

**進め方**: 1ツールずつ、そのツールの `state` と DOM に焼き込む。共有ライブラリ化はしない
（単一HTML完結の方針を崩さない）。移した時点で下の「実装済み」に印を付ける。

- [x] `animator.html`（2026-08-24）
- [x] `econte.html`（2026-08-24。§2-2b の差分つき）
- [x] `manga-plate.html`（2026-08-24。§2-2c の差分つき）
- [ ] `oban-builder.html`

---

## 4. 関連

- スキル `animator-brush-ops` … ブラシまわりの手癖の正準。**§「キーでも同じ3通りを出せるようにする」がこのSPECの要約**
- スキル `kinetic-stage-design-system` … ESCの優先順位・ボタンの見た目
- `SPEC_09_MANGA_PLATE.md` … manga-plate 側（§v2-10）。EDIT ホワイト廃止の判断もここ
- `ANIMATOR_HANDOVER.md` … ANIMATOR 側の実装経緯
- `ECONTE_HANDOVER.md` … econte 側の実装経緯（§5-G）。**踏んだ落とし穴もここ**
