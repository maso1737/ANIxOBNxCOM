# SPEC 09 — MANGA PLATE（パラメトリック漫画素材ツール）【ドラフト】

> **この仕様書単体で実装可能なように書いてある。** 行番号は変動するため**必ずシンボル名でgrep**。
> 実装前に読むもの: `oban-builder.html`（`frames[]` / `quad` / `migrate` / `viewerHTML`）、
> `animator.html`（`gPalette` / MANGAプリセット=MOTION_COMIC_SPEC Phase 4）、`PIPELINE.md`、
> スキル `kinetic-stage-design-system` / `animator-ref-overlay`（REF下敷きの正準）。

## 0. 目的と大原則

- **目的**: コマ枠線・スクリーントーン・スピード線を**パラメータで後から調整できる**静的素材（＝板）として生成し、
  透過PNGで OBAN / COMPOSER に渡す。クリスタのトーン/集中線ツールの超軽量版
- **役割分担（変えないこと）**:
  - **animator** = 動く手描き素材（キャラ・目パチ・なびき・手描きオノマトペ）。改修ゼロ
  - **MANGA PLATE** = 静的なパラメトリック板（トーン・線・枠）。**手描き機能は持たない**
  - **OBAN / COMPOSER** = 枠マスク・コマ内外の重ね（飛び出し）・タイミング。素材の中身に関知しない
- **正はパラメータJSON**。ラスタ化は書き出し時のみ。単一HTML・依存ゼロ・`file://` 直開きOK・iPad Safari対応
- タッチは**鳥山明程度**: 大きめドットのトーン数種＋集中線/流線＋矩形枠。種類を増やして創作の圧にしない
- UI は KINETIC STAGE デザインシステム準拠。確認は modalConfirm（native confirm 禁止）

## 1. しないこと（v1スコープ）

- 手描き・ラスタレイヤー・ブラシ（animatorの領分）
- テキスト組版・オノマトペ生成（手描きが正。MOTION_COMIC_SPEC の思想どおり）
- グラデトーン・柄トーン・多角形/円マスク・アニメーション再生機能
- 撮影処理（fx）。トーンをシェーダでやる案は**将来メモ（§7）**へ退避

## 2. データモデル

```js
PLATE = { version:1, name, w:1920, h:1080, bg:'transparent'|'#F5F1E8',
  elems:[ ELEM, ... ] }   // 配列順=重ね順（後=前面）。localStorage('manga-plate') 自動保存

ELEM 共通: { id, type, name, visible:true, x, y, rot }   // 座標はPLATE px・中心基準
type別:
  'frame'  … { w, h, lw:8, color:'#000' }                          // 矩形枠線（塗りなし）
  'tone'   … { w, h, dot:12, gap:22, density:0.4, angle:45,        // 網点。dot=ドット径px
               color:'#000', shape:'dot'|'square' }                //   大ドット既定=モアレ耐性
  'focus'  … { r0:120, r1:900, count:90, lw:5, taper:0.9,          // 集中線。r0=白場半径
               jitter:0.35, seed:1, color:'#000' }                 //   jitter=長さ/角度ゆらぎ
  'stream' … { w, h, count:24, lw:4, taper:0.8, jitter:0.3,        // 流線（平行スピード線）
               seed:1, color:'#000' }                              //   rotで方向を決める
```

- **seed 決定論**: 同じseedなら同じ線。乱数は `mulberry32(seed)` 等の自前PRNG（`Math.random` 禁止 →
  `single-html-verify` の決定論契約に乗せる）
- REF下敷き: `ref:{ image:dataURL|null, opacity:0.4, x,y,scale }`。**書き出しに含めない**（animatorのREFと同じ思想）。
  実装は `animator-ref-overlay` スキルの canon を移植（ドラッグ移動/ホイール拡縮/原寸中央）

## 3. 画面と操作（触ってすぐわかる、が最優先）

- 左=キャンバス（`#stage`。パン=空白ドラッグ / ズーム=ホイール / 選択ELEMはハンドルで移動・回転・リサイズ）
- 右=ELEMリスト（並べ替え=重ね順、👁トグル、＋ボタンで type 選択追加、選択で下にパラメータスライダー群）
- パラメータ変更は**即時再描画**（プレビューは表示解像度、書き出し時のみフル解像度レンダ）
- `Ctrl+Z` undo（commit方式、スライダーは300ms合体 — OBANの `commitD` と同型）/ `Esc` 選択解除
- COPY/PASTE PLATE: PLATE JSON のクリップボード入出力（file://拒否時は prompt窓フォールバック — OBANと同じ）

## 4. 出口（ここが本体）

| 出力 | 内容 | 用途 |
|---|---|---|
| **PNG (elem別)** | 選択ELEM 1つ＝透過PNG 1枚 | OBANで「トーンはコマ内・集中線は飛び出し」と別配置 |
| **PNG (全体)** | 全ELEM合成の透過PNG | 1枚もの |
| **PNG ×N SEEDS** | focus/stream を seed+1,+2,… で N枚（既定3）連番書き出し `name_001.png…` | OBANにD&D→自動seqパネル→**loopで線がバタつく**演出（4枚以上で自動グループ化される点に注意→既定は4枚推奨） |
| **PLATE JSON** | パラメータそのもの | 再編集・受け渡しの正 |

- 書き出し倍率 `SCALE ×1/×2`（§6 モアレ対策）。ファイル名は `name_elem.png` / `name_full.png`
- ダウンロードは `<a download>` 方式（OBANの `viewerHTML` ダウンロードと同型）

## 5. 実装フェーズ

### P0 — 骨格＋frame＋PNG書き出し
単一HTML雛形（KINETIC STAGE準拠）・PLATE/ELEMモデル・localStorage自動保存・ELEMリスト・
`frame` エレメント・キャンバス操作・PNG(elem別/全体)。**この時点で「枠線をパラメトリックに引いて出す」が完成**

### P1 — tone
網点レンダラ（オフスクリーンにドット格子→angle回転→ELEM矩形でクリップ）。density はドット径×格子比で近似。
プリセットチップ: 10% / 20% / 40% / 60%（MANGAパレットのグレー4段と対応する濃度）

### P2 — focus / stream
seed決定論の線群レンダラ。taper=先細り（線幅を根元→先端で補間）。プリセットチップ: 集中線(標準/密) / 流線(標準/太)

### P3 — REF下敷き＋COPY/PASTE PLATE＋×N SEEDS書き出し

### P4 — OBAN FRAME 枠線（**oban-builder.html 側の小改修**）
- `frames[]` に `line:{on:false, w:8}` を追加（`migrate()` が欠損補完）。ONなら **quad の4点をそのままstroke**
  （マスクと枠線が絶対にズレない、が狙い。色は黒固定）
- チップに `LINE` トグル＋太さ。`viewerHTML()` にも同じ描画を複製（ビルダー/ビューア完全同期の規律どおり）
- 手描きラフ枠が欲しいコマは、MANGA PLATE か animator の枠PNGを最前面 ord のパネルで上書き（機能追加不要）

### P5（任意・将来）— COPY FOR COMPOSER
elem別PNGを dataURL 埋め込みの IMAGE_v1 疑似JSONとしてクリップボードへ（composer `importImageFile` の経路に乗せる）。
v1では PNG 書き出し→D&D で十分なので急がない

## 6. リスクと判断メモ

- **モアレ**: トーンを作業解像度で焼いた後に COMPOSER/OBAN のカメラで拡縮すると干渉縞。対策は
  ①既定を大ドット（dot≥10px）にする ②`SCALE ×2` で焼いて配置側で縮小 ③気になる場合は最終解像度で焼き直し。
  仕様としては①＋②で十分（鳥山タッチの大ドットは元々モアレに強い）
- **白黒前提**: color は既定 `#000`。MANGAパレット（MOTION_COMIC_SPEC Phase 4 の10色）とホワイト `#FFF` だけ
  パレットチップで出す。フルカラーピッカーは置かない（圧にしない）
- **focus の中心**: コマの中心と一致させたいケースが多い → PLATE の w/h を OBAN のコマ比に合わせて作る運用。
  自動連携（quadを読んで中心を合わせる等）は**やらない**（ツール間の結合を増やさない）
- **check.js**: 対象4ファイル→5ファイルに `manga-plate.html` を追加。`PIPELINE.md` の表に入口/出口を追記。
  ランディング `index.html` への追加は OBAN 追記と同時でよい

## 7. 将来メモ（v1ではやらない）

- トーンを satsuei-fx のハーフトーンシェーダにする案（最後まで無劣化調整できるが、fxはツール横断の
  グローバルチェーンでありELEM単位の適用に合わない。必要になったら SPEC_06 側で検討）
- tdr_live 参加（PLATE更新→OBAN自動反映）。静的素材は再D&Dで足りるため見送り
- グラデトーン・カケアミ・ベタフラッシュ

## 8. 受け入れ基準

- [ ] `frame`/`tone`/`focus`/`stream` を各1つ置き、パラメータをスライダーで変えると即時反映される
- [ ] elem別PNGが透過で書き出され、OBANにD&Dしてコマ内（FRAME子）/コマ外（ルート）に配置できる
- [ ] `focus` の ×4 SEEDS 書き出し→OBANで自動seq化→loopで線がバタつく
- [ ] 同じ PLATE JSON を PASTE すると**ピクセル一致**で再現される（seed決定論）
- [ ] リロード後に localStorage から完全復元（REF画像は除く。プレースホルダ表示）
- [ ] OBAN: FRAME の LINE ON で quad に沿った枠線が出て、ビューア書き出しでも同一に見える。OFF時は従来と完全不変
- [ ] `node tools/check.js` ALL PASS（manga-plate.html 追加後）
- [ ] `PIPELINE.md` 更新（MANGA PLATE の行を追加、OBAN の入口に「MANGA PLATE PNG」を追記）

---

# v2 — PAGE / PANEL COMPOSER（2026-08-13 実装済み）

> v1 は「板（素材）を作る」ツールだった。v2 は **原稿用紙とコマ割りが主役**になり、v1 の
> tone/focus/stream は「コマに割り当てる仕上げアイテム」として残っている（描画コードは無改造）。

## v2-1. 用紙（ジャンプ規定・600dpi B4）

`PAPER_PRESETS` / `paperOf()`。mm を dpi で px 化して保持する。

| プリセット | 裁ち落とし | 仕上がり | 内枠 |
|---|---|---|---|
| `B4_600`（既定・モノクロ） | 230×320mm = **5433×7559px** | 220×310mm = 5197×7323px | 180×270mm = 4252×6378px |
| `B4_350`（カラー） | 同 mm・350dpi | 〃 | 〃 |
| `B5_600`（同人誌） | 192×267mm | 182×257mm | 150×220mm |

用紙を切り替えると全ページのコマ・素材が dpi 比で拡縮される（確認モーダルあり）。
トンボ／仕上がり／内枠は `guide` 表示のみで**書き出しに入らない**。

## v2-2. コマ＝「base ∩ 分割線の列」。poly は毎回そこから導出する

```js
PANEL = { id, base:[{x,y}...], cuts:[CUT...], poly:[...], border, bleed, from }
CUT   = { id, nx, ny, ax, ay, gap, side }   // 直線 n·P = n·A、side=+1/-1 でどちら側を残すか
```

**poly を持ち回らず cuts から毎回作り直すのが要。** これで「一度引いた分割線をあとから動かす／回す」が成立する。

- `rebuildPanel(k)` … `base` に cuts を順に `clipHalf()`（Sutherland–Hodgman）していく。
  `side>0` は `n·P ≤ d-gap/2`、`side<0` は `n·P ≥ d+gap/2`
- `splitPanels()` … 対象コマの cuts に新しい CUT を1本足した子を2枚作る（`side` が逆）。斜めも同じ式
- **間隔は線の向きで補間**: `gapFor(nx,ny) = gut.lr*|nx| + gut.tb*|ny|`
  （縦線＝左右60 ／ 横線＝上下180 ／ 斜めはその中間）。クリスタ既定値と一致
- 対象は **コマ選択中＝そのコマだけ／未選択＝線が横切る全コマ**（モード切替UIを増やさないための規約）
- 分割で親コマが消えるとき、**親に入っていた素材は中心が入る方の子へ引き継ぐ**（迷子にしない）
- `applyCut(pg,cid,nx,ny,ax,ay)` … 同じ `cut.id` を持つ全コマを書き換えて再構築。
  **1枚でも潰れたら丸ごと元に戻す**（`JSON` スナップショット。選択も貼り直す）
- `mergePanel()` … 「最後の1本だけ side が違う相手」＝兄弟を探し、その CUT を取り除く。**分割の正確な逆操作**
- `sortPanels()` が読み順（右綴じ＝上から、同じ帯なら右が先）に並べ替える。コマ番号表示がこの順
- `dispPoly()` … `bleed` ONのコマは、**内枠に接している辺だけ**用紙の端まで伸ばす（ブチ抜き）

> v2.0 で作った `cuts` を持たないコマは `migrate()` が `cuts:[]` を入れる。
> 線編集の対象外になるだけで、表示・書き出しは従来どおり。

## v2-3. 素材とマスク

`PAGE.items[]` は `img / tone / focus / stream / frame` の共通配列。`panelId` が要（かなめ）:

- **`panelId` あり** … そのコマのポリゴンで `clip()` してから描く → **枠の下**（はみ出しがカットされる）
- **`panelId` なし** … 枠線より**上**に描く → 擬音・飛び出し

この1本のルールだけで「コマ内の絵」と「上に乗せる擬音」が分かれる（フラグを増やさない）。

- **境界効果**: `edgeSprite()` がアルファを2リング×24方向にずらして描き `source-in` で単色化 → 本体の下に敷く。
  キャッシュキーは呼び出し側が渡す（網点化後は canvas で `.src` が無いため **tag 引数が必須**）
- 画像は `MATSRC{srcId→dataURL}` に持ち、**undo履歴には積まない**（`snapshot()` は BOOK のみ）
- 保存は **IndexedDB `manga_plate/book/'current'`**（dataURL が localStorage に乗らないため）。
  v1 の `localStorage['manga-plate']` は初回起動時に1ページ目の items へ引き継ぐ

## v2-4. 連携（受け側の改修ゼロ）

**既存の共通配管にそのまま乗る**のが設計の要。新しい語彙を作っていない。

| 方向 | 手段 | 受け側 |
|---|---|---|
| → ANIMATOR REF | `exPut()` で `tdr_exchange` に `PROJECT_v1`（1セル＝コマ画像）＋ `tdr_live` の `project-update` | ANIMATOR `REF ▸ ＋FROM SAVED`（`exGetAll()` を読む既存経路） |
| ← ANIMATOR 取込 | `exGetAll()` から選んで画像素材化 | — |
| ← 自動更新（往来） | `tdr_live` の `project-update` を購読し、`item.linkId === projectId` の素材を差し替え | — |
| → COMPOSER | `exPut()` 後 `composer.html?id=<pid>` | COMPOSER 既存の `?id=` ディープリンク |
| → OBAN | `exPut()` ＋ `project-update` | OBAN `＋FROM ANIMATOR`（`apCandidates()` が EX_DB と `gApSeen` を併せて見る） |

- 起動時に `composer-hello` を投げて ANIMATOR 側の `gLiveActive` を立てる（composer/OBAN と同じ作法）
- `BOOK.links[projectId] = {pageId, panelId, box, scale, ox, oy, w, h}` を送信時に記録し、
  戻りは `placeLinked()` が `scale = 1/lk.scale` でコマにピタリ収める（**送った矩形にピクセル一致で戻る**）
- 送信解像度が縦長コマと合っていないと画素が無駄になるため、`updateLinkNote()` が**使用率%**を出し、
  55%未満なら `コマ比` を促す（`fitLinkToPanel()` = 長辺2048でコマ比に合わせる）

## v2-5. モアレ対策（グレー→網点）

`tonizeImage(img, it)` … **画像1枚ごと**の変換で、ページ全体は舐めない（作業解像度が小さいので速い）。

1. 画素を輝度で3分割 — `l<0.10`＝線画は**原画のまま**残す／`0.10≦l≦0.94`＝中間調を網点対象／`l>0.94`＝白
2. 中間調の被覆率からセルごとにドット半径を決めて 45° 格子で打つ（セル＝`dpi/lpi/scale`）
3. `destination-in` で中間調マスク外へはみ出さないようにし、線画を上から戻す

**線画とトーンが同じ絵に混ざっていても線が潰れない**のがこの3分割の狙い。
既定 50線（Gemini 資料の「WEBは50線が最も安全」に合わせた）。書き出しの階調は
**生グレー（既定・モアレ皆無）／網点化／カラー**の3択。`網点プレビュー` で画面でも確認できる。

## v2-6. 操作系（2026-08-13 追補・他ツールと揃える）

**ツールは3つだけ。増やさずに Wクリックでモードを持たせる**（animator の手癖と同じ作法）。

| ツール | 既定 | Wクリック |
|---|---|---|
| `SEL`（V） | コマ選択 | **素材選択**（トーン/流線が下敷きでも掴める） |
| `DIV`（D） | 斜め | — （向きは 1/2/3 と「用紙の外から引く」で決まる） |
| `EDIT`（P） | 分割線の移動・回転 | **ホワイト**（投げ縄で白かぶせ） |

- **定規メタファ**: `pointerdown` が用紙の外なら、上下の外＝横線 / 左右の外＝縦線に**自動で切替え、向きボタンも点灯させる**
  （クリスタのルーラーからガイドを引く操作に対応。既定は斜めのまま）
- **EDIT/線**: `cutsOfPage()` が同じ `cut.id` を共有するコマを束ね、`cutSegment()` が見える範囲を出す。
  端の●＝反対端を軸に回転（Shiftで15°）、中央の■＝法線方向へ平行移動。`gap` は回すたび `gapFor()` で再計算
- **EDIT/白**: 投げ縄を `type:'white'` の多角形アイテムにする。開始点のコマに割り当たるので**コマ外へはみ出さない**。
  トーンより後ろの配列位置に入るので上にかぶさる。色を黒にすれば「足す」側にも使える
- **左右入替**: `TAB`（animator と同じ）。既定は**レールがキャンバスとサイドパネルの間＝右**。`localStorage['mp_swap']`
- **◆Items の行**: `◉`表示ON/OFF ／ 本体を**Wクリックで改名・つかんで上下ドラッグで並べ替え** ／ `▲▼`前後 ／ `✕`削除。
  表示は前面が上なので、**ドラッグの着地位置も見た目どおり**にする（配列indexは `len-1-表示index`）。
  パラメータ欄に削除ボタンは置かない（行の`✕`に一本化）
- **トップバー**: `作品名 ▸ EXPORT/IMPORT JSON ▸ ◀ PAGES ▶ ▸ ⚙ ⛶ HOME`。
  入出力の位置は OBAN / econte と揃えてある（→ ルート `CLAUDE.md`）。`◆File` セクションは廃止し、
  `全消去（新規）` は `◆Panel` の末尾へ移した
- **⚙ 操作一覧**: econte 準拠の**段組ヘルプ**（`HELP_GROUPS` → `buildHelp()`）。縦スクロールの羅列にしない
- **⛶ 全画面**: animator と同じ実装をそのまま移植（iPhone Safari は非対応なのでボタンごと隠す）
- `↑`/`↓` で ◆Items を送る（表示順＝前面が上、と一致させること）

## v2-6b. 見開き・iPad・芯くずし（2026-08-14 追補）

### 見開き（左右2ページ同時作業）
- **`PAGES` を Wクリック**で ON/OFF（キーは `W`・`localStorage['mp_spread']` に保存）。
  レールのツールと同じ「クリック=主機能 / Wクリック=モード切替」の手癖に合わせた。
  **単クリックの一覧オープンは 210ms 遅らせる**（Wクリック1回目で一覧が開いてしまわないように）
- **モデルは1ページ単位のまま一切変えない。** 世界座標 = ページ座標 + オフセット、という1段だけを足した:
  `spreadSlots()`（左→右の並び・`null`=向かいの白紙）/ `worldW()` / `curOx()` / `pageAtWorldX()`。
  `evPt()` が「作業中ページのページ座標」を返すので、**既存のツール処理は全部そのまま動く**
- 並びは PAGES 一覧と同じ規則（1p目は単独＝表紙、以降2pずつ。右綴じなら右が若い番号）
- **反対のページをタップすると、そちらが作業対象に切り替わる**（`focusPageAt()`。pointerdown と drop の入口だけ）。
  作業中の側は `P◯ ●`（rouge）＋用紙の外周ラインで示す。BLUE（選択外を青く）は作業中ページだけに掛ける
- 描画は「用紙ごとの部品」を切り出して `translate` して呼ぶ形に整理:
  `drawSheetGround` / `drawBlankSheet` / `drawShade` / `drawGuides` / `drawPanelNums` / `drawPageTag`。
  **z順は単ページ時とまったく同じ**（地→中身→BLUE→ガイド→コマ番号→選択・ハンドル）

### iPad（他ツールと語彙を揃える）
- **2本指タップ=UNDO / 3本指タップ=REDO**（econte の `gTap` をそのまま移植。250ms・12px・window の capture 段）。
  ※ OBAN は発注者要望で 3本指=CAPTURE。**語彙が違うのは OBAN だけ**
  - **2026-08-15 実機確認済み（発注者iPad）**: 3本指タップは二重発火も不発も無し。
    OBAN のメモにあった「iPadOS 自身の やり直し が発火する」は**勘違いだったと判明**（`OBAN_BUILDER_HANDOVER.md` に訂正済み）
- **2本指ピンチ=ズーム＋パン**（指の中点を画面に貼り付けたまま拡縮）。それまで iPad には**ズーム手段が無かった**
  - 指は同時に着かないので、**3本目が着くまでの数十msを「2本指ピンチ」と誤認する窓**がある
    （実機で「3本指を置くとピンチが少し反応する」として報告された）。
    対策は `PINCH_SETTLE=80ms` の猶予＋`PINCH_SLOP=16px`。**猶予中は基準を指に追従させるだけ**にしてあるので、
    猶予明けに溜まった分がガクッと反映されることもない（実測: 猶予中+60px→効き始め後+100px で倍率1.625＝飛ばない）
  - `3本→2本` に戻ったら `pinchStart()` で取り直す（これが無いと、一度3本触ると全部離すまでピンチが死ぬ）
- 指が2本以上のあいだは `gestureLock` で1本目のツール操作を打ち切る
  → DIV 中にピンチしても**誤って分割されない**
- スライダーの「突っかかり」対策は3点セット:
  1. **当たり判定を背高に**（見た目は細い線のまま。`::-webkit-slider-runnable-track` で線、thumb は `margin-top` で中央）
  2. **`touch-action:none`** — 横スクラブが `#side` の縦スクロールに取られない
  3. **`renderQ()`（rAF間引き）** — `commitD()` とドラッグ系の `render()` を置換。
     実測: スライダー入力40発 → render **1回**（重い状態で 12ms/回なので、体感がまるごと変わる）
- `@media (pointer:coarse)` は**スタイルシートの最後に置く**。途中に置くと後段の `.btn` などに負ける（一度踏んだ）

### 断ち切り（bleed）の作り直し ← 2026-08-15 バグ修正
- **症状**: 斜め分割したコマを断ち切りにすると、外へ出た辺の角度が変わる（19.3°の線が15.3°に寝る）
- **原因**: 旧 `dispPoly()` は**頂点を真上/真横へクランプ**していた。矩形の角なら正しいが、
  斜めの辺の端点を「まっすぐ上」へ動かすと辺が剪断される
- **直し方**: 頂点を動かすのをやめ、**`base`（内枠）だけを用紙いっぱいに広げて、同じ `cuts` で切り直す**。
  分割線は一切動かさないので、斜めの辺は自分の角度のまま用紙の端まで伸びる。
  分割由来でない旧コマ（`cuts` が空＝内枠いっぱいの矩形）だけ従来のクランプ（`outToPaper()`）にフォールバック
- 検証: 全頂点が「用紙の辺の上」か「分割線からちょうど `gap/2`」のどちらかであること。
  ゆるい斜め／急な斜め／水平／垂直、両側、分割線2本の入れ子まで確認
- 同時に、**用紙プリセット変更が `poly` しか変換していなかった**のも直した（`base` と `cuts.ax/ay/gap` も一緒に運ぶ。
  法線は等倍なので不変）。放置すると用紙を変えたあとの線編集・断ち切りが壊れる

### 集中線の「芯くずし」（`core`）
- 白場が真円すぎるのを崩すパラメータ。0=真円 / 1=ぐしゃぐしゃ。**新規=0.25、既存データ=0**（`migrate` で明示補完）
- 低周波のうねり（2〜5山＋倍音）で全体を歪ませ、そこに1本ごとのばらつきを足す。
  **乱数列は本体の `rnd` と別に持つ**（`mulberry32(seed*7919+13)`）ので、`core=0` の絵は従来と1pxも変わらない
- プリセットチップに `芯くずし`(0.55) / `真円`(0) を追加

## v2-6c. 文字・ブラシ・ホワイト／重なりの規則（2026-08-16 追加）

### レールに3つ足した（`╱斜` の下・キーは T / B / W）

| ツール | 動き | Wクリック |
|---|---|---|
| `TEXT`（T） | クリックで置いて **◆Items の欄に打つ**（キャンバス直編集は持たない） | 縦書き⇔横書き |
| `BRUSH`（B） | ドラッグでベクター手描き。**描き始めに戻ると囲みとみなして塗る** | 筆圧 ON/OFF（**既定ON**・ANIMATORとは逆） |
| `WHITE`（W） | 投げ縄で囲った中を白でかぶせる | 白⇔黒 |

- **見開きのキーは `W`→`M` に移した**（W を WHITE に譲った。PAGES Wクリックが主なので影響は小さい）
- `WHITE` は **EDIT の Wクリック（線⇔白）と同じ実体**。どちらから入っても `type:'white'` の多角形アイテムになる。
  ANIMATOR の投げ縄はラスタのスキャンライン塗りだが、**こちらはベクターのまま持つ**
  （600dpiで書き出しても粗が出ない・あとから色と不透明度を変えられる）
- ブラシは `pts:[{x,y,w}]`。`w` は筆圧係数（`0.25+0.75*pressure`・**ペン軸のみ**）。
  描画は「区間の四角＋節の丸」を塗る方式（入り抜きが素直に出る／頂点が増えても破綻しない）
- 空のまま置かれた文字は `pruneEmptyText()` が掃除する（置いてみて打たなかった分が ◆Items に溜まらない）

### 書体（アンチック体）
- **しっぽりアンチック / 同B1 / ゴシック** の3つを ◆Items のセレクタで選ぶ。既定はアンチック。
  Google Fonts の `@import` に相乗り（SIL OFL・商用可）。資料: `_Research/manga/HTMLWebアプリで使えるアンチック体.md`
- ⚠ **canvas は `ctx.font` に指定しただけでは Webフォントを取りに行かない。**
  boot で `document.fonts.load()` を明示的に呼び、届いてから描き直すこと（忘れると黙ってゴシックで描かれる）
- 縦書きは「1行＝1列・右の列から左へ」。`ー（）「」…` は90°倒し、`、。` は右上へ寄せる

### 重なりの規則（レイヤーもモードも持たない）
```
画像(0) → 網・線＝トーン/集中線/流線/frame(1・乗算) → 文字・ブラシ・ホワイト(2)
```
- `ITEM_BAND` で段を決め、**同じ段の中だけ ◆Items の並び（▲▼・ドラッグ）が効く**
- 網・線は `globalCompositeOperation='multiply'`（`INK_MULTIPLY`）。
  **これが「グレーで塗ると線画が潰れる」の答え**。実測: ベタのグレーを線画に重ねたとき、
  真っ黒に近い画素が 乗算=50,730 / 通常合成=12,089（＝線が灰色に負けていた）
- 白（`#FFFFFF`）のアイテムは乗算から外す（乗算の白は何もしないため。ホワイトが効かなくなる罠）

### ◆Items の並べ替え（AE / COMPOSER と同じ語彙）
- `↑↓`＝選択送り（`Shift`で先頭/末尾）／ `Ctrl+↑↓`＝並べ替え（`Shift`併用で最前面/最背面）
- ⚠ **行のドラッグ並べ替えは move/up を window に張ること。**
  行に張ると `renderItemList()` で行が作り直された瞬間にイベントが切れ、
  **「上へは1つずつしか動かない」**という症状になる（2026-08-16 修正）。
  落とす位置も「行の中心と比較」ではなく**カーソルが入っている行**で判定する（上下非対称になるため）

### 置き場所
- **全消去は ⚙（操作一覧）の右上**へ移した。econte と同じ場所・同じ2段確認
- **左右入替は トップバーの ⚙ の右隣（`⇄`）**。ヘルプの中から出した

## v2-6d. 磨き込み（2026-08-16 第2弾）

### 文字は「先頭合わせ」
段落は中央合わせにしない。**縦書き＝全列の書き出しを同じ高さに／横書き＝全行の左端をそろえる**
（一番長い行を基準にブロックの端を出す）。中央合わせだと行ごとに頭がズレて読みにくい。
実測: `ドン` と `ドドドン` の2行で左端／上端のズレ 0px。

### 置き場所
- **左右入替 `⇄` はレール最上段**（`.railtop`）。ツールより一回り小さい黄色ボタン。
  同じ所に **全画面 `⛶`** も置き、**トップバーの `⛶` と同じ処理を共有**（`btns.forEach`）。
  トップバーは他ツールと同じ `… ⚙ ⛶ HOME` に戻した

### ホワイト（W）
- Wクリックの白⇔黒トグルは**廃止**。色は **◆Items の色チップ**で選ぶ（既定=白）
- **パレットは2役**: 素材を選んでいれば その素材の色 ／ **選んでいなければ「次に置くものの色」**
  （`toolColorKey()`＝brush/white/text。見出しに「次に置く◯◯の色」と出す）
- **長押し（500ms）＝透明消し**（animator の FILL長押しと同じ手ぐせ・`onLongPress`）。
  実装は **用紙が白なら用紙色で塗る／透過用紙なら `destination-out`**。
  ⚠ オフスクリーン合成にしなかったのは、用紙が 5433×7559 で**中間バッファが約160MBになる**ため。
  実測: 白用紙→中心 `[255,255,255,255]` ／ 透過用紙→ `[0,0,0,0]`

### ◆Items
- **ロック**（行の錠）: `it.locked`。`itemAt()` が拾わなくなる＝キャンバスで誤って動かさない。
  リストからは選べる（パラメータ欄に「ロック中」と出す）
- **結合 `▼結`**: ペン／ホワイトのみ・**同じコマ・同じ種類・塗りと透明消しは混ぜない**。
  形は `subs[]`（座標を結合先の原点に合わせて足す）。以後は1アイテムとして移動・拡縮・フチが効く
- **コマごとの区切り線＋左の色帯**（`.el-sep` / `PIN_COLORS`）。グループ化はしない＝**並び順＝重なり順のまま**
- 名前変更は `onDoubleActivate`（iPadのダブルタップでも入る）

### ペンのフチ
文字・画像と同じ `edge{on,w,color}`。**同じ形を「太らせた色」で1回、そのあと本体で1回**塗るだけ。
白い線に黒フチも同じ仕組みで出せる。

### iPad: 素材のピンチ拡縮
- **素材の上で2本指＝その素材の拡大縮小＋移動 ／ 余白＝視点のズーム＋パン**（OBAN と同じ決め打ち方式）
- 対象は `PINCH_SCALABLE`＝img / text / focus / stream / frame / brush / white。
  **トーンは除外**（網の間隔が変わるとモアレる。触ったらトーストで理由を出す）
- 指を置いた瞬間の `pinchBase()` を基準に**絶対値で書き戻す**＝何度動かしてもズレが溜まらない
- 対象の決め方: 「選択中の素材の上」または「素材選択モードで素材の上」。`tool==='sel'` のときだけ

## v2-9. ◆Items の「コマ欄」化（2026-08-24 実装済み）

区切り線だけだった ◆Items を、**コマごとの欄（グループ）** にした。狙いは
「どのコマに入れるか」をプルダウンで選び直す手間を消すこと。

### 並びはフォーマットとして固定

`pg.items` は常に **コマ欄順に正規化して持つ**（`normalizeItems()`。安定ソートなので欄の中の並びは保つ）。

```
配列（背面→前面）: コマ1 … コマN → 枠の上
リスト表示（上→下）: 枠の上 → コマN … コマ2 → コマ1
```

- **コマ欄そのものは動かない。**上下移動（▲▼ / Ctrl+↑↓ / 行ドラッグ）は**欄の中だけ**。
- **「枠の上」は常に一番上の欄**。以前のように「枠の上」の塊が複数できることはない
  （＝上へ運んで1つにまとめる手間が不要になった）。
- 描画順（`renderPage`）は元から「コマ内の素材 → コマ枠線 → 枠の上の素材」なので、
  **欄をまたぐ配列順は絵に影響しない**（コマは重ならない）。だから並びを固定しても安全。
- 空の欄も**常に出す**（放り込む先／追加先として要るため）。

### 追加先（addTarget）

`addTarget`（コマID / null＝枠の上）が ＋ボタンの入れ先。**追加しても外れない**ので、
コマ2を選んでおけば何枚足してもコマ2に入る。追従するのは以下：

| 操作 | addTarget |
|---|---|
| ◆Items の「追加先」チップをクリック | そのコマ（＋ `selPanel` も選択・素材選択は解除） |
| コマ欄の見出しをクリック | 同上 |
| キャンバスでコマを選ぶ（SEL/コマ） | そのコマ |
| 画像をコマの上へD&D／文字ツールでコマ内をクリック | そのコマ |
| ページ切替・コマ削除・コマ分割 | null（枠の上）に戻す |

`addItem()` は `selPanel` ではなく **`addTarget`** を見る（初期位置もそのコマの中心）。

### プルダウンを廃止（配置＝チップ）

`◆Items ▸ 配置` の `<select>` を**出しっぱなしのチップ列**に置換。押すだけで移る。
選択が複数なら**まとめて**移る。同じチップUIを「追加先」でも使っている（`renderAddTarget()` / `renderItemParams()`）。

### 別のコマへ移す3つの道

1. **行を別のコマ欄までドラッグ**（空の欄・見出しの上にも落とせる）→ `rowDragMove()`
2. **見出しの `⇥移動`**（選択中があるときだけ出る）→ `assignItemsTo()`
3. **配置チップ**

### 複数選択

`selItems[]`（`selItem` は主。必ず `selItems` に含まれる）。
`renderItemList()` 冒頭の `syncMultiSel()` が「主が居ないなら単独に戻す」で整合を取るので、
既存の `selItem=…` 直代入コードを書き換えずに済ませている。

| 操作 | 挙動 |
|---|---|
| Ctrl / ⌘ + クリック | 足し引き |
| Shift + クリック | 範囲（表示の並びで） |
| リストのドラッグ | 選択ぜんぶ一緒に動く／別コマへも一緒に移る |
| キャンバスのドラッグ | 選択ぜんぶ一緒に動く（`drag.mv[]`。ロック中は除外） |
| 表示◉ / ロック▪ / ✕ / DEL | まとめて効く |

### 表示まわりの整理（同日）

- トップバーの `ANI×OBN×COM // SPEC_09` を削除
- レール最上段の ⛶（全画面）を削除。左右入替 `⇄` の1つだけにして **38×16 の横長**に
  （全画面はトップバーの ⛶ に一本化）

## v2-10. 操作文法を ANIMATOR に揃える（2026-08-24 実装済み・SPEC_17）

**正準は `animator.html`。** 詳細と各ツールの差分は `SPEC_17_INPUT_GRAMMAR.md` §2-2c。

### EDIT（`P`）の「ホワイト」モードを廃止

元は「**前のコマの上に白をかぶせる**」ための逃げ道だった。◆Items がコマ欄になって
「枠の上」へ置けば同じことができる（§v2-9）ので、**同じ機能が2つの入口を持っている**状態になっていた。
投げ縄（`G` / `W`）へ一本化し、`editMode` という状態ごと削除。

`P` は分割線の移動・回転だけになった＝**2連打の相手が居ない**ので、
**空振りする押し方は作らない**（econte の `G` と同じ判断）。

### 投げ縄は `G`（`W` も残す）／ GUIDE は `Ctrl+@` へ

`G` を ANIMATOR・econte と同じ位置に揃えた。`W` は昔からのキーなので**取り上げない**。
`G` を空けるため GUIDE を `Ctrl+@`（Photoshop のグリッド `Ctrl+'` と同じ位置。USレイアウトの `'` も受ける）へ。

### 囲い方が2通りに（SPEC_17 §2-3）

| 始め方 | 動き |
|---|---|
| ドラッグで始める | フリーハンド。離した所で確定（従来） |
| **クリックで始める** | **多角形**。クリックのたびに頂点、次の一辺はラバーバンドで見える |

確定は **Enter / 始点をクリック / Wクリック** の3通り。**Esc でいつでも捨てられる**（塗らない・**Undoも積まない**）。
ツールを変えても・ページを変えても捨てる。
判定は `LASSO_DRAG_PX=6`（これ以上動いたらフリーハンド）と `LASSO_CLOSE_PX=14`（始点への吸着・**画面px**）。
画面pxで測るのは、ズームしても指の感覚を変えないため（`lasso.scr[]` に画面座標も並行して溜めている）。

⚠ **Alt+クリックは使わない**（スポイトの正準割り当てと衝突する）。「クリックで始めたら多角形」なら修飾キーが1つも要らない。

出口は `lassoFinish()` の1つだけ。フリーハンドも多角形も同じ所へ落ちる。

### 点灯（モードバッジ）を ANIMATOR と同じに

| 位置 | 色 | 意味 | 出る所 |
|---|---|---|---|
| 左上 | 紫 `#B98CFF` | 投げ縄で囲う道具である | 投げ縄（**常時点灯**＝バケツを持たないので） |
| 右上 | 水色 `#5AE9FF` | 透明消し | 投げ縄 |
| 右上 | 金 `#FFD27A` | 筆圧ペン ON | ブラシ |

ラベルも状態で変わる（投げ縄 `白`/`色`/`消`・ブラシ `筆圧`/`均一`）。
`syncToolLabels()` が**ラベル・バッジ・タイトルを1か所で**同期する（ANIMATOR の `applyFillModeUI()` と同じ役目）。

### 告知は `stageToast()`（0.9秒）

キー操作はボタンの点灯が視界の外なので、切替後の**状態の名前**をステージ上部に一瞬出す。
manga-plate の `#toast` は元からステージ内・上部中央なので、**時間だけ縮めて使い回し**。
通常の `toast()`（2.6秒）は「やったことの結果」用にそのまま残す。

## v2-7. ANIMATOR 側の変更（1行だけ）

`renderRefLayer()` の「参照が尽きたら非表示」を、**1コマだけの参照＝静止画とみなして全ティックで出す**ように変更。

```js
const still = r.ticks.length === 1;
if(!still && t >= r.ticks.length) continue;
const img = r.ticks[still ? 0 : t];
```

MANGA PLATE が送るコマは1コマなので、これで**下敷きが消えずに全フレーム描ける**。
複数コマの参照は従来どおり尽きたら消える（回帰確認済み）。`maxRefTicks()` は1のままなのでタイムラインも伸びない。

取り込み側は複数コマに対応済み（`item.srcs[]` + `item.cell`）。ANIMATOR が複数コマを持つときは
◆Items に**コマ送り**が出て、LIVE 更新でコマ数が増減しても追従する。

## v2-8. まだ無いもの（意図的）

- 頂点そのものの移動（分割線を動かす方が破綻しないため、そちらに寄せた）
- 手描き・ラスタブラシ（ANIMATOR の領分。v1 の大原則を維持）
- テキスト組版（擬音は手描き＝MOTION_COMIC_SPEC の思想どおり）
- ZIP一括書き出し（依存ゼロを保つため `<a download>` の連続で代替）

## 9. 決定事項（2026-07-16 実装済み）

1. ファイル名: `manga-plate.html`（確定・実装済み。P0〜P3 一括実装）
2. `tone` は「ドット径(dot)＋間隔(gap)」指定＋%プリセットチップ（10/20/40/60%・被覆率readout付き）で実装
3. P4（OBAN枠線）は先行実装済み: `frames[].line={on,w}`・チップLINEトグル・ビューア同期・migrate補完
4. 実装メモ: focus の飛び出し配置用に、キャラ側は elemBounds=r1×2 でヒット判定。undo は snapshot方式(上限60)。
   REF画像は localStorage に保存しない（セッション限り・プレースホルダなしで単純に消える）
