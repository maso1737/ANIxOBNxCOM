# SPEC_13 — ECONTE V2「STUDIO」（1画面統合＋BOARD強化＋大判カメラ枠列＋MP4）

対象: `econte.html`（SPEC_10 P0+P1 実装済みの上に載せるV2）。
実装前に必読: `ECONTE_HANDOVER.md`（cuts[]共有ペイントの前提・undoスタックの落とし穴・
strip で setPointerCapture を使ってはいけない理由が書いてある）。
変更後チェック: `node tools/check.js`（6ファイル一括・econte含む）。

**進捗（2026-08-19）**: V2-A / B / C / D1 / D2 / E1 / E2（a〜d）/ **D3** 実装済み。
**V2-E3（フォトバッシュ・SINGLE限定）も片付いた** —— ただし `cut.layers[]` ではなく
**「浮いた選択」**（SPEC_15 P2・実装 2026-08-19）で。§9c の設計は不採用。
画面の役割は **§9 で確定済み**（先にそこを読むこと）。

## 0. 結論（何をするか）

ユーザーのラフスケッチどおり、BOARD / SHEET / TIMELINE の3ビューを**1画面「STUDIO」に統合**する。
SHEETは右側の「レイヤー一覧風」縦パネルになり、プレイヘッド位置のカットとフォーカス枠が連動する。
これは可能で、設計上も自然：**cuts[] が唯一のデータなので、3ビューが1ビューに減るだけ**。
同期処理は今までどおり一切書かない（描き込み先・尺・番号は全部同じ配列を見る）。

ビュー構成は `STUDIO | EDIT` の2つになる（board/sheet/tl の個別ビューは廃止）。
EDITへの遷移（サムネ/クリップのダブルクリック・✎）と復帰はそのまま。

```
┌─────────────────────────────────────────────┐
│ IMPORT   ...              [✓焼込] EXPORT   … SAVED │ ← トップバー
├───────────────┬───────────────┬─────────────┤
│               │  （映像プレビュー）   │ C1 [thumb] 内容 2+00 ▲▼│
│   BOARD       │   1280×720 fit       │ C2 [thumb] 内容 1+12 ▲▼│ ← SHEETパネル
│  （スキャン写真＋  │──────────────│ C3 [thumb] 内容 …     │   (縦スクロール)
│   CUT枠たち）    │ C1  2+00             │   …                  │
│  MOVE/CUT/FIT │ [TEXT 内容・セリフ]    │        [＋空コマ追加]  │
├───────────────┴───────────────┴─────────────┤
│ ▶ [C1     |C2  |C3        ]  ←タイムライン帯（横断・従来のstrip） │
└─────────────────────────────────────────────┘
```

- 左（BOARD）: 幅 ~40%（A4比くらいの面積）。従来のBOARD機能＋§2の強化
- 中央: プレビュー（現行 #tl-cv 相当）＋その下に「C# / 尺 / TEXT(内容・セリフ)」＝**現在カットの編集欄**
- 右（SHEETパネル）: 幅 ~22%・最小200px。行 = `C# | サムネ | 内容(2行省略) | 尺 | ▲▼✕⟳`
- 下: タイムライン帯（全幅横断）。クリップ右端ドラッグ尺変更・スクラブは現行実装のまま
- **フルスクリーンボタン**（トップバー右）: `document.documentElement.requestFullscreen()` トグル

最小成立幅の目安: 1180px（iPad横）。右パネルは折りたたみボタン（≡）で隠せるようにする。

## 1. STUDIO のフォーカス同期（V2-A の核）

- 「現在カット」= `cutIndexAt(state.tl.frame).i`。この1つの導出値に全UIが従う:
  - 右パネル該当行に `.focus` 枠（--ice系）＋ `scrollIntoView({block:'nearest'})`
  - 中央下の C#ラベル / 尺input / TEXTtextarea が現在カットの `durF` / `note` を編集
  - BOARD上の該当CUT枠もハイライト色を変える
- 右パネル行クリック = `seek(cutStartFrame(i))`（カット頭へ）。サムネ/行のダブルクリック = EDIT
- 実装: `renderStudioSync()` を1つ作り、`seek()` / `renderTL()` の末尾から呼ぶ
  （毎フレームDOM全再構築はしない。フォーカス class の付け替えとテキスト欄の値だけ）。
  再生中の textarea 書き換えはフォーカス中(document.activeElement)ならスキップ
- 旧 `setView('board'|'sheet'|'tl')` の呼び出し箇所はすべて `setView('studio')` に整理。
  `renderSheet()` は右パネル用 `renderSheetPanel()` に改名・簡略化（行テンプレは流用可）

## 2. BOARD 強化（V2-B）

1. **Ctrl+V ペースト**（PC）: `window.addEventListener('paste')` → `clipboardData.items` から
   `image/*` を取り出し `addPhotoFromImage()`（ビュー中央へ）。STUDIO表示中のみ・入力欄フォーカス中は無視。
   **貼った写真への REF 紐づけは §2-1a でこの経路に同乗させる**

### 2-1a. REF BOARD 連携（`photo.ref`）— 送信側は実装済み

`Tools/ref-board/ref-board.html`（**移動しない**。cross-project の参照インボックスで、
public repo である本リポジトリには入れない）側は **2026-08-01 に実装済み**。
econte 側は「受け取って写真に貼る」だけでよい。

**なぜ V2-B に同乗させるか**: 後から足すと、それまでに貼ったスクショを手で貼り直すことになる。
フィールド1個・保存1語なのでここで入れる。

#### データ（econte 側で足すのはこれだけ）

```js
// photos[] の要素に1つ追加（現行 {id,name,img,blob,x,y,w,h}）
ref: { refId, url, svc, title, memo, tags:[] } | null
```

- `saveAll()` の `phItems` に `ref: p.ref || null` を1語追加。`loadAll()` も同様。
  **IndexedDB のスキーマ変更は不要**（既存データは `undefined` → null 扱いで無害）
- **`cut` 側には持たせない**。カットの参照一覧が要る場面（カラースクリプト等）は
  `bakeRect` に重なる `photos[].ref` を**その場で集める＝導出値**。
  SPEC_10 の「同期を作らない」原則をここでも崩さないこと

#### 受信（クリップ棚）

```js
const LS_CLIP = 'refboard.clip.v1';    // 後から econte を開いた場合の受け皿
const BC_NAME = 'refboard_live';       // 同一オリジンなら即時（tdr_live と同じ手口）
```

送られてくるペイロード（実測値・そのまま）:

```json
{ "type":"clip", "v":1, "ts":1785594954892,
  "item":{ "refId":"ms9qq2vb79dtr",
           "url":"https://x.com/digiusagi/status/2082983714846548072",
           "svc":"x", "title":"ライン調整", "memo":"プラグイン", "tags":["アニメ","AE"] } }
```

- `localStorage[LS_CLIP]` にも**同じ形**（`{v,ts,item}`・`type` 無し）で残る。
  起動時に読み、`BroadcastChannel` は開いている間の即時反映用。**両方見ること**
- `v!==1` は無視。`ts` が **24時間以上前のものは無視**（貼り忘れの誤爆防止）
- econte → ref-board へ返す通知（任意だが入れる）:
  - `{type:'clip-used', v:1, ts}` … 写真に紐づけた。**ref-board 側は受信して棚を空にする実装済み**
  - `{type:'clip-clear', v:1, ts}` … ユーザーが econte 側で解除した
  - 受信して棚を消したときは `localStorage.removeItem(LS_CLIP)` も行う（両側で同じ状態にする）

#### UI（最小）

- STUDIO の BOARD 上部に細い帯: `CLIP: ライン調整 — 次に貼る画像に付けます [解除]`（クリップがある時だけ）
- ペースト/インポートで写真が増えたら、その写真に `ref` を入れて棚を空にする（`clip-used` を送る）。
  **複数枚を一度に貼った場合は先頭1枚だけ**に付ける
- 選択中の写真パネルに **🔗 / ⇄ / ✕**（2026-08-22 に modalPick から分解。**1クリック＝1動作**）:
  - **🔗** … `ref` があれば `window.open(ref.url)`、無ければ現在のクリップを紐づけ（既存スクショの後付け）
  - **⇄** … 今のクリップに差し替え　**✕** … 外す（`pushLog` で Ctrl+Z 可）
  - ⇄ / ✕ は `ref` 付きのときだけ出す（`#photosel-box.has-ref`）。**無い操作は見せない**
- `ref` 付きの写真は BOARD 上で角に小さいマーカー（`--ice`）を描く。`renderBoard()` に数行

#### 前提（重要）

`BroadcastChannel` / `localStorage` の共有は**同一オリジンのときだけ**成立する。
ルート CLAUDE.md の運用どおり `python -m http.server 8000` をルートで立て、
`localhost:8000/Tools/ref-board/…` と `localhost:8000/ANIxOBNxCOM/econte.html` で開けば成立する。
file:// 直開きや **GitHub Pages の econte × ローカルの ref-board** では繋がらないので、
下の §2-1a-2（URL経路）が本命のフォールバック。手貼り用に `⏻ REF`（`clipPasteJson`）も残す
（ref-board 側の帯にある **⧉** が同じ JSON をクリップボードへ出す）。

### 2-1a-2. 別オリジン経路（`?refclip=` ＋ `postMessage`）— 2026-08-22 実装済み

**実運用は「ref-board はローカル / econte は GitHub Pages」**で、ここは同一オリジンにならない。
そこで ref-board の **→ECONTE** を「クリップして econte を開く」1クリックに統合した。
ユーザー手順は **→ECONTE → 写真を選ぶ → 🔗** の3手で終わる（旧: ⧉→タブ切替→⏻REF→貼付→OK→…）。

送信側（`Tools/ref-board/ref-board.html` の `sendToEconte()`）:

1. `setClip(it)`（従来どおり localStorage + BroadcastChannel。同一オリジン運用はこのまま効く）
2. クリップJSONを**黙って**クリップボードにも入れる（⏻ REF 手貼りの保険。トーストは出さない）
3. 生きた窓ハンドル `gEconteWin` があれば `postMessage({type:'refclip',v:1,ts,item},'*')` ＋ `focus()`
   — **econte を再読込させない**ため。無ければ `window.open(url+'?refclip='+b64u(JSON),'econte')`

- `b64u()` は **base64url(UTF-8)**（`TextEncoder`→`btoa`→`+/=`置換）。title/memo が日本語なので `btoa` 直打ち不可
- URL 上限 `URL_MAX=1800`。超えるときは **memo だけ 400→120→0 と削る**（`refId`/`url`/`tags` は落とさない）
- 行き先は `localStorage['refboard.econte.v1']`（☰メニューで変更可）。既定は Pages の `econte.html`

受信側（`econte.html`）:

- `clipFromQuery()` … 起動時に `?refclip=` を読んで `clipSet(d,true)`。読んだら
  **`history.replaceState` でクエリを消す**（再読込で同じ REF が二度乗るのを防ぐ）。
  `localStorage[LS_CLIP]` より**優先**する（今まさに飛んできた1件だから）
- `window.addEventListener('message')` … `{type:'refclip',v:1,item:{url}}` の形だけ受ける。
  ここに載るのは「次に 🔗 で付ける候補」だけで**押すのは必ずユーザー**なので送信元オリジンは限定しない。
  形が合わないものは黙って捨てる
- `unb64u()` は `b64u()` の対。壊れた文字列は `null` を返して**例外を投げない**
2. **HEIC読み込み**（PC）: accept に `.heic,.heif` 追加。まず `createImageBitmap(file)` を試し
   （iPad/新Safariはネイティブで通る）、失敗したら **heic2any をCDNから遅延ロード**して
   JPEG Blobへ変換（単一HTML方針の例外はCDNのみ＝JSZipと同じ扱い）
3. **写真の回転**: `photo.rot`（0/90/180/270）。選択中写真パネルに ↺90° ボタン。
   `renderBoard()` と `bakeCut()` の drawImage を「中心回転」で描く共通ヘルパ
   `drawPhoto(ctx, p)` に一本化（90/270時は当たり判定のw/h入替に注意）
4. **モノクロ化・輝度**（最低限）: `photo.filter = {gray:false, bright:1.0, contrast:1.0}`。
   選択中写真パネルに GRAYトグル＋明るさ/コントラストの2スライダー（0.5〜2.0）。
   実装は `ctx.filter = 'grayscale(1) brightness(x) contrast(y)'` を drawPhoto 内で設定
   （renderBoard/bakeCut両方に効く＝再ベイクで絵コンテにも反映）
5. **CUT枠の編集**: MOVEツールで既存CUT枠をクリック選択（枠線とC#ラベルを当たり判定。
   写真より優先）。選択枠は 内側ドラッグ=移動 / 角ハンドル=リサイズ（16:9固定）/ Delete=削除。
   離した時に `rebakeCut(i)` 相当を自動実行（`dirtyBase`）。
   **V2-D でこの操作がそのまま「カメラ枠列 `cam[]` の編集」に拡張される**（§5a）ので、
   枠のヒットテスト/ドラッグ/リサイズは最初から**枠1つ = `cam[]` の1要素**を編集する形で書くこと
   （V2-B 時点では `cam.length===1` しか無いだけ、という実装にしておく）
6. **BOARD統合Undo**: animator と同じ「一元ログ」方式。`gLog = [{type, undo(), redo()}]`。
   type: photo-add / photo-move / photo-del / photo-rot / photo-filter / cut-add / cut-rect /
   cut-del / **paint**（プレビュー描画=既存drawCスナップショット）。
   STUDIO の Ctrl+Z/Y はこのログを叩く（EDIT内は従来どおりカット内drawC undoのまま）。
   ※既存の「TIMELINEペイント直後のCtrl+Z」も paint エントリとしてこのログに乗せ替える

## 3. EDIT / ペイント改善（V2-C）

1. **トレース透かし**: `cut.baseAlpha`（0..1・既定1.0）。`compositeTo()` で
   `ctx.globalAlpha = cut.baseAlpha` を baseC 描画時だけ適用（＝サムネ/プレビュー/書き出し全部に効く）。
   EDITサイドとSTUDIOミニツールに「BASE %」スライダー。0.2〜0.4がトレース用の想定
2. **TOOLパネル左右入替**: 設定 `gUi = {side:'left'|'right'}`（localStorage `econte_ui_v1`）。
   `#edit-main` の flex-direction: row-reverse 切替＋ミニツールの left/right 切替
3. **パレット挙動変更**: `selectPalSlot()` から `setETool('fill')` を削除
   （**色を選んでもツールは変わらない**。animator正準からの意図的な逸脱として
   コメントを残すこと。スキル animator-color-palette の canon と差分が出る点）
4. パレットJSON入出力（⇩⇧）は実装済み。STUDIOミニツールにも同じ4ボタンを出す
5. **EDITズーム/パン**: `state.editView = {x,y,z}`。ピンチ=拡縮＋パン、1本指タッチ=パン、
   ホイール=カーソル中心ズーム、FITボタン=リセット。
   実装は editCv の CSS transform（width/height style変更）でよい —
   `toCanvasCoord()` は getBoundingClientRect ベースなので**変更不要**（重要）

## 4. iPad 対応（V2-Cに含める）

1. **ダブルタップ検出**: iPadはネイティブ dblclick が飛ばないことがある。
   共通ヘルパ `bindDoubleTap(el, fn)`（pointerup 2回が350ms以内・24px以内）を作り、
   タイムラインクリップ・右パネル行・サムネに `dblclick` と併用で張る
2. **2本指タップ=UNDO / 3本指タップ=REDO**: pointerdown中のtouch数を追跡し、
   全部が250ms以内に上がって移動<12pxならタップと判定。EDIT/STUDIO両方で有効。
   誤爆防止: タップ判定が出たら直前のピンチ/描画開始をキャンセル
3. **指=操作・ペン=描画**（animatorと同じ操作感）: `paintDown()` の冒頭で
   `e.pointerType === 'touch'` は描画に入らず パン/ピンチ経路へ回す。
   ペン(pointerType 'pen')とマウスだけが描く。※現行実装は指でも描いてしまうので変更

## 5. 大判カメラ枠列＋可変ベイク＋書き出し（V2-D・データ移行あり）

**初版の「1.2x のりしろ固定＋カメラプリセット」は廃止**（2026-07 差し替え）。理由:
大判は「必ず 1536×864」ではなく案件ごとに違う（3倍PAN／大きめT.U.／複合の A→B→…→L）。
固定倍率ののりしろは「足りるかどうかの賭け」であり、PAN先で画が切れる事故を構造的に防げない。

**代わりに、先に枠を置いてベイク範囲を枠から逆算する**（＝実際の大判レイアウトと同じ順序）。
実装量はむしろ減る: `OS` 定数もプリセット7種も強さスライダーも不要になり、
UI は §2-5「BOARDのCUT枠編集」の自然な拡張1本になる。

### 5a. カメラ枠列 `cut.cam[]`（この方式の核）

```js
cut.cam = [
  { k:'A', x,y,w,h, dur:60, ease:'LINEAR'|'EASE'|'HOLD', key:true },  // dur = 次の枠までのコマ数
  { k:'B', x,y,w,h, dur:60, ease:'LINEAR' },
  { k:'C', x,y,w,h, key:true },                                        // 最終枠に dur は無い
]
```

- 座標は **BOARD座標**・16:9固定。ラベル `k` は A,B,C… を順に自動採番（並べ替えでリナンバー）
- `cut.durF = Σ cam[].dur` の**読み取り専用**にする。尺入力は枠ごと
  （`cam.length<=1` の FIX カットのみ、従来どおり `durF` を直接入力）
- 枠の数字は **`dur`（次の枠までのコマ数）**を正とする。ユーザーの大判指定（`A 60Fr`）と一致。
  通しフレームは表示のみ併記（`A 0f / B 60f / C 120f`）
- **プリセットは持たない**: B枠がAより小さい＝T.U.、位置違い＝PAN、両方＝PAN+T.U.。
  枠を置けば全部表現できる。`{preset, amount}` は廃止
- ease は composer 準拠の3モード（LINEAR / EASE / HOLD）。HOLD＝その枠で止まる＝タメ
- 再生/書き出しは `drawCamFrame(ctx, cut, f)` を `compositeTo()` から分離して新設:
  フレーム `f` から区間 i と区間内 t を出し、`cam[i]`→`cam[i+1]` の矩形を ease 補間して
  `bakeRect` 座標から 1280×720 へ描く（`cam.length<=1` は従来どおり全面描画）
- **`key:true` はカラースクリプト一覧に出す枠**（§5h）。既定は始点と終点だけ true

### 5b. ベイク範囲 `cut.bakeRect`（「PAN先に画が無い」の構造的解決）

```
bakeRect = fit16_9( union(cam[].rect) を bakeExpand 倍に広げた矩形 )   // bakeExpand 既定 1.02
```

- **定義上、全ての枠は bakeRect の内側**＝「PANした先に画がない」が原理的に起きない
- 枠を追加/移動/リサイズしたら `bakeRect` を再計算 → `dirtyBase` → 自動再ベイク
  （既存 `rebakeCut()` の経路をそのまま使う。ユーザー操作としては何も増えない）
- `cut.bakeExpand`（1.0〜1.5）だけ手動保険として残す（枠ぎりぎりが不安なとき用）
- **`cut.src` は廃止し `cam[0]` に吸収**。空コマは `cam: []` / `bakeRect: null`

### 5c. 可変ベイク解像度（1536×864 固定をやめる）

```
s     = CONTE_W / min(cam[].w)                 // 最寄り枠が等倍(1280幅)になる密度
bakeW = round(bakeRect.w * s), bakeH = round(bakeRect.h * s)
if(bakeW*bakeH > MAX_AREA) 全体に sqrt(MAX_AREA/(bakeW*bakeH)) を掛けてクランプ
```

- `CONTE_W/H (1280×720)` は **「本番枠＝出力解像度」の意味に純化**する。
  ベイクサイズは `cut.bakeW/bakeH`（カット毎に違う）
- `MAX_AREA = 3840*2160`（animator の `CFG.MAX_AREA` と同じ考え方）
- クランプが効いたカットは SHEET行と中央プレビューに `最寄り枠 0.72x` と表示。
  枠を減らす／大判を割る、の判断をユーザーができるようにする（自動では割らない）
- **実作業量の本体はここ**: 現行 `econte.html` の `CONTE_W/CONTE_H` 直参照を全点検して
  カット毎サイズに置き換える（約20箇所。`bakeCut` / `compositeTo` / `cutThumb` /
  `toCanvasCoord` / FILLの走査と `visited` 配列 / undoの `getImageData` /
  `fitEdit` / TLプレビューのレイアウト等）。
  ※**プレビュー/書き出し側は 1280×720 のまま**（`drawCamFrame` が吸収する）

### 5d. メモリ：遅延デコード（大判を入れるならセットで必須）

- 大判1カット ≒ `baseC + drawC` で **66MB**（3840×2160×4×2）＝通常カットの約9倍。
  10カットで660MB → iPad Safari は落ちる。**現行の「全カットの canvas を常時保持」のままでは大判は成立しない**
- Blob を正とし、**必要なカットだけ decode する LRU**（既定8カット）に改める:
  - `cut.baseC/drawC` への**直参照を禁止**し、`getBase(cut)` / `getDraw(cut)` 経由に統一
  - 常駐対象: EDIT中のカット / プレイヘッド前後1カット / サムネ生成中の1枚
  - 追い出し時に dirty なら先に encode（`drawC`=PNG・線画 / `baseC`=JPEG q0.85・写真）
- サムネは 256×144 を `cut.thumb` にキャッシュし `dirty*` が立った時だけ再生成。
  現行 `cutThumb()` は呼ばれる度にフル合成しているので、大判ではここが最初に詰まる

### 5e. 素材はみ出し警告（守りの2段目）

構造で守れるのは「ベイク範囲」まで。**紙（写真）そのものが足りない**ケースは別対処:

- `bakeRect` のうち `photos[]` の union に含まれない領域を **BOARD上に赤ハッチ表示**
- 該当カットは SHEET行に ⚠。焼く前に気づけることが目的なので**警告のみ**（自動補正しない）
- 紙をスキャンして並べる運用では実際に一番起きるので、V2-D の中では優先度高め

### 5f. 焼き込み3モード＋INFO帯

- `焼き込み: OFF / 画面内 / INFO帯` の3択（現行チェックボックスを置換）
- **INFO帯**: 出力1280×720のうち、絵を 1/1.2（1066×600）に縮めて中央配置し、
  余白の帯に `C# ／ 尺 ／ note先頭行` を描く（画に一切重ならない・プロの絵コンテ動画風）。
  プレビューも同じ描画関数を通す（WYSIWYG維持）
- 枠列カットでは `C1-A→B` のように現在区間も出す

### 5g. MP4書き出し（AE読み込み可・iPad再生可）

- 優先順: **WebCodecs(H.264 avc1.42) + mp4-muxer(CDN)** → `MediaRecorder('video/mp4')` →
  現行 `MediaRecorder(webm)`。機能検出で自動フォールバック、ボタン表示に形式を出す
- WebCodecs経路は**非実時間**（フレームを直接エンコード）なので、
  現行の「実時間・タブ前面のまま待つ」制約が消える。2分コンテでも数秒〜十数秒
- ファイル名 `econte_<ts>.mp4`。フレーム供給は `drawCamFrame()` 経由
  （＝枠列がそのまま出力に効く）→ `drawBurnin()`（§5f）

### 5h. カラースクリプト一覧PNG（SPEC_10 P2 の出口をここで確定）

同じ `cuts[]` のまま**表示単位だけ2つ**用意する（「同期を作らない」原則は維持）:

| 単位 | 使う場所 | 1要素 | サムネ | 尺表示 |
|---|---|---|---|---|
| **CUT単位** | SHEET右パネル | カット | `cam[0]`(A枠)の切り出し | 合計 |
| **枠単位** | カラースクリプト一覧 | 枠 | その枠の切り出し | `A→B 60Fr` |

- SHEET行の右端に **枠配置ミニ図**（`bakeRect` を縮小し、A→B→… の矩形と矢印を描く）。
  撮影指示が1行で読める
- **複合カット（A〜L のように枠が続く）で一覧が埋まる問題**は `cam[].key` で解決:
  - 既定は**始点と終点だけ `key:true`**、中間は false。ユーザーがチェックで足す/外す
  - → 「A,B それぞれ別セル」＝2枠カットは既定でそのまま2セル
  - → 「C,D…と続く複合」＝既定で代表2セルに畳まれ、色が変わる枠だけ手で足す
- 出力: グリッドPNG（既定4列・セル 480×270）。セル下に `C1-A ／ A→B 60Fr`。
  **カット境界に区切り線**を入れ、どこまでが同一カットか読めるようにする
- **プリント前提にしない**（絵コンテ用紙の体裁・ページ分割・ト書き欄は作らない）。
  1枚の長いPNG。画面で色設計を見るためのもの

### 5i. マイグレーション（meta.ver 1 → 2）

初版の「1280×720 を 1536×864 の中央へ載せ替え」は不要になり、**既存データは無改造で成立する**:

```
cut.src → cam:[{k:'A', ...src, key:true}] / bakeRect = src / bakeW,bakeH = 1280,720
cut.src === null（空コマ） → cam:[] / bakeRect:null
```

- ビットマップの再エンコードは発生しない（`baseC/drawC` はそのまま 1280×720 のカットとして生きる）
- 一方向・自動・トースト告知。IndexedDB の `cuts` store に `cam` / `bakeRect` / `bakeW` / `bakeH` を追加

## 6. フェーズ分割（この順で実装）

| フェーズ | 内容 | 規模 |
|---|---|---|
| **V2-A** | STUDIO統合レイアウト＋フォーカス同期＋フルスクリーン＋パレット挙動変更＋TOOL左右入替＋iPadダブルタップ | 大（ただし既存部品の再配置が主） |
| **V2-B** | BOARD強化（ペースト/HEIC/回転/モノクロ輝度/CUT枠編集/統合Undoログ）＋**REF BOARD連携 `photo.ref`（§2-1a・送信側は実装済み）** | 中 |
| **V2-C** | baseAlphaトレース透かし＋EDITピンチズーム＋指=操作・ペン=描画＋2本指UNDO | 中 |
| **V2-D1** | カメラ枠列 `cam[]`＋`bakeRect` 自動算出＋BOARD枠列編集UI＋`drawCamFrame`（**ベイクは1280×720のまま**） | 中 |
| **V2-D2** | 可変ベイク解像度（`CONTE_W/H` 直参照の総点検）＋遅延デコードLRU＋サムネキャッシュ＋はみ出し警告 | 大（データ形式が変わる） |
| **V2-E1** | 小物まとめ（JSON入出力／SHEET行ドラッグ並べ替え／ショートカット一覧／HSVピッカー）… §9f・§9e | 中 |
| **V2-E2** | カラースクリプト編集（EDITのGRIDモード＋`cut.colorC`＋ブレンド）… §9b | 中 |
| **V2-E2b** | GRIDの作り直し（黒背景・ホイール拡縮・可視セル解像度追従・行カラー帯・尺バー）… §9b | 中 |
| **V2-D3** | INFO帯（焼き込み3モード）＋MP4書き出し＋カラースクリプト一覧PNG | 中 |
| **V2-E3** | コラージュ（`cut.layers[]` 最大5＋アフィン変形＋投げ縄マスク＋ブラシチップ）… §9c・§9d | 大 |

- **V2-A〜V2-D2 は実装済み（2026-08-02）**。以降は上表の順（E1 → E2 → D3 → E3）。
  **E2 を D3 より先に**やるのは、GRIDのセルがそのまま一覧PNGのセルになるため（§9b）
- 各フェーズ完了ごとに `node tools/check.js` ＋ 実機param-check表（ユーザー共通CLAUDE.mdの鉄則）
- **V2-D1 と V2-D2 は必ず分ける**。D1 は既存の1280×720ベイクのまま枠列とカメラ補間だけ入るので
  （＝枠が大きくても解像度が足りないだけで動く）、カメラワークの操作感を先に確認できる。
  D2 で初めてサイズが可変になり、ここが唯一の不可逆なデータ変更
- V2-D2 着手前にIndexedDBの構造変更をユーザーに一言確認。**遅延デコードは D2 と同時に入れる**
  （後回しにすると大判カットを作った瞬間に落ちるので、分割してはいけない組み合わせ）

## 7. 変更されるSPEC_10の記述（実装時にSPEC_10へ追記すること）

- 3ビュー（BOARD/SHEET/TIMELINE）→ STUDIO+EDIT の2ビューへ
- `photos[]` に `ref`（REF BOARD の元リンク）を追加… V2-B 以降（§2-1a）
- パレット「色選択で塗りツールへ切替」→ 切替しない（本アプリ独自仕様）
- `cut.src`（単一矩形）→ `cut.cam[]`（カメラ枠列）＋ `cut.bakeRect`… V2-D1以降
- `cut.durF` 手入力 → `Σ cam[].dur` の読み取り専用（FIXカットのみ直接入力）… V2-D1以降
- キャンバスサイズ 1280×720固定 → **カット毎に可変**（`cut.bakeW/bakeH`）。
  `CONTE_W/H` は「本番枠＝出力解像度」の意味に純化… V2-D2以降
- 全カットの canvas 常時保持 → Blob正＋LRU遅延デコード（`getBase()/getDraw()` 経由）… V2-D2以降
- 書き出し WebM実時間 → MP4非実時間（フォールバックあり）… V2-D3以降
- 出口に「カラースクリプト一覧PNG（枠単位・`cam[].key` で間引き）」を追加… V2-D3以降
- STUDIO中央 = プレビュー＋COLLAGE、EDIT = SINGLE＋GRID の2モードずつ… V2-E1以降（§9a）
- `cut.colorC`（カラースクリプト専用の低解像度レイヤー）を追加… V2-E2以降（§9b）
- `cut.layers[]`（カット専用のコラージュ・最大5）を追加… V2-E3以降（§9c）

## 8. 既知の罠（実装者向け・再掲）

- `#tl-strip` 系のポインタ処理に **setPointerCapture を使わない**（dblclickが死ぬ。HANDOVER参照）
- ペイント系は必ず `paintDown(e, cv)` / `renderPaintViews()` 経由（cv決め打ち禁止）
- カット切替時に undoStack/redoStack をクリア（カットまたぎundo破損防止）
- ツール/色/ブラシのUIは EDITサイド と STUDIOミニツール の**両方**を更新する
- 新規idを足したら check.js が配線を実検査する（`$('#id')` 形式で書くこと）
- **V2-D2以降**: `CONTE_W/CONTE_H` をキャンバス寸法として使わない
  （＝出力寸法。カットのビットマップ寸法は `cut.bakeW/bakeH`）。
  特に FILL の `visited` 配列サイズと undo の `getImageData` はここを間違えると静かに壊れる
- **V2-D2以降**: `cut.baseC/drawC` を直接触らない（LRUで null になり得る）。必ず `getBase()/getDraw()`
- **V2-D1以降**: `cut.durF` に代入しない（`recalcDur(cut)` 経由。枠列カットでは導出値）
- **V2-E2以降**: ブレンドモードは**レイヤーの属性**。1ストロークごとに掛けない
  （`globalCompositeOperation='multiply'` で描くと自分の線同士で乗算が重なって濁る）
- **V2-E2以降**: `cut.colorC` は**ベイク空間の低解像度版**（512×288）。LRUの対象外で常時常駐させる
  （一覧で待たずに描けることが目的。ここをLRUに入れるとGRIDが破綻する）
- **V2-E3以降**: `cut.layers[]` は**原本Blob＋変形値**で持つ。ベイクして焼き込まない
  （何度変形しても劣化しない／メモリがベイク寸法に引きずられない）

## 9. V2-E 画面の役割（2026-08-02 ユーザー合意・確定）

「STUDIOで何をして EDITで何をするか」を決め直したもの。**§5h（カラースクリプト一覧PNG）の
作りがここに依存する**ので、D3 より先に E2 を実装する。

### 9a. 役割の再定義

| 画面 | モード | 役割 |
|---|---|---|
| **BOARD**（STUDIO左） | — | 紙のネーム/ラフを並べて**どこを切るか**決める（全カット共通） |
| **STUDIO中央** | `PREVIEW` | 動画コンテのプレビュー。カメラ枠列が効いた絵 |
| 〃 | `COLLAGE` | そのカット専用の合成（§9c）。**紙の全面＝bakeRect** を表示し、カメラ枠はガイド |
| **EDIT** | `SINGLE` | 現行どおり1カット全面の加筆（トレース透かし・ズーム・線画） |
| 〃 | `GRID` | **カラースクリプト編集**（§9b）。セル＝`cam[].key` の枠 |

- **STUDIO中央のペイントは残す**が、ミニツールは**既定OFF**の小型のまま
  （P1の「再生しながらプレビューに直接描いて絵コンテに反映」を殺さないため。
  `✎ TOOLS` で出す現行の仕組みをそのまま使い、初期状態を hidden にするだけ）
- **BOARD と cut.layers の役割を混同しない**:
  BOARD＝全カット共通の「紙」、`cut.layers`＝そのカットだけの合成

### 9b. カラースクリプト編集（V2-E2）

> ⚠️ **この節の「カラー層は枠ごと・出力枠512×288」は 2026-08-18 の SPEC_15 P1 で無効。**
> 色は **カット1枚・ベイク空間（`cut.plateC`＝ベイク×0.4・長辺1024上限）** になり、
> `drawC`（line）とまったく同じ扱い＝カメラ枠で切り出して出力枠へ伸ばす。
> T.U./PAN すると色も一緒に寄る（クロスフェードは廃止）。
> 濃・描画モードは**絵コンテ全体の設定**（`gPaint`）。以下は経緯として残す。

```js
// ⚠️ SPEC_15 P1 で廃止。現行は cut.plateC / cut.plateBlob（カット1枚・ベイク空間）
cut.colors[k] = {           // ★ 枠(cam[k])ごとに1枚。空コマも1枠ぶん持つ
  c,                        // 512×288 の色ラフ（**出力枠1280×720の空間**）
  blob,                     // 保存用PNG
  blend,                    // 'multiply'(既定) | 'normal' | 'lighter'(加算) | 'screen'(発光) | 'overlay'
  alpha                     // 0..1（既定 1.0）
}
```

> ~~**カット単位（ベイク空間）にしてはいけない**~~（2026-08-02 の判断。**2026-08-18 に撤回**
> ＝SPEC_15 P1 でカット単位に統一した。下の「A と B が必ず同じ色になる」は事実だが、
> それは欠点ではなく**紙に色が塗ってある**という正しい振る舞いだった、というのが結論）。
> T.U. の B枠は A枠の内側にあるので、ベイク空間で持つと**A と B が必ず同じ色になる**。
> 出力枠の空間にすると枠ごとに独立し、おまけに座標変換もブラシ倍率補正も不要になる。
> 再生時は区間の始点と終点の色を **先に通常合成でクロスフェードしてから、ブレンドを1回だけ**掛ける
> （ブレンドを2回重ねると中間で濁るため）。

- **合成順**: 下地(bg) → `baseC` → `layers[]` → **`colorC`** → `drawC`
  （合成順そのものは SPEC_15 P1 後も同じ。`colorC` が `plateC` に替わっただけ）
  （線画が最前面。色は写真や線の下に敷く＝色計画として読める）
- **`colorC` はベイク空間**（`drawC` と同じ座標系）。PANカットでは A地点と B地点に
  別の色を置ける＝「紙に色が塗ってある」が正しい振る舞い
- **解像度は 512×288 固定**（≒590KB/カット・60カットで35MB）。
  **LRUの対象外で常時常駐**させる。一覧で待たずに描けることが最優先
- 保存は PNG。`cut.colorC` は IndexedDB の `cuts` store に `colorBlob` として持つ

**GRIDモード**（2026-08-02 にユーザー提供の参考画像＝TS3カラースクリプト等に合わせて確定）:
- セル ＝ `cam[].key` が true の枠（既定は始点と終点。§5h と同じ単位）
- セルの絵は `drawCamFrame(cut, camStartFrame(cut, k))`
- セルに直接描くと `colorC` に入る。座標は **セル → 出力(1280×720) → `camToBake()` →
  colorCスケール** の順で戻す（既存の `paintCoord()` を1段拡張するだけ）
- **背景は黒・セルの枠線なし・余白も黒**。色設計の画面で背景に色味があると色が転ぶので必須
- **装飾は最小**。キャプションは `C1 ／ 2+00` の1行だけ、✎ はホバー/選択時のみ
- **ホイールで拡縮**（カーソル位置基準・0.25〜6倍）。列数は幅から自動計算（既定セル幅200px）。
  指1本=スクロール／2本=ピンチ。iPadは指ダブルタップで SINGLE へ
- **可視セルだけ表示サイズに合わせて再描画**（160px刻み・上限1280×720）。
  見えていないセルは320pxに戻す。これが無いと拡大でボケる／持つと重い
- **行ごとのカラー帯**（参考画像のTS3準拠）: 各セルの代表色6色をヒストグラムで抽出して並べる。
  **導出値なので保存しない**。HUDの「色帯」でON/OFF
- **セル上に尺バー**（コマ数に比例）。枠列の**最終枠は尺を持たないのでバーを出さない**
- カット境界の区切り線は**出さない**（1セル1カットが大半でノイズになるため §5h から変更）。
  同一カットの連続は `C3-A / C3-B` のラベルで読める。行間はセル高の約1/3
- **ブラシの数値は出力枠(1280px)基準**で解釈する（`strokeScaleFor()`）。
  そのまま「レイヤーのpx」にすると、**T.U.した枠のセルだけ急に太く見える**（枠がベイクの一部しか
  使っていないため）。色は面で塗るので、描く先が色のときだけブラシ上限を 300 に広げる
- **コマをまたぐ塗り**: 1ストロークで複数セル＝複数カットの `colorC` に色を流せる。
  セルの隙間ではペンを上げた扱いにして渡り線を出さない。**Undoは触った全カットを1手にまとめる**
- **Undoは場面ごとに分離する（必須）**: EDIT SINGLE=カット別スタック／
  **EDIT GRID=カラー専用ログ（塗りだけ）**／STUDIO=統合ログ。
  GRIDのUndoを統合ログに繋ぐと、連打で `cut-add` まで遡って**カットが消える**（2026-08-02 に発生）
- ✎（SINGLEへ）は **キャプション行（`C1 2+00` の横）に置く**。画の上には重ねない。
  iPad は**指のダブルタップ**でも SINGLE へ入れる
- **セルの複数選択**: キャプションをクリックで選択／Ctrl+クリックで追加・解除／Shift+クリックで範囲。
  選択中は **濃・描画モードが一括で効き**、`Ctrl+↑/↓` で**選択カットをまとめて並べ替え**できる
- 色を編集するのは **GRIDだけ**（色は枠ごとなので、紙の全面を見る SINGLE では対象が決まらない）。
  SINGLE では「描く先＝色」を無効化する
- HUDで **行間ON/OFF** を切り替えられる（詰めて全体を俯瞰したいとき用）
- **この画面がそのまま §5h の一覧PNGになる**（描く画面と出す画面を同じにする＝
  「同期を作らない」原則の延長）

**EDITの主従（2026-08-02 確定）**: **GRIDが主戦場・SINGLEは詰めるとき用**。
トップバーの `EDIT` は GRID を開き、`✎` / ダブルクリック（iPadはダブルタップ）で SINGLE へ入る。

### 9c. コラージュ（V2-E3）

> ⚠️ **`cut.layers[]` は不採用**（2026-08-18・SPEC_15 P2）。原本Blob＋アフィン＋マスクを
> カットごとに最大5枚持つと LRU と保存形式の作り直しが要るため。
> 代わりに **フローティング選択**（同時に1枚だけ・保存しない）で作った
> **＝実装済み 2026-08-19**。焼き込み先は「持ち上げた層」（SPEC_15 **P2-6**。V4で `drawC` は
> 枠ごとのパッチ `cut.fr[k]` になったので、当初の「常に drawC へ」からは変えてある）。
> 以下は経緯として残す。

```js
cut.layers[] = {            // 最大5層
  id, blob, img,            // 原本画像（変形前のまま持つ）
  x, y, w, h,               // ベイク空間での配置（変形前の外接矩形）
  rot, skewX, skewY,        // アフィンのみ
  blend, opacity,           // 'normal'|'multiply'|'lighter'|'screen'|'overlay'
  mask: { pts: [[x,y],…], invert } | null   // 投げ縄（レイヤーローカル正規化座標）
}
```

- **変形はアフィンまで**（移動・回転・拡縮・傾き）。`ctx.setTransform` で1発。
  **台形/パースは見送り**（Canvas2Dに透視変換が無く、メッシュ分割かWebGLが要るため）
- **投げ縄は非破壊マスク**。パスの点列で保持し、描画時に `clip()`。
  切り直せる／Undoが軽い／保存が小さい。「完全に切り取る」は必要になってから足す
- **原本Blob＋変形値**で持つ（ベイクしない）。何度変形しても劣化しない
- 編集は **STUDIO中央の COLLAGE モード**。紙の全面を表示し、カメラ枠はガイド線
- **指＝変形ハンドル／ペン＝描画**（V2-C の切り分けを踏襲）
- LRU: `layers[].img` も cut と一緒に出し入れする（`ensureResident` の対象に足す）
- サムネ／プレビュー／動画書き出しは `compositeTo`/`drawCamFrame` を通るので**自動で反映される**

### 9d. ブラシ（V2-E3）

- **`.abr`（Photoshopブラシ）のパーサは作らない**。v6以降はバイナリdescriptor構造で
  対応版にムラがあり、単一HTML方針に合わない
- 代わりに:
  1. **内蔵チップ3〜5種**（丸ソフト／チョーク／かすれ／テクスチャ）を data URI で焼き込む
  2. **透過PNGをブラシチップとして読み込む**（Photoshop側からチップをPNG書き出しすれば済む）
- ユーザーから「このブラシみたいにしたい」と参照が来たら、**その見た目を内蔵チップとして再現する**
  （2026-08-02 時点でユーザーから追って依頼予定）
- チップは `colorC` と `drawC` の両方で使える

### 9e. HSVカラーピッカー（V2-E1）

- **H / S / V の3スライダー**（ユーザー提供の参考画像どおり）:
  - H: 0–360、虹のグラデーション帯
  - S: 0–100%、現在のH/Vに応じたグラデーション
  - V: 0–100%、現在のH/Sに応じたグラデーション
  - 右端に数値表示＋上下ステッパー
- **既存の48色パレットと併用**（パレット＝保存した色、HSV＝作る色）。
  localStorageキーは現行 `econte_palette_v1` のまま
- 置き場所: EDITサイドバー と STUDIOミニツールの**両方**（既存の鉄則どおり）

### 9f. 小物（V2-E1）

- **IMPORT / EXPORT JSON**: ZIP（JSZip・CDN）。
  中身は **写真の原本＋`cam[]`＋`drawC`＋`colorC`＋`layers[]`＋メタ**。
  **`baseC`（ベイク）は入れない** — BOARDから再生成できる派生物なので、
  入れるとファイルが数十MB単位で膨らむ。読み込み後に `rebakeFromCam()` で焼き直す
- **SHEET行の上下ドラッグ並べ替え**（ショートカット `Ctrl+↑/↓`）。**▲▼ボタンは撤去**。
  ドラッグ中は挿入位置のプレビューを出す
- **SETTINGSにショートカット一覧**（表示のみ・アプリ内変更は不要）。
  `SHORTCUT_ACTIONS` が既にレジストリ化済みなので、一覧を描くだけ

### 9g. 既存データの扱い

2026-08-02 時点でユーザーは**テスト段階**のため、**既存データの互換は考慮しなくてよい**
（＝レイヤー導入時にマイグレーションを書かなくてよい）。ただし
「読めなくなる変更」を入れるときは、そのフェーズの報告で明示すること。
