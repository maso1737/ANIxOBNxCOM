# SPEC_15 — ECONTE V3「ペイントの一本化」

`econte.html` のペイント周りを作り直す仕様。**着手前に SPEC_13 §9（画面の役割）と
ECONTE_HANDOVER.md の V2-G3 を読むこと。**

起点は 2026-08-17 のユーザー観察:

> T.U.の場合、SINGLEでLineに描いたペイントはタイムラインでT.U.対応しててうごく。
> colorに描いたペイントはタイムラインでフェードで変わる。
> てっきり同期しているとおもってたんですが、これって両方Lineに描いたペイントみたいに
> しておかないメリットありますか？

> 濃 は各カットで設定でなく、下設定で全部に対して連動しててほしいです。特にPANやT.U.のときかな、
> 塗ってみて違う見え方になりあちこちで合わせたり確認の必要がでます。
> 描く先とブラシサイズも各じゃないほうがいいです。

**戻り先タグ: `econte-v2-g3`（`32f5862`）。**
`git checkout econte-v2-g3 -- econte.html` で着手前に戻せる。

実装順は **P1 → P2 → P3**。P1 を先にやらないと P2 の焼き込み先が決まらない。

>  ⚠️ **続きは `SPEC_16_ECONTE_V4.md`。** P1 の「カット1枚」は実使用で
>  「A枠に描いた線が B枠のセルにも出る」に当たったため、**枠ごとの画**へもう一段進めた。
>  P1 の土台はそのまま使っている（フェードには戻さない）。
>  **P2 フォトバッシュは V4 の後に実装済み（2026-08-19）** → P2-6 に V4 への読み替えを記載。

**状況（2026-08-19）: P1 / P2 / P3 すべて実装済み。**
- P1・P3-1 … 2026-08-18（`gPaint` は P1 で色から per-slot の 濃/モード が消える時点で必要になるため、切り離せなかった）
- P3-2〜P3-4 … SPEC_16 §5-B1 として V4 と同じ回で実装
- **P2（フォトバッシュ＝浮いた選択）… 2026-08-19。V4 の「枠ごとのパッチ」に合わせて2点だけ読み替えた**（下記 P2-6）

実装の要点と落とし穴は `ECONTE_HANDOVER.md` の **V3-P1 / V3-P2** に記録。

---

## 0. なぜ今こうなっているのか（消す前に理由を残す）

`drawCamFrame()` の中で2つの層は**別の場所に立っている**:

- **line（`drawC`）** … ベイク空間。カメラ枠で切り出して出力枠へ伸ばす → **カメラの後ろ（＝セルの上）**。T.U.すると一緒に寄る
- **color（`colors[k]`）** … 出力枠空間 512×288 を枠ごとに1枚。カメラ変換を通さず出力枠にベタ置き → **カメラの前（＝レンズの前のゲル）**。T.U.しても寄らず、代わりに A枠の色 → B枠の色 をクロスフェードする

同期していないのではなく、**撮影台の上とレンズの前という別の場所にいる**。
分けてある理由は2つあった:

| | 理由 | 判定 |
|---|---|---|
| (a) | 枠ごとに色を変えたい（寄りながら色が変わる演出） | **これが唯一の本当の理由。P1で捨てる** |
| (b) | 512×288 と小さいのでLRUに載せず常駐でき、GRIDが待たされない | **要る。P1でも保つ**（下の寸法規則で成立する） |

---

## P1. line / color の一本化（カラー層をベイク空間の1枚へ）

### P1-1. データ構造

```
削除: cut.colors[]  = [{ c, blob, blend, alpha, dirty }, …]   ← 枠ごと・出力枠512×288
新設: cut.plateC    = HTMLCanvasElement                        ← カット1枚・ベイク空間
      cut.plateBlob = Blob (PNG)
      cut.dirtyPlate = bool
```

blend / alpha は**カットから外して全体設定へ**（P3）。

### P1-2. 寸法規則（ここが (b) を保つ鍵）

```js
const PLATE_K = COLOR_W / CONTE_W;   // = 0.4
const PLATE_MAX = 1024;              // 長辺の上限
function plateSize(cut){
  const b = bakeSize(cut);
  let w = Math.round(b.w * PLATE_K), h = Math.round(b.h * PLATE_K);
  if(w > PLATE_MAX){ h = Math.round(h * PLATE_MAX / w); w = PLATE_MAX; }
  return { w: Math.max(16, w), h: Math.max(9, h) };
}
```

`calcBakeSize()` はすでに「**一番寄った枠が1280pxになるように**」ベイクを組んでいる。
その 0.4 倍ということは「**一番寄った枠に色が512px当たる**」＝**今と同じ色解像度**。

| カット | 今（枠ごと） | P1後（ベイク1枚） |
|---|---|---|
| FIX | 590KB | 590KB（同じ） |
| T.U. 1.4× | 2枚 = 1.18MB | 717×403 = 1.16MB（同じ） |
| T.U. 3× | 2枚 = 1.18MB | 1024×576 = 2.4MB（**PLATE_MAXで頭打ち**） |

**`plateC` は LRU に載せない**（今の `colors` と同じ扱い）。上限があるので載せなくても持てる。

### P1-3. 描画（合成順は変わらない）

`compositeTo()` / `drawCamFrame()` の合成順 `bg → baseC → 色 → drawC`（`baseOver` 時は base が最後）は
**そのまま**。変わるのは色の置き方だけで、**line とまったく同じ扱い**になる:

```js
// drawCamFrame の枠列カット経路（sx,sy,sw,sh はカメラ枠の切り出し）
const p = getPlate(cut);
if(p) ctx.drawImage(p, sx*PLATE_K, sy*PLATE_K, sw*PLATE_K, sh*PLATE_K, 0, 0, OW, OH);
```

`globalAlpha` / `globalCompositeOperation` は**全体設定**（P3）から1回だけ掛ける。

### P1-4. 座標とブラシ（特殊扱いが全部消える）

```js
// paintCoord: 色もベイク空間を経由するだけ
if(state.paintTarget === 'color'){
  const b = toBakeCoord(clientX, clientY, cv);
  return { x: b.x * PLATE_K, y: b.y * PLATE_K };
}

// gStrokeK: line と同じ関数で済む（枠ごとの特例が消える）
gStrokeK = strokeScaleFor(cut, tgt.width, ca ? ca.local : 0);
```

`strokeScaleFor()` は展開すると **`k = 見ている枠の幅 ÷ 一番寄った枠の幅`**。
だから「6」は常に**出力画面での6px**を意味する。line も color も同じ物差しになる。

### P1-5. 消えるもの（V2-G3で足したものを含む）

| 関数 / 状態 | 扱い |
|---|---|
| `colorMixAt` | **削除**（クロスフェードが無くなる） |
| `drawColorFrame` | **削除** |
| `drawColorPlate` | **削除**（P1-3が代わり） |
| `bakeToColorCoord` | **削除** |
| `frameAtBake` | **削除**（V2-G3で追加したもの） |
| `plateStrokeSeg` / `gPlateMode` / `strokeCoord` | **削除**（V2-G3で追加したもの） |
| `paintSnap` の `{all:[…]}` 分岐 / `applySnap` の `snap.all` 分岐 | **削除**（1枚に戻るので不要） |
| `colorSlots` / `colorSlot` / `getColorAt` / `colorBlendOf` / `colorAlphaOf` | **`getPlate(cut)` 1本に集約** |
| `eachSelSlot` | **`eachSelCut`** に（枠ではなくカット単位） |
| `cellColorCoord` / `gridStrokeTo` | **残すが中身を差し替え**（セル → ベイク → plate の2段変換） |
| `strokeSegOn` の `if(d === cut.colorC)` | **削除**（そもそも死にコード） |
| `syncEditPanels` の「（いま A 枠）」表示 | **削除** |
| `clearDraw` の `wide` 分岐 | **削除**（1枚なので分岐不要） |

### P1-6. 移行（既存データ）

読み込み時、`c.colors` があって `c.plate` が無ければ1回だけ変換する:

```
plateC を作る
→ 各枠 k について、colors[k] を camRect(k) の位置・大きさでベイク空間へ描く（k の昇順）
→ cut.dirtyPlate = true（次の保存で plate として焼き直る）
```

**これは `drawColorPlate()` が今まさに SINGLE に表示している絵そのもの。**
つまり「**SINGLEで見えていたものが、そのまま正になる**」。見た目が変わらないまま仕組みだけ減る。

- 枠ごとに色を変えていたカットは、**後の枠が上に乗った1枚**に潰れる（重なり部分は B が勝つ）
- 変換したカット数をトーストで出す。黙って潰さない
- `PROJECT ZIP`（`exportProject` / `importProject`）も同じ移行を通す

### P1-7. 失うもの（実装前にユーザー合意済み・2026-08-17）

1. **T.U./PAN の途中で色が変わる演出**（A枠=暖色 → B枠=寒色 のフェード）。必要ならカットを2つに割る
2. GRIDで T.U.カットの A/Bセルが「同じ絵の広い版と寄った版」になる（Bセルを塗るとAセルにも出る）
3. カラースクリプト（GRID / ⇩C.SCRIPT）は**そのまま動く**。A=広い版・B=寄った版として出る

---

## P2. 軽いフォトバッシュ（浮いた選択）【実装済み 2026-08-19】

**SINGLE 限定。GRID には出さない**（SPEC_13 §9 — GRIDに重い変形UIを持ち込むとセル数ぶん効く）。

### P2-1. SPEC_13 V2-E3 の `cut.layers[]` は採用しない

E3 の「原本Blob＋アフィン＋マスクを最大5枚、カットごとに保存」は、
ハンドオーバーが警告している通り LRU と保存形式の作り直しが要る。**採用しない。**

代わりに **フローティング選択**（Photoshopで投げ縄→ドラッグしたときの手触り）にする。

| | `layers[]`（E3） | 浮いた選択（本仕様） |
|---|---|---|
| 常時のメモリ増 | カット×5枚の原本 | **同時に1枚だけ・保存しない** |
| 保存フォーマット | 変更が要る | **変更なし** |
| LRU | 作り直し | **触らない** |
| Undo | 専用の仕組み | 既存の `pushPaintLog` パターン |
| 変形のやり直し | あとから何度でも | 確定後は不可（Ctrl+Zで戻してやり直す） |
| 実装量 | 大 | **250〜350行** |

### P2-2. 状態

```js
let gFloat = null;
// { src:'base'|'line'|'color',        持ち上げ元
//   cv,                               マスク済みの中身（bbox寸法・ベイク空間）
//   x, y, w, h,                       いまの配置（ベイク空間）
//   ox, oy, ow, oh,                   持ち上げた時の配置（Escで戻す）
//   before: { line:ImageData, src:ImageData|null } }
```

**同時に1枚だけ。** bbox が `MAX_AREA/2` を超えたら「範囲が大きすぎます」で断る。

### P2-3. 操作

1. **`SEL` ツール**（PEN/ERASE/FILL の隣に4つ目・ショートカット `A`）でドラッグ＝投げ縄
   マスクは `lassoFillPolygon()` のスキャンラインを流用する
2. 離すと**持ち上がる**。変形枠（4隅ハンドル＋辺）が出る
   - 枠の内側をドラッグ = 移動
   - 隅 = 拡大縮小（**既定は縦横比維持**・`Shift` で自由）
   - 回転は v1 では入れない（要望が出たら枠の上に専用ハンドルを足す）
3. **確定** = `Enter` ／ 枠の外をクリック ／ ツール変更 ／ カット移動
4. **取り消し** = `Esc`（元の位置へ戻す）

### P2-4. 安全のためのルール（ここは動かさない）

- **焼き込み先は常に `drawC`（line層）。`baseC` には絶対に書かない。**
  元の絵が無傷なら ⟳（BOARDから再ベイク）がいつでも安全に押せる
- **BASE からはコピーしか取れない**（元に穴を開けない）。
  BASE は ⟳ で焼き直せる唯一の層なので、そこに穴を開けると ⟳ が破壊的な操作に見えてしまう。
  元を隠したければ line 層の上から塗る
- LINE / COLOR からは**切り取り**（元から消える）。`Alt` ドラッグでコピー
- 色を持ち上げた場合も**焼き込み先は line**。plate には戻さない
  （plate は「カット全体の色」なので、切り貼りの受け皿にしない）

### P2-5. 描画とUndo

- 浮いている絵は **`renderEditCanvas()` の中で `compositeTo()` の後に1回描くだけ**。
  `compositeTo()` 自体には入れない → **サムネ・GRID・書き出しには出ない**（確定するまで存在しない）
- 変形枠は `edit-cv` の上に直接描く（`drawLassoPreview` と同じ場所）
- Undo は `gridStrokeUp()` と同じ形。**持ち上げ元と `drawC` の before/after を1手にまとめて** `pushPaintLog`

### P2-6. V4（枠ごとのパッチ）への読み替え【実装時に決めた2点・2026-08-19】

本節は `plateC` / `drawC`（カット1枚）前提で書いてある。V4 で画が `cut.fr[k]` の
**枠ごとのパッチ**になったので、次の2点だけ差し替えて実装した。**残りは仕様のまま。**

| | 仕様（V3前提） | 実装（V4） | 理由 |
|---|---|---|---|
| 1 | 中身は1枚の層から切り出して1枚の層へ戻す | **紙（ベイク空間）の1枚として持ち上げ、確定時は重なる枠ぶん全部のパッチへ落とす** | 投げ縄塗り（`lassoEnd`）とまったく同じ配り方＝**面で効く操作の振る舞いがツール間でそろう**。持ち上げの切り取りも同じ枠集合から抜く＝同じ場所で確定すれば往復で元に戻る |
| 2 | 「色を持ち上げても焼き込み先は line」（P2-4） | **持ち上げた層へ戻す**（line→line / color→color。**BASE→line は仕様どおり**） | P2-4 の理由は「plate はカット全体の色だから受け皿にしない」だったが、それは plate がカット1枚だった V3 の話。V4 で plate も line と同じ枠ごとのパッチになったので、color を line に落とすと**カラースクリプト（色帯・C.SCRIPT）から絵が消える**。切り替えたいときは `floatKindOf()` の1行 |

**安全弁（P2-4）はそのまま動かしていない**: BASE からはコピーだけ・元に穴を開けない／
浮いている絵は `renderEditCanvas` でしか描かない（サムネ・GRID・書き出しには出ない）。

操作の割り当て（`Alt` は既にスポイトなので P2-3 の「Altドラッグ＝コピー」だけ移した）:

| | |
|---|---|
| `A` / SEL ボタン | 選択ツール（**SINGLE 以外ではグレー。持ったまま出ると PEN に戻る**） |
| そのままドラッグ | いまの「描く先」から**切り取り**で持ち上げ |
| `Ctrl`＋ドラッグ | **コピー**で持ち上げ（元が残る） |
| `Shift`＋ドラッグ | **REF（写真）からコピー**（常にコピー・焼き込み先は line） |
| 枠の中 / 隅 | 移動 / 拡縮（既定は縦横比維持・`Shift` で自由）。回転は無し |
| `Enter` / 枠の外 / ツール変更 / 画面移動 | 確定 |
| `Esc` | 取り消し（**Undoログを汚さない**） |

---

## P3. 濃・描く先・ブラシを「下設定」へ

依頼の直接の理由:

> 特にPANやT.U.のときかな、塗ってみて違う見え方になりあちこちで合わせたり確認の必要がでます。

枠ごと・カットごとに濃さが違うと、同じ色で塗ったつもりでもカメラが動いた先で見え方が変わる。
**色の濃さと描画モードはカット固有の情報ではなく、この絵コンテ全体のトーン。**

### P3-1. 全体設定へ移すもの

| 今 | P3後 | 保存先 |
|---|---|---|
| `colorSlot(cut,k).alpha`（枠ごと） | `gPaint.colorAlpha`（**全体で1つ**） | `UI_LS` ＋ PROJECT ZIP の meta |
| `colorSlot(cut,k).blend`（枠ごと） | `gPaint.colorBlend`（**全体で1つ**） | 同上 |

- `#color-alpha` / `#color-blend` を触ると**全カットの見え方が同時に変わる**
- `eachSelSlot()` は不要になるので削除（一括変更する対象がそもそも1つになる）
- パネル見出しは `COLOR LAYER（カラースクリプト・全体設定）` にして、カット固有でないことを明示する
- **カットごとの上書きは作らない。** 要望が出るまで足さない

移行: 既存の per-slot 値の**最頻値**を初期値にする。同数なら先頭カットの値。
「濃を全体設定にしました（◯◯% を採用）」とトーストで出す。

### P3-2. 画面を変えても勝手に切り替わらないようにする

`setEditMode()` の以下の1行が「描く先が各画面ごとに違う」の正体:

```js
setPaintTarget(m === 'grid' ? 'color' : 'draw');   // ← これを消す
```

- **描く先は選んだら選んだまま。** SINGLE ⇄ GRID ⇄ STUDIO を行き来しても変わらない
- `setView('studio')` の `setPaintTarget('draw')` も同様に**消す**
- 初期値だけ `'draw'`。以後は `UI_LS` に覚える

### P3-3. ブラシサイズを1つにする

```js
function brushMax(){ return state.paintTarget === 'color' ? 300 : 64; }   // ← 廃止
const BRUSH_MAX = 300;                                                    // ← 1つに
```

上限が描く先で変わると、line→color→line と往復しただけで数字が勝手に変わる（64でクランプされる）。
**上限は1つ。数字も1つ。** `UI_LS` に覚える。

### P3-4. 「下設定」の置き場所

上の3つは EDIT サイドパネルの現在地のままでよい（`COLOR LAYER` / `BRUSH`）。
**ラベルに「全体」と入れる**ことだけが要件。パネルを新設しない。

---

## 4. 実装後に必ず出すもの

`node tools/check.js`（6ファイル一括）に加えて、**動作チェック表**（ユーザー共通ルール）。
最低限これは実機で振ること:

| 確認 | 期待 |
|---|---|
| T.U.カットの色が寄りに追従する | line と同じ動き。フェードしない |
| 出力枠での線の太さ | A枠で描いても B枠で描いても、画面上は同じ太さ |
| 既存プロジェクトの移行 | SINGLEで見えていた絵と一致。トーストに変換件数 |
| 濃を動かす | 全カットが同時に変わる |
| SINGLE ⇄ GRID ⇄ STUDIO | 描く先・ブラシサイズが変わらない |
| フォトバッシュ確定 | Ctrl+Z 1回で持ち上げ前に戻る |
| フォトバッシュ中のサムネ/書き出し | 浮いている絵は**出ない** |
| ⟳ 再ベイク | BASE が無傷。フォトバッシュ結果は line に残る |
| iPad | 指=選択・ペン=描画の切り分けが崩れていないこと |

`verify/` の決定論VRTは econte 未対応。**合成順に触るので、対応させるならこの機会**（SPEC_08）。

---

## 5. 関連

- 本仕様の背景を触って理解する教材 →
  `LP_motion-graphics/TOOL_MECHANICS/SPEC_LAB_01_SPACES.md`
  （P1の判断＝「色をレンズから外してセルの上へ置き直す」を、実装前に画で確かめられる）
- `PIPELINE.md` — 色の保存形式が変わるので **P1 実装時に更新すること**
- SPEC_13 §9b — 「カラー層は枠ごと」の旧規定。**P1で無効になるので該当箇所に取り消し線を入れる**
- SPEC_13 V2-E3 — `cut.layers[]` は **P2 で不採用**を明記する
