# SPEC_18 — iPad 操作文法の統一（IPAD GRAMMAR）

**状態: P0 実装済み（2026-08-26〜27）。P1 以降は未着手。**

SPEC_17（キーの押し方）の iPad 版。**指とペンで役割を分け、5本のツールで同じ手つきにする。**

> ⚠ **この SPEC の数値は推測ではなく実機で測ったもの。** 測定器は `ipad-probe.html`。
> 測定環境: iPadOS 18.7 / Safari 26.6 / 1032×1376 @2x / 最大同時タッチ点 5 / 2026-08-26〜27。
> **仕様を変えるときは probe で測り直してからにする。**

---

## 1. 第一原則 — 指とペンで役割を分ける

| | 役割 |
|---|---|
| **指** | ナビゲーション・UI操作・数値スクラブ・多指ショートカット |
| **Apple Pencil** | **描く面だけ。** ブラシ・投げ縄・コマ割り線・アンカー移動 |

**キャンバス上で指は描かない。UI 面でペンは「ただの指」として振る舞う**（掴まない・フォーカスを渡さない）。
Procreate / CSP / Nomad Sculpt / Fresco すべてこの分離を採っている。

この1行を立てると、以下の判断がほぼ自動的に決まる ——
「ペンが入力欄に触れたらどうする？」→ **フォーカスを渡さない（＝Scribble が起きない）**。
「指がキャンバスに触れたら？」→ **視点操作**。

---

## 2. 実測で決着したこと

### 2-1. 多指タップ（②タブ・2日分）

| 本数 | 実測 | 判定 |
|---|---|---|
| 1本 | 届く | UI操作に使う |
| 2本 | 届く | **UNDO**（業界標準どおり） |
| 3本 | **20回すべて届いた**（pd:ON / pd:OFF とも） | **REDO。使ってよい** |
| 4本 | 16回届き、**アプリは一度も切り替わらなかった** | **PREVIEW 再生／停止** |
| 5本 | 6回届いた。ただし**「上手くやらないと別アプリに変わるズレが起きる」** | **割り当てない** |

**結論の要点:**

- **3本指は使える。** 研究資料B（と OBAN の 2026-08 のコメント）が言う「OSが横取りするから不可能」は
  **この端末では否定された**。OBAN のコメントは、3本指をキャプチャ⇔REDOと付け替えていた最中の
  観測だったと考えられる。**`oban-builder.html` の該当コメントは訂正すること。**
- **`preventDefault` は多指タップの到達には要らない。** ON と OFF で届き方が変わらなかった
  （ログの `[pd:ON]` / `[pd:OFF]` 刻印で確認）。preventDefault が要るのは
  **ピンチ・スクロールを取られないため**であって、タップの到達のためではない。
- **OSの編集バーが出ても、やり直しは実行されていない。**
  「OS横取り検出」（画面外の contenteditable で `beforeinput(historyUndo/historyRedo)` を捕まえる罠）は
  **2日間で一度も発火しなかった**。＝二重実行ではなく、バーが表示されるだけ。
- **バーが出る条件は「入力欄にフォーカスがあるとき」** ——1日目は④タブで number 欄を触った後だったので出た。
  2日目は触らずに撃ったら**両モードとも出なかった**。
  つまり **§2-2 の「数値欄を div にする」を実施すれば、バーは自然に出なくなる**（要・追試）。

### 2-2. Scribble（④タブ）

| | Scribble | キーボード | スクラブ | Wタップで打ち換え |
|---|---|---|---|---|
| A 素の `input[type=number]` | **出る**（手書きオンリーになる） | 出る | ✕ | — |
| B `readonly` な input | **出ない**（一切無反応） | 出ない | ✕ | — |
| C `inputmode="none"` | **出る** | 出ない | ✕ | — |
| **D div スクラブ** | **出ない** | 出ない | **○** | **○** |

**D が正準。** `<input>` を DOM に置かず `<div>`（または `<span contenteditable="false"`）で値を表示し、
`pointerdown` で `preventDefault()` + `setPointerCapture()` → 横ドラッグで数値を変える。
**Wタップのときだけ `<input>` を動的生成**して focus / select、確定または Esc で破棄する。

> ⚠ **研究資料2本はどちらも「readonly では Scribble が出る」と書いているが、実機では出なかった。**
> ただし readonly はスクラブと両立しないので、正準にはしない。**資料より実機を採る。**

### 2-3. standalone（①⑤タブ）

| 項目 | 実測 |
|---|---|
| `navigator.standalone` | `true` |
| `display-mode: standalone` のメディアクエリ | **`browser` と出る＝当てにならない** |
| `env(safe-area-inset-top)` | **32px** |
| `env(safe-area-inset-bottom)` | **20px**（ホームバー） |
| Fullscreen API | **無い**（⛶ ボタンは消す） |
| `ctx.filter` | **プロパティ自体が無い**（econte の `CTX_FILTER_OK` 代役が正しく効いている） |

- **standalone の判定は `navigator.standalone` を見る。** メディアクエリは使わない。
- **`<link rel="manifest">` を張ってはいけない。** 張ると「ホーム画面に追加」が
  そのページではなく manifest の `start_url` をインストールする（2026-08-26 に実際に踏んだ）。
  standalone 化は **`apple-mobile-web-app-*` メタだけ**で足りる。
- **ホーム画面のアイコンは index の1枚で足りる。** 当初「5本それぞれ追加が要る」と見えたが、
  P0 のメタが全ページに乗った後は **ANIMATOR→COMPOSER の LIVE 遷移でも URL バーが出なくなった**。
- **⛶ 全画面ボタンは `canFS` 判定で消す**（standalone では API が無い＝すでに全画面）。

### 2-4. 筆圧（③タブ）

| 項目 | 実測 |
|---|---|
| 1発目の pressure | 0.0800 |
| 2〜7発目の中央値 | 0.0800 |
| **着地スパイク** | **無い**（1.00倍） |
| `getCoalescedEvents` | **3.98 点 / move**（＝取りこぼしを防ぐには必須） |
| pressure が **ちょうど 0** の点 | 1363点中 **37点** |

- **着地スパイクは存在しなかった。** 研究資料2本の「1発目は信用できない」は、この端末には当てはまらない。
- **代わりに、ストローク中に pressure が 0 へ落ちるサンプルが多数ある（37/1363）。**
  センサーのノイズが実在する＝**高い側へ跳ねるサンプルもあると考えるのが自然**。
- probe（平滑化なし・弧長テーパー切替あり）では**インク溜まりが再現しない**。

---

## 3. 統一ジェスチャ表（決定）

| 入力 | 操作 | 割り当て | 備考 |
|---|---|---|---|
| **Pencil** | down / move | **描画・直接編集のみ** | UI 面ではフォーカスを渡さない |
| 1本指 | tap | UI操作・要素選択 | **キャンバス上の1本指では描かない** |
| 1本指 | drag | 視点パン／数値スクラブ | |
| 1本指 | hold 300ms | 対象の掴み・スポイト | リストの並べ替えには使わない（§4-2） |
| **2本指** | **tap** | **UNDO** | 全ツール共通 |
| 2本指 | pinch | **対象の上＝その対象／余白＝視点** | manga / oban の型が正準 |
| **3本指** | **tap** | **REDO** | 使ってよい（§2-1） |
| **4本指** | **tap** | **PREVIEW 再生／停止** | oban の実績。animator / composer / econte へ展開 |
| **5本指** | — | **割り当てない** | アプリ切替に化ける（§2-1） |

**しきい値（全ツール共通）**

| 定数 | 値 | 根拠 |
|---|---|---|
| タップ成立 | `< 300ms` かつ移動 `< 14px` | 既存3本の実装値と同じ |
| Wタップ成立 | `< 320ms` | SPEC_17 と同じ |
| **「動かさずに離した」判定** | **`10px`** | **3px だとふつうのタップの手ブレで潰れる（実機で踏んだ）** |
| リストの掴み（ハンドル無しの場合） | `300ms` / キャンセル `10px` | 研究2本が一致 |
| 筆圧 EMA の α | `0.15〜0.30` | 研究2本が一致 |

---

## 4. 各ツールで直すこと

### 4-1. 数値欄を div スクラブへ（§2-2）

| ツール | 対象 |
|---|---|
| `composer.html` | `.kf-val` / `.fx-row input` の全プロパティ（`attachScrub` を div 版へ） |
| `animator.html` | `#fps-input` |
| 他 | 素の `input[type=number]` を順次 |

`composer.html` の現状は「入力欄はクリック=数値入力を許可するため preventDefault しない」という
コメント付きの実装になっている。**この方針を反転させるのが本 SPEC の要。**

### 4-2. リストの並べ替えは ≡ ハンドルから

研究2本とも「専用ハンドルが iPadOS の標準作法／長押しはスクロールと誤干渉する」で一致。

| ツール | 現状 | やること |
|---|---|---|
| `econte.html` | 長押し350msで掴み＋**1本指を置いたまま他をタップで複数選択** | 掴みを ≡ ハンドルへ移す。**複数選択の型はそのまま残す**（これは良い） |
| `composer.html` | **キーボード（↑↓）しか入口が無い** | econte の型を移植 |
| `manga-plate.html` | ▲▼ボタンのみ | ハンドルドラッグを足す |

**掴みをハンドルに限ると、行本体の長押しと2本目タップが全部空く**＝取り合いが消える。

### 4-3. 多指タップの統一

| ツール | 2本 | 3本 | 4本 | 5本 |
|---|---|---|---|---|
| animator | ✅ある | ✅ある | **足す** | — |
| composer | ✅ある | ✅ある | **足す** | — |
| econte | ✅ある | ✅ある | **足す** | — |
| manga-plate | ✅ある | ✅ある | （プレビュー無し＝不要） | — |
| oban | ✅ある | **REDOとして復活させる** | ✅ある | **CAPTURE を外す** |

### 4-4. standalone まわり

- 正準は **`black-translucent` + `viewport-fit=cover` + `env(safe-area-inset-top)` の押し下げ**
  （OBAN が実装済み・実機で確認済み）。全画面を使えて、なお重ならない。
- 他4本は `default`（不透明バー）のまま。**急がない。** 揃えるときは OBAN の `#bar` を写す。
- **`<link rel="manifest">` は張らない。**

### 4-5. ANIMATOR の点在インク（保留・任意）

**当初の「着地スパイク説」も「手ブレ補正説」も実機で否定された**
（補正 OFF でも出る／probe では着地スパイクが無い）。

残る説明は **`e.pressure` のサンプルノイズ**。probe が同じ端末で
**1ストローク1363点中37点が pressure ちょうど 0** を記録している＝センサーは実際に跳ねる。
[`animator.html`](animator.html) は **筆圧を一切平滑化せず生値を `pressureRadius()` へ渡している**ので、
高い側へ跳ねた1サンプルがそのまま太い1点として残る。

- 直すなら **EMA 1行**（`pSm += (p - pSm) * 0.25`）。機能追加ではないので重くならない。
- ただし**ユーザー判断で「本物のインクのようで味」として許容もあり**。**急がない。**
- 弧長テーパー（入り12px・抜き12px）は probe で効果を確認済み。入れるなら EMA と同時。

---

## 5. 進め方

- [x] **P0** メタ／safe-area／⛶ボタン／probe（2026-08-26〜27）
- [ ] **P1** `composer.html` をパイロットで通す（§4-1 数値欄・§4-2 トラック並べ替え・§4-3 4本指）
- [ ] **P2** スキル化（`ipad-input-kit`）。**P1 を通してから**——いま作ると誤った正準を固める
- [ ] **P3** 横展開 econte → manga → oban → animator
- [ ] **P4**（任意）ANIMATOR の筆圧 EMA ＋ 弧長テーパー

**パイロットが composer な理由**: 不満が集中していて、iPad 対応が一番薄く、かつ「描かない側」なので
**UI 面の文法だけを純粋に固められる**。描く面は animator が正準なので混ぜない。

---

## 6. 実装上の落とし穴（probe を作って実際に踏んだもの）

**すべて本体側にも同型がありうる。P1 で移植するとき必ず見ること。**

1. **`getCoalescedEvents()` は空配列を返すことがある。** そのまま回すと move の点を全部落とす
   → `if(!list || !list.length) list = [e];` を必ず入れる
2. **`setPointerCapture` は普通に例外を投げる**（`NotFoundError`）→ try/catch しないとストロークが丸ごと消える
3. **`display:none` の中で canvas を初期化すると 0×0 のまま。** タブを開いた時点で採寸し直す
4. **`lastTap = 0` の初期値は罠。** `performance.now()` がまだ小さい起動直後300msだけ、
   一発目が2連打と誤判定される → 十分過去（`-1e9`）を入れる
5. **`input.remove()` がその場で `blur` を誘発して確定処理が二重に走る** → 参照を先に null にしてから消す
6. **`'filter' in ctx` は当てにならない。** 実際にぼかして確かめる
   （`oban-builder.html` の `probeCanvasFilter` が正準）
7. **`getCoalescedEvents()` の返すオブジェクトはプールされている**（資料B）。
   配列に持つなら `{x, y, pressure}` に値コピーする

---

## 7. まだ決めていないこと

- **OSの編集バーの発生条件**——「入力欄にフォーカスがあるとき」という推定の追試。
  §4-1（数値欄の div 化）を composer でやった後、3本指タップでバーが出ないことを確認する
- 他4本を `black-translucent` へ寄せるか（§4-4）
- ANIMATOR の筆圧 EMA を入れるか（§4-5）

---

## 8. 関連

- `ipad-probe.html` / `ipad-probe-nav.html` … **測定器。仕様を変えるときはここで測り直す**
- `SPEC_17_INPUT_GRAMMAR.md` … キーの押し方（本 SPEC の姉妹編）。**しきい値はあちらと揃える**
- `_Research/iPadOS Webアプリジェスチャ設計調査.md` … 研究稿A
- `_Research/NotebookLM_iPadOS 26におけるPWA型プロ向け制作ツールのタッチ.md` … 研究稿B
  （**AとBは3本指の可否で対立している。実機はAを支持した**）
- `SPEC_16_ECONTE_V4.md` §5-D … econte 側の iPad タスク（本 SPEC に吸収する）
