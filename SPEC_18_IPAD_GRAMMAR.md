# SPEC_18 — iPad 操作文法の統一（IPAD GRAMMAR）

**状態: P0 実装済み（2026-08-26〜27）／ P1 = `composer.html` 実装済み（2026-08-29〜31）。P2 以降は未着手。**
**タイムライン操作の最新の決着は §4-7（P1d・2026-08-31）→ §4-6（P1c）。横展開のときはまずそこを読む。**

SPEC_17（キーの押し方）の iPad 版。**指とペンで役割を分け、5本のツールで同じ手つきにする。**

> ⚠ **この SPEC の数値は推測ではなく実機で測ったもの。** 測定器は `ipad-probe.html`。
> 測定環境: iPadOS 18.7 / Safari 26.6 / 1032×1376 @2x / 最大同時タッチ点 5 / 2026-08-26〜27。
> **仕様を変えるときは probe で測り直してからにする。**

---

## 1. 第一原則 — 指とペンで役割を分ける

| | 役割 |
|---|---|
| **指** | ナビゲーション・UI操作・数値スクラブ・多指ショートカット |
| **Apple Pencil** | **描く面だけ。** ブラシ・投げ縄・コマ割り線・アンカー移動 |

**キャンバス上で指は描かない。UI 面でペンは「ただの指」として振る舞う**（掴まない・フォーカスを渡さない）。
Procreate / CSP / Nomad Sculpt / Fresco すべてこの分離を採っている。

この1行を立てると、以下の判断がほぼ自動的に決まる ——
「ペンが入力欄に触れたらどうする？」→ **フォーカスを渡さない（＝Scribble が起きない）**。
「指がキャンバスに触れたら？」→ **視点操作**。

---

## 2. 実測で決着したこと

### 2-1. 多指タップ（②タブ・2日分）

| 本数 | 実測 | 判定 |
|---|---|---|
| 1本 | 届く | UI操作に使う |
| 2本 | 届く | **UNDO**（業界標準どおり） |
| 3本 | **20回すべて届いた**（pd:ON / pd:OFF とも） | **REDO。使ってよい** |
| 4本 | 16回届き、**アプリは一度も切り替わらなかった** | **PREVIEW 再生／停止** |
| 5本 | 6回届いた。ただし**「上手くやらないと別アプリに変わるズレが起きる」** | **割り当てない** |

**結論の要点:**

- **3本指は使える。** 研究資料B（と OBAN の 2026-08 のコメント）が言う「OSが横取りするから不可能」は
  **この端末では否定された**。OBAN のコメントは、3本指をキャプチャ⇔REDOと付け替えていた最中の
  観測だったと考えられる。**`oban-builder.html` の該当コメントは訂正すること。**
- **`preventDefault` は多指タップの到達には要らない。** ON と OFF で届き方が変わらなかった
  （ログの `[pd:ON]` / `[pd:OFF]` 刻印で確認）。preventDefault が要るのは
  **ピンチ・スクロールを取られないため**であって、タップの到達のためではない。
- **OSの編集バーが出ても、やり直しは実行されていない。**
  「OS横取り検出」（画面外の contenteditable で `beforeinput(historyUndo/historyRedo)` を捕まえる罠）は
  **2日間で一度も発火しなかった**。＝二重実行ではなく、バーが表示されるだけ。
- **バーが出る条件は「入力欄にフォーカスがあるとき」** ——1日目は④タブで number 欄を触った後だったので出た。
  2日目は触らずに撃ったら**両モードとも出なかった**。
  つまり **§2-2 の「数値欄を div にする」を実施すれば、バーは自然に出なくなる**（要・追試）。

### 2-2. Scribble（④タブ）

| | Scribble | キーボード | スクラブ | Wタップで打ち換え |
|---|---|---|---|---|
| A 素の `input[type=number]` | **出る**（手書きオンリーになる） | 出る | ✕ | — |
| B `readonly` な input | **出ない**（一切無反応） | 出ない | ✕ | — |
| C `inputmode="none"` | **出る** | 出ない | ✕ | — |
| **D div スクラブ** | **出ない** | 出ない | **○** | **○** |

**D が正準。** `<input>` を DOM に置かず `<div>`（または `<span contenteditable="false"`）で値を表示し、
`pointerdown` で `preventDefault()` + `setPointerCapture()` → 横ドラッグで数値を変える。
**Wタップのときだけ `<input>` を動的生成**して focus / select、確定または Esc で破棄する。

> ⚠ **研究資料2本はどちらも「readonly では Scribble が出る」と書いているが、実機では出なかった。**
> ただし readonly はスクラブと両立しないので、正準にはしない。**資料より実機を採る。**

### 2-3. standalone（①⑤タブ）

| 項目 | 実測 |
|---|---|
| `navigator.standalone` | `true` |
| `display-mode: standalone` のメディアクエリ | **`browser` と出る＝当てにならない** |
| `env(safe-area-inset-top)` | **32px** |
| `env(safe-area-inset-bottom)` | **20px**（ホームバー） |
| Fullscreen API | **無い**（⛶ ボタンは消す） |
| `ctx.filter` | **プロパティ自体が無い**（econte の `CTX_FILTER_OK` 代役が正しく効いている） |

- **standalone の判定は `navigator.standalone` を見る。** メディアクエリは使わない。
- **`<link rel="manifest">` を張ってはいけない。** 張ると「ホーム画面に追加」が
  そのページではなく manifest の `start_url` をインストールする（2026-08-26 に実際に踏んだ）。
  standalone 化は **`apple-mobile-web-app-*` メタだけ**で足りる。
- **ホーム画面のアイコンは index の1枚で足りる。** 当初「5本それぞれ追加が要る」と見えたが、
  P0 のメタが全ページに乗った後は **ANIMATOR→COMPOSER の LIVE 遷移でも URL バーが出なくなった**。
- **⛶ 全画面ボタンは `canFS` 判定で消す**（standalone では API が無い＝すでに全画面）。

### 2-4. 筆圧（③タブ）

| 項目 | 実測 |
|---|---|
| 1発目の pressure | 0.0800 |
| 2〜7発目の中央値 | 0.0800 |
| **着地スパイク** | **無い**（1.00倍） |
| `getCoalescedEvents` | **3.98 点 / move**（＝取りこぼしを防ぐには必須） |
| pressure が **ちょうど 0** の点 | 1363点中 **37点** |

- **着地スパイクは存在しなかった。** 研究資料2本の「1発目は信用できない」は、この端末には当てはまらない。
- **代わりに、ストローク中に pressure が 0 へ落ちるサンプルが多数ある（37/1363）。**
  センサーのノイズが実在する＝**高い側へ跳ねるサンプルもあると考えるのが自然**。
- probe（平滑化なし・弧長テーパー切替あり）では**インク溜まりが再現しない**。

---

## 3. 統一ジェスチャ表（決定）

| 入力 | 操作 | 割り当て | 備考 |
|---|---|---|---|
| **Pencil** | down / move | **描画・直接編集のみ** | UI 面ではフォーカスを渡さない |
| 1本指 | tap | UI操作・要素選択（**画面の絵をタップ＝そのトラックを選ぶ**） | **キャンバス上の1本指では描かない** |
| 1本指 | drag | 視点パン／数値スクラブ | |
| 1本指 | hold 350ms | 対象の掴み（**タイムラインでは時間方向の横移動**）・スポイト | リストの並べ替えには使わない（§4-2） |
| **2本指** | **tap** | **UNDO** | 全ツール共通 |
| 2本指 | pinch | **対象の上＝その対象／余白＝視点** | manga / oban の型が正準。composer 実装済み |
| **3本指** | **tap** | **REDO** | 使ってよい（§2-1） |
| **3本指** | **左右 drag** | **タイムラインの伸縮** | Procreate Dreams の手つき。タップ(REDO)とは14pxで分ける |
| **4本指** | **tap** | **PREVIEW 再生／停止** | oban の実績。animator / composer / econte へ展開 |
| **5本指** | — | **割り当てない** | アプリ切替に化ける（§2-1） |

**「対象の上か」の判定は当たり判定を自前で書かない。**
判定用の 1×1 キャンバスに、押した点が (0,0) に来るようずらして**本番と同じ描画関数**を通し、α を読む
（composer の `trackAlphaAt` / `trackIdxAtPoint` が正準）。こうすれば親子チェーン・カメラ・Z透視・
トリム・SOLO・OP が全部そのまま効く。変換を二重実装すると必ずズレる。

**しきい値（全ツール共通）**

| 定数 | 値 | 根拠 |
|---|---|---|
| タップ成立 | `< 300ms` かつ移動 `< 14px` | 既存3本の実装値と同じ |
| Wタップ成立 | `< 320ms` | SPEC_17 と同じ |
| **「動かさずに離した」判定** | **`10px`** | **3px だとふつうのタップの手ブレで潰れる（実機で踏んだ）** |
| リストの掴み（ハンドル無しの場合） | `300ms` / キャンセル `10px` | 研究2本が一致 |
| 筆圧 EMA の α | `0.15〜0.30` | 研究2本が一致 |

---

## 4. 各ツールで直すこと

### 4-1. 数値欄を div スクラブへ（§2-2）

| ツール | 対象 |
|---|---|
| `composer.html` | `.kf-val` / `.fx-row input` の全プロパティ（`attachScrub` を div 版へ） |
| `animator.html` | `#fps-input` |
| 他 | 素の `input[type=number]` を順次 |

`composer.html` の現状は「入力欄はクリック=数値入力を許可するため preventDefault しない」という
コメント付きの実装になっている。**この方針を反転させるのが本 SPEC の要。**

**✅ composer 実装済み（2026-08-29）。** 16個（`kf-*` 8 / `fx-*` 8）を `<div>` 化した。要点：
- `makeNumField(el)` で div に `.value` を生やす互換シム。`updateKfUI` / `updateFxHud` /
  `bindPropInputs` / スクラブは **呼び出し側を1行も変えずに** 動く
- 打ち換え中（`_editing`）は外からの書き込みを無視する＝打っている最中に上書きされない
- `<div>` は `change` を飛ばさないので、確定は `beginEdit` 側が `commitProp` する
  （`bindPropInputs` の `change` 配線は削除。◆ドットの配線だけ残した）
- Wタップのしきい値は 300ms / 320ms、スクラブ開始は **指=10px・マウス=3px**。
  しきい値ぶん**だけ**吸収して残りはその move から効かせる（`startX=e.clientX` に引き直すと、
  move が粗いときに最初の移動量が丸ごと消える）
- **`blur` だけに頼らない。** `focus()` が通らない場面があるので「よそを触ったら確定して閉じる」
  document 監視を併設した（無いと入力欄が開きっぱなしになる）
- 指の当たりは `@media (pointer:coarse)` で数値欄28px・ステッパ18×28px・◆14px へ拡大
  （#inspector は幅216px固定。横あふれしないことを確認済み）

### 4-2. リストの並べ替えは ≡ ハンドルから

研究2本とも「専用ハンドルが iPadOS の標準作法／長押しはスクロールと誤干渉する」で一致。

| ツール | 現状 | やること |
|---|---|---|
| `econte.html` | 長押し350msで掴み＋**1本指を置いたまま他をタップで複数選択** | 掴みを ≡ ハンドルへ移す。**複数選択の型はそのまま残す**（これは良い） |
| `composer.html` | ⠿ハンドル＋ラベル縦ドラッグは実装済みだが **指では効かなかった**（`touch-action` 未指定・ハンドル12px） | ✅ 2026-08-29 修正 |
| `manga-plate.html` | ▲▼ボタンのみ | ハンドルドラッグを足す |

**掴みをハンドルに限ると、行本体の長押しと2本目タップが全部空く**＝取り合いが消える。

**✅ composer 実装済み（2026-08-29）。**
- `.tl-drag-handle{touch-action:none}`。**これが無いと指ではスクロールに取られて一切掴めない**
- 当たり判定だけ `::before` で左（行の padding-left 14px 分）とタテに広げた。
  **見た目の12pxは `LABEL_W=164` の計算に入っているので変えないこと**
- 並べ替えドラッグに **pointerId を持たせた**。window で pointermove を追う実装なので、
  これが無いと「行を押さえたまま**別の指**を動かした」だけで押さえている行が動き出す
- econte の型（1本を行に置いたまま別の行をタップ＝複数選択）を移植。
  押さえている行を基点にして `selectTrack(idx,'toggle')` を足していく
- **ストリップの長押し(350ms/10pxでキャンセル) = 時間方向の横移動**（PC の Alt+ドラッグの指版）。
  掴んだら `gStripGesture='offset'` を立てて並べ替え・マーキーに手を出させない。
  **解除は次のタスクまで遅らせること**（`setTimeout(...,0)`）。即 null にすると window 側の
  マーキー用 pointerup が「動かさず離した＝クリック」と誤解して、離すたびにインジケータが飛ぶ

### 4-3. 多指タップの統一

| ツール | 2本 | 3本 | 4本 | 5本 |
|---|---|---|---|---|
| animator | ✅ある | ✅ある | **足す** | — |
| composer | ✅ある | ✅ある | ✅ 2026-08-29 | — |
| econte | ✅ある | ✅ある | **足す** | — |
| manga-plate | ✅ある | ✅ある | （プレビュー無し＝不要） | — |
| oban | ✅ある | **REDOとして復活させる** | ✅ある | **CAPTURE を外す** |

**✅ composer 実装済み（2026-08-29）。** ビューポート限定だった判定を **画面全体（window の capture 段）** へ移し、
econte / oban と同じ `gTap` の型に揃えた。しきい値は §3（300ms / 14px）。
**ビューポート側のパン/ピンチは残し、タップ判定だけ剥がした**（二重発火を避けるため）。
多指タップが成立したら `cancelVpGesture()` で直前のパン/ピンチを打ち切る。

### 4-3b. Wタップ（指・ペン）で「送り」を出す — dblclick は当てにできない

**`pointerdown` で `preventDefault()` した要素には、ブラウザの `dblclick` が飛んでこない。**
composer のキー（`.tl-kf-d`）は「iPad用ダブルタップでも切替」というコメント付きで `dblclick` を
持っていたが、**指でもペンでも一度も発火していなかった**（2026-08-30 発注者報告で判明）。

- §4-1 の数値欄と同じく、**pointerup で自前にタップを2回数える**（320ms以内）
- 数値欄・キー・イーズ量の3か所で同じしきい値を使う（§3）
- ついでに「複数選択中のキーを1回触っただけでは選択を畳まない」ようにした。
  そうしないと1回目のタップで選択が1つに落ちて、**一括変更が成立しない**

### 4-3c. ez と influence の対応（表示の連動）

キーの送り `EASE最大` は、補間の中で **influence 33.3% として効いている**
（`getKfValue` が `ei/eo` 未設定のとき `ez*33.3` にフォールバックする）。
なので送りで切り替えたら **イーズ量スライダー／数値も 33 へ動かす**のが正しい。
手で入れた値と区別できるよう、ez由来の推定値は色を落として出す（HOLD は「—」）。

### 4-4. standalone まわり

- 正準は **`black-translucent` + `viewport-fit=cover` + `env(safe-area-inset-top)` の押し下げ**
  （OBAN が実装済み・実機で確認済み）。全画面を使えて、なお重ならない。
- 他4本は `default`（不透明バー）のまま。**急がない。** 揃えるときは OBAN の `#bar` を写す。
- **`<link rel="manifest">` は張らない。**

### 4-5. ANIMATOR の点在インク — **原因特定・実装済み（2026-08-27）**

**当初の「着地スパイク説」も「手ブレ補正説」も実機で否定された**
（補正 OFF でも出る／probe では着地スパイクが無い）。真因は別だった。

#### 真因: `pressure || 0.5`

[`animator.html`](animator.html) の `pressureRadius()` にあった
`Math.min(1, pressure || 0.5)` が犯人。この式は
**「筆圧が来ていない環境なら 0.5 とみなす」**つもりで書かれているが、
JS の `||` は 0 も falsy なので **筆圧ちょうど 0 のサンプルまで 0.5（中くらいの太さ）に化かす。**

probe の実測では **1ストローク1363点中37点が pressure ちょうど 0**（§2-4）。
その37点が周囲の約2倍の太さのドットとして線上に残っていた ——
「意図的には出せない」「偶発的」「筆圧ONのときだけ」というすべての症状と一致する。

**実測（penSize=20・LIN・pressure 0.30 の直線の途中に 0 を1点混ぜる）**

| | pressure 0 の地点の線の太さ | ストローク中の最太列 |
|---|---|---|
| RAW（従来） | **16px** | **その地点**（＝そこが一番太い） |
| EMA | 8px | 別の場所（＝埋もれた） |

#### 入れたもの — `SM` トグル（RAW → EMA → EMA+）

**既定は `RAW` ＝入れても何も変わらない。** 描き比べてから選べるようにしてある。
場所は左レールの `PRS`（筆圧カーブ）の下。保存は `localStorage['animator_pensmooth_v1']`。

| | 中身 |
|---|---|
| **RAW** | 従来どおり。`pressure \|\| 0.5` もそのまま＝**完全な互換** |
| **EMA** | 筆圧を EMA(α=0.25) で均す＋**「0」と「未提供」を区別**する。粒の対策はこれで足りる |
| **EMA+** | EMA ＋ 入りを弧長 12px で絞る＋半径の下限を 0.7→0.35 に下げて抜きが細くなれるようにする |

- 平滑化係数 `P_EMA_A = 0.25`（研究稿A・Bが一致した 0.15〜0.30 の中央）
- 入りのテーパーは**時間ではなく弧長**で見る＝フレーム落ちに影響されない
- **抜きのテーパーは入れていない。** 弧長で作るには「終端12px分を描かずに溜める」必要があり、
  描線が目に見えて遅れる。EMA+ では代わりに半径の下限を下げて、筆圧が落ちるぶん自然に細くする
- 未提供（`null`/`undefined`）のときは全モードで従来どおり 0.5 扱い＝**筆圧非対応環境の挙動は不変**

> ユーザーの見立ては「これはこれで本物のインクのようで味といえば許容」。
> **どれを使うかは好みで選べばよい。** 既定を RAW にしてあるのはそのため。

---

## 4-6. タイムラインの手つきの整理（P1c・2026-08-31）— **composer 実装済み**

実機で触って出た引っかかりの是正。**データも保存形式も触っていない。** VRT 0%・`node tools/check.js` PASS。

### 第一原則の各論 ——「どこを誰が担当するか」

| 面 | 指 | ペン | マウス |
|---|---|---|---|
| ルーラー（目盛りバー） | タップ=インジケータ移動／左右へこする=**タイムライン伸縮** | スクラブ | スクラブ |
| ストリップ上部 | 縦=並べ替え／長押し+横=時間移動／タップ=選択+シーク | **何もしない** | マーキー矩形／Alt+横=時間移動 |
| トリムバー（IN/OUT） | **反応しない**（下のストリップへ素通り） | **専任** | 従来どおり |
| キー（◆） | タップ=選択／Wタップ=イーズ送り／押しながら別キーをタップ=複数選択 | 同左（**Wクリック可**） | 従来どおり＋矩形 |
| ラベル帯 | 縦=並べ替え（⠿）／**左スワイプ=⧉複製 / ✕削除**／押しながら別行タップ=複数選択 | 何もしない | 従来どおり |

### §5-A タイムライン上端の掴みバーが指で掴めなかった真因

`#tl-resize` は `#timeline` の中にあり、**`#timeline{overflow:hidden}` が上へはみ出した掴み代を
まるごとクリップしていた。** `@media(pointer:coarse)` で `top:-20px;height:21px` に太らせていたのは
**逆効果で、実効の当たりは 1px しか残っていなかった**（PC でも 8px 中 5px）。

→ **`#app` 直下へ出し、`bottom:calc(var(--tl-h) + var(--tp-h) - 3px)` で置く。**
`--tp-h`（=`#transport` の 26px）を `:root` に切り出して `grid-template-rows` と共有する。
実測: PC 8px / 指 **30px** がすべて `elementFromPoint` で `#tl-resize` を返す。

### §5-B トリムとキーの住み分け（1コマ目が選べない問題）

`.tl-trim-handle` が `top:0;bottom:0` でキー帯の上に被さっていたので、**1コマ目では
「トラック端のトリムバー」と「F1のキー」が当たり判定で区別できなかった。**

→ **`.tl-trim-handle{bottom:23px}`（`KF_BAND_H` と同値）。上＝トリム／下の帯＝キー・マーカー。**
`::before` で当たりだけ左右 6px 広げる（見た目の 7px 幅は据え置き＝IN/OUT 位置がズレない）。
指の端末ではキーの当たりも `::after` で ±4px 広げる。

### §5-C ペンがインスペクタの文字を選択してしまう

`#inspector-body` は 2026-08-02 に「本文はコピーできる」で `user-select:text` にしてあった。
→ **`@media(pointer:coarse)` のときだけ `user-select:none`。** `<input>`/`<textarea>` は戻す。
PC の挙動は変えない。

### §5-D ラベル帯の左スワイプ ＝ ⧉複製 / ✕削除（Procreate のレイヤー左スワイプ）

行の `✕` が誤爆しやすいという報告への答え。**指の端末では行の `✕` を隠して**ここへ寄せた。

- 引き出しはラベル帯（`LABEL_W=164`）に**重ねる**だけ＝行のジオメトリは動かない
- 判定は「横 34px 以上 かつ 横 > 縦」。`.tl-track-label{touch-action:pan-y}` で
  **縦はリストのスクロールに渡す**（`none` にすると指で上下送りができなくなる）
- 同時に開くのは1つ。よそを触ったら閉じる（`document` の capture 段）
- ラベル帯の並べ替えは、指のときだけ「はっきり縦」（`dy>9 && dy>dx*2`）を要求してスワイプに譲る

### §5-E 複数選択まわり

- **「押しながらタップ」をストリップ面でも効かせる**（従来はラベル帯だけ＝名前の近くでしか
  できず誤爆しがちだった）
- **押しただけでは選択を畳まない。** 従来は行の `pointerdown` が無条件に `selectTrack(idx)` を
  呼んでいたので、**複数選択 → ⠿ を掴む の時点で単体選択に戻り、まとめて動かせなかった**。
  畳むのは「動かさずに離したとき」＝ `pointerup`（ストリップ側はマーキーの `pointerup` が担当）
- **`doTrackReorder` が複数選択をひとかたまりで運ぶ。** 落とし先は添字の算術ではなく
  **落ちた位置にある行（アンカー）の実体**で決める——運ぶ側が複数あると
  「残り配列の何番目」が算術では合わない。`measureDropGeo` に `order`（UI順の state 添字）を追加
- ★**`gTrackMultiSel` / `gKfMultiSel` は次のタスクまで残す。** 即 `false` にすると、
  後から登録された window の `pointerup`（マーキー）が「動かさず離した＝クリック」と解釈して
  選択を1本に畳んでしまう（`gStripGesture` とまったく同じ理由）
- 「押しながらタップ」が成立したら `killTapGesture()` で多指タップ（2本=UNDO）を打ち切る

### §5-F 指とペンの排他

- **トリムは `pointerType==='touch'` を弾く**＝指はストリップへ素通り。
  合わせて `stripSkipSel(e)` を用意し、**指のときだけ「トリムに譲る」相手から外す**
  （ストリップ側・マーキー側の3か所で同じ関数を使う。式を分けると必ずズレる）
- **並べ替えは `pointerType==='pen'` を弾く**（`attachTrackDrag`）。
  バーが太いのでペンが当たって勝手にトラックが動いていた

### §5-G キーの複数選択と、ペンのWクリック

- **矩形（マーキー）はマウス専用。** 指・ペンでは枠が引っ付いてくるだけで用を成さない。
  代わりに **「1つ押さえたまま別のキーをタップ」**（トラックと同じ型）。
  押さえる側と足す側で**ポインタ種別が違ってよい**＝指で押さえてペンでタップ、が実用上いちばん通る
- **ドラッグ判定を マウス3px / 指・ペン10px（`NUMSCRUB_TAP_PX`）に分けた。**
  3px のままだとペンの手ブレで「動かした」ことになり、
  **§4-3b で入れたはずの自前Wタップが一度も成立していなかった**（キーだけ・数値欄は無事）

### §5-H タイムライン伸縮を「ルーラーを指1本でこする」へ

**3本指の左右ドラッグは廃止。** 画面のどこでも効くため他のジェスチャと当たり、
「いつのまにか効かなくなった」ように見えていた（発注者報告）。3本指**タップ**=REDO は残す。

→ **ルーラー（目盛りバー）を指1本で左右へこする＝伸縮。基点は指を置いたコマ**なので、
触っているところが動かないまま前後が伸び縮みする。**14px 動くまでは何も起こさず、
そのまま離せばタップ＝インジケータがそこへ飛ぶ。** ペン・マウスは従来どおりスクラブ
（ANIMATOR はペンで伸縮だが、COMPOSER はあえて指で使用感を見る＝発注者判断）。

---

## 4-7. 手つきの再配分（P1d・2026-08-31 その2）— **composer 実装済み**

§4-6 を実機で触った結果の是正。**データも保存形式も触っていない。** VRT 0%・`node tools/check.js` PASS。

### §5-K 役割の組み替え —「横移動を2本指へ」が起点

| | 旧（§4-6） | 新（§4-7） |
|---|---|---|
| トラックの時間方向の横移動 | 指1本の**長押し350ms**→左右 | **指2本**をストリップに置いて左右 |
| IN/OUT トリム | **ペン専任**（指は素通り） | **指でもペンでも掴める** |

長押しをやめたことで**指1本が空き**、トリムを指に戻せた。
「トリムを掴もうとすると長押しで移動になる」も「トラック端のバーが指で選べない」も、
これ1つで両方消える。`stripSkipSel()` は種別で分けるのをやめ、常に個別要素へ譲る。

- 2本指は `offPts`（Map）で数え、**同じストリップに2本目が乗った瞬間**に掴む
- 掴んだら window で pointermove を追う（片方の指が行の外へ出ても切れない）。capture は取らない
- **行の外で指を離すと pointerup を取り逃がして Map に残る**＝次の1本目で誤発火するので、
  4秒以上前のものは掃除する（`gTap` と同じ型・§6-9）

### §5-L ペンで「動かない・選べない」の真因 — `!e.buttons`

**`pointermove` の `if(!e.buttons) return;` が、キー／トリム／マーカー／WAハンドルの4か所に残っていた。**
§6-14 に「ペンはホバーの move（`buttons=0`）も飛ばすので `buttons` だけで打ち切らない」と
書いてあるのに、数値欄にしか適用していなかった。

- 症状: ペンでキーを左右に動かせない／トリムのIN・OUTが動かない／
  **キーをタップしても選択されないことがある**（＝ドラッグ扱いで抜けていた）
- 直し: **追う相手を `pointerId` で固定し、`buttons` の保険は `pointerType==='mouse'` のときだけ**

### §5-M キーの複数選択 —「指でトラックを押さえて、ペンでキーを次々タップ」

§5-G の「キーを1つ押さえたまま別のキーをタップ」は残すが、
**細かいキーを指で押さえると指自身に隠れる**（発注者報告）。そこで押さえる側を広げた：

> **指でトラックの行（ラベルでもストリップでも）を1本押さえている間、ペンでキーをタップすると追加選択。**

`gHeldTrack` が立っている間、キーの `pointerdown` は `kfSelToggle` に化ける。
★ **`gHeldTrack` には時刻を持たせ、15秒以上前のものは捨てる。**
pointerup を取り逃がして残ると、以後ペンのタップが全部「追加選択」に化けて
**キーが1つも選べなくなる**（実装中に実際に踏んだ）。

### §5-N Wタップで画面全体が上下する / タイムラインで UI が揺れる

`<meta viewport … user-scalable=no>` は **iOS 10 以降 Safari に無視される**。

- **Wタップ拡大**が生きていて、キーをWタップするたびに画面が拡大＝「上にヒュッと上がる」、
  もう一度で元に戻る（キーボードが出たように見えていた正体）→ **`touch-action:manipulation`**
- タイムラインの中身が収まっているとき、上下ドラッグがページへ伝播して**ゴムバウンド**
  → **`overscroll-behavior:none`（html,body）＋ `contain`（#tl-tracks）**

`manipulation` はピンチ拡大を許すので、ビューポートの操作感は変わらない。

### §5-J ワークエリアの IN/OUT を指のサイズへ

ルーラーの「指1本＝伸縮」と取り合いになるので、**端のバーが優先**（`pointerdown` で `stopPropagation`）。
見た目の3px線はそのままに、`::before` で当たりを広げる（PC 24px / 指 **36px**）。`z-index` は 4→6。

### §5-A' 上端の掴みバーをもうひと回り

左手の指で掴むので 30px → **38px**（タイムライン側への食い込みも 4→6px）。

### §5-M' ADD KF / RESET KF を INSPECTOR の最上段へ

独立した `.insp-section`（`#insp-kfbtns`）にしたので、**下端のリサイズがその真下でスナップする**
（`attachPanelResize` はセクションの下端＋10pxが吸着点）。
本文54pxまで畳めて、右上に2つのボタンだけが残る＝指で押しやすい。

---

## 5. 進め方

- [x] **P0** メタ／safe-area／⛶ボタン／probe（2026-08-26〜27）
- [x] **P1** `composer.html` をパイロットで通す（2026-08-29・§4-1 数値欄・§4-2 トラック並べ替え・§4-3 4本指）
- [x] **P1b** 実機フィードバック反映（2026-08-30）。§4-3b Wタップ／§4-3c ez連動／ピンチ＝素材スケール／
      長押し＝横移動／3本指＝タイムライン伸縮／画面タップ＝トラック選択／下端リサイズ枠の拡大
- [x] **P1d** 手つきの再配分（2026-08-31・**§4-7**）。横移動を2本指へ→トリムを指に開放／
      ペンの `!e.buttons` 撲滅（キー・トリム・マーカー・WA）／指でトラックを押さえてペンでキーを追加選択／
      Wタップ拡大とゴムバウンドの抑止／WA端を指サイズへ／ADD KF・RESET KF を INSPECTOR 最上段へ
- [x] **P1c** タイムラインの手つきの整理（2026-08-31・**§4-6**）。掴みバーのクリップ解消／
      トリムとキーの住み分け／ペンの文字選択／左スワイプで複製・削除／複数選択のまま並べ替え／
      指とペンの排他／矩形廃止→押しながらタップ／伸縮をルーラー指1本へ（3本指は廃止）
- [ ] **P2** スキル化（`ipad-input-kit`）。**P1 を通してから**——いま作ると誤った正準を固める
- [ ] **P3** 横展開 econte → manga → oban → animator
- [x] **P4** ANIMATOR の筆圧 EMA ＋ 入りの弧長テーパー（2026-08-27・`SM` トグル。既定 RAW ＝互換）

**パイロットが composer な理由**: 不満が集中していて、iPad 対応が一番薄く、かつ「描かない側」なので
**UI 面の文法だけを純粋に固められる**。描く面は animator が正準なので混ぜない。

---

## 6. 実装上の落とし穴（probe を作って実際に踏んだもの）

**すべて本体側にも同型がありうる。P1 で移植するとき必ず見ること。**

1. **`getCoalescedEvents()` は空配列を返すことがある。** そのまま回すと move の点を全部落とす
   → `if(!list || !list.length) list = [e];` を必ず入れる
2. **`setPointerCapture` は普通に例外を投げる**（`NotFoundError`）→ try/catch しないとストロークが丸ごと消える
3. **`display:none` の中で canvas を初期化すると 0×0 のまま。** タブを開いた時点で採寸し直す
4. **`lastTap = 0` の初期値は罠。** `performance.now()` がまだ小さい起動直後300msだけ、
   一発目が2連打と誤判定される → 十分過去（`-1e9`）を入れる
5. **`input.remove()` がその場で `blur` を誘発して確定処理が二重に走る** → 参照を先に null にしてから消す
6. **`'filter' in ctx` は当てにならない。** 実際にぼかして確かめる
   （`oban-builder.html` の `probeCanvasFilter` が正準）
7. **`getCoalescedEvents()` の返すオブジェクトはプールされている**（資料B）。
   配列に持つなら `{x, y, pressure}` に値コピーする

**P1（composer）で新たに踏んだもの — 横展開のとき必ず見ること**

8. **`setPointerCapture` の裸呼びが本体にも15箇所あった。** 例外を投げるとハンドラが
   そこで止まるので、**パンもピンチも並べ替えも丸ごと死ぬ**。全部 try/catch で包んだ
9. **多指タップの `pts` は pointerup を1回取り逃がすと二度と空にならない＝ジェスチャが永久に死ぬ。**
   タップは300ms以内にしか成立しないので、`tapDown` で「2秒以上前のものは掃除」を入れた
   （econte / oban も同型なので、横展開のとき一緒に入れる）
10. **window で pointermove を追うドラッグは pointerId で絞る。** 絞らないと
    「押さえている指」と「動かしている別の指」が混線する。指が1本しかないPCでは絶対に出ない
11. **`input.focus()` が通らないことがある**（activeElement が body のまま）。
    `blur` だけを確定の入口にすると閉じられなくなるので、外側 pointerdown での確定も併設する
12. **スクラブのしきい値は「引き直す」のではなく「差し引く」。** `startX=e.clientX` にすると
    move が粗い環境で最初の移動量が丸ごと消える

**P1b で踏んだもの**

13. **`preventDefault()` した pointerdown の要素には `dblclick` が来ない。**
    「iPad用にダブルタップ対応済み」と書いてあっても、実際には一度も発火していないことがある（§4-3b）
14. **`window` で pointermove を追うスクラブは `e.buttons` だけで打ち切らない。**
    ペンはホバーの move も飛ばすので途中で抜ける＝「タップで飛ぶだけ」に見える。
    pointerId で追う相手を固定し、`buttons` 判定はマウスだけの保険にする
15. **指は同時に着かない。** 3本指ジェスチャは、2本目まででピンチが動いてしまう。
    3本目が乗った時点で**1本目を置いた時の状態へ巻き戻す**（`viewAtDown`）
16. **2つのジェスチャが「相手に譲る」条件は必ず同じ式にする。**
    並べ替えが `dy>dx*2` で始まるのにマーキーが `h>w` で譲っていたため、斜めに入ると
    **どちらも始まらない死角**ができていた

---

## 7. まだ決めていないこと

- **OSの編集バーの発生条件**——「入力欄にフォーカスがあるとき」という推定の追試。
  §4-1 は composer で入った。**実機で 3本指タップしてバーが出ないことの確認が残っている**
  （PCでは Scribble も編集バーも再現できないため、ここだけは実機待ち）
- 他4本を `black-translucent` へ寄せるか（§4-4）
- `SM` の既定を EMA 側へ寄せるか（いまは RAW ＝従来どおり。しばらく描き比べてから決める）

---

## 8. 関連

- `ipad-probe.html` / `ipad-probe-nav.html` … **測定器。仕様を変えるときはここで測り直す**
- `SPEC_17_INPUT_GRAMMAR.md` … キーの押し方（本 SPEC の姉妹編）。**しきい値はあちらと揃える**
- `_Research/iPadOS Webアプリジェスチャ設計調査.md` … 研究稿A
- `_Research/NotebookLM_iPadOS 26におけるPWA型プロ向け制作ツールのタッチ.md` … 研究稿B
  （**AとBは3本指の可否で対立している。実機はAを支持した**）
- `SPEC_16_ECONTE_V4.md` §5-D … econte 側の iPad タスク（本 SPEC に吸収する）
