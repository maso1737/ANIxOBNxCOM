# DEPTH_PLATE_HANDOVER — 見えがかり特化 2.5D ツール（構想〜ラボ着手）

モーションコミックで「一瞬だけ画面が立体化して迫る」を作るための4本目の道具。
**2026-09-04 時点で概念設計は固まり、実装はこれから。** 新チャットはこの1枚から始めてよい。

- 概念設計の全文（図つき） → https://claude.ai/code/artifact/d495ff09-f043-480d-835e-f13e0fd80320
  **更新するときは Artifact ツールの `url` にこのURLを渡すこと**（渡さないと別URLが発行される）
- 調査の原文9本 → `_Research/DEPTH_PLATE/`（**下の §3 に要点を写してあるので、通常は読まなくてよい**）

---

## 0. 3行で

1. **作るのは3Dエディタではなく「板に厚みを与える装置」。** 既存3本に足りないのは3Dではなく、
   カメラが少し振れた瞬間に「全部ペラい板だ」とバレること、その1点だけ。
2. **決定6件のうち4件が決着済み**（§2）。残る2件（振れ幅・深度で彫るか）は
   **読んで決めるものではなく、ラボを触って決めるもの**になった。
3. **次にやること＝ LAB A か LAB D を1本作る**（§4）。SPEC 化はそのあと。

---

## 1. 動かない設計判断（ここを変えると全部組み直し）

| | 決めたこと | 理由 |
|---|---|---|
| **表現の型** | 前面のみの**レリーフ（深度マップ変位）**。裏面は生成しない | カメラ1方向特化。裏面カリングという「工程」自体が要らなくなる |
| **UV** | **前面投影UV ＝ 画面そのもの**。UV展開はしない | 避けるのではなく構造的に不要。ANIMATOR の絵を無変換で貼れる |
| **スカルプト** | メッシュを彫るのではなく**深度マップをブラシで描く** | 既存のブラシ資産・矩形タイルUndoがそのまま効く（※決定2＝要検証） |
| **格子は1枚** | 同じ格子に **Z変位（深度）＋ XY変位（嘘パース）＋ 線幅マスク**を載せる | Live2D の最頻用変形＝ワープデフォーマは、深度メッシュの格子と同じもの |
| **輪郭線** | **背面法は不採用**。深度＋法線のエッジ検出（ポストプロセス）<br>**主線は手描き・生成線は補助** | 裏面が無いので背面法は原理的に不可。§3-② も読むこと |
| **出口** | **連番PNG**（＋ `*.depth.png` 同時吐き） | COMPOSER / OBAN が「連番4枚以上は1トラックに自動集約」する＝**下流の改修ゼロ** |
| **対象環境** | **Chrome / PC のみ。iPad は対象外** | 2026-09-04 のユーザー判断。SPEC_18 の適用は考えなくてよい |
| **深度の出どころ** | ①手で描く（本命）②Blender の Zパス（MCP 接続済み）③単眼深度推定（要るとき）<br>**AI 3D生成は見送り** | コストが出るうえ自作できる、という判断（2026-09-04） |

### やらないと決めたもの（蒸し返さない）

- **背面法（インバーテッドハル）** … 裏面が無い
- **サジェスティブ・コンター** … 業界で完全に棄却。明滅する・高密度メッシュが要る・
  **数学的に一意なのでアニメ的な嘘を差し込む余地が無い**（3つ目が決定的）
- **法線マップでの微細凹凸** … 寄るとジャギー。ここでは「深度マップに描く」が代わり
- **AI 3D生成サービス** … 上記
- **iPad 対応** … 上記
- **インテリアマッピングを DEPTH PLATE に取り込むこと** … 共有する部品が1つも無い。別ラボ（§4 LAB D）

---

## 2. 決定表（残り2件）

| # | 決めること | 状態 |
|---|---|---|
| 1 | 振れ幅の上限 | **未** → **LAB B で実測して決める**（数字で決めない。§3-① 参照） |
| 2 | スカルプトは「深度を描く」でいいか | **未** → **LAB A を触れば体で分かる**。推奨は「深度で行く」 |
| 3 | 深度の精度 | ✅ RGB 3ch パッキング（実効24bit）／NEAREST 固定／可逆保存のみ／編集中は float テクスチャ |
| 4 | 輪郭線 | ✅ 描き線が主・生成線は補助。**＋ 線幅マスクを描ける機構を必ず持つ** |
| 5 | iPad | ✅ 対象外 |
| 6 | 名前 | 仮 **DEPTH PLATE**（MANGA PLATE の兄弟）。ラボ中は仮名でよい |

---

## 3. 調査の要点（`_Research/DEPTH_PLATE/` の9本から。ここだけ読めば足りる）

### ① 振れ幅 — **当初の見立てが4倍甘かった**

単位は「度」ではなく**画面上の視差ピクセル数**。以下フルHD基準。

| 視差px | 度（換算） | 成立する構造 |
|---|---|---|
| 0〜10 | ±0.5〜1.5° | 単層＋スカートで**破綻ゼロ** |
| 10〜25 | ±2〜3.5° | **単層の限界**。0.5〜0.8秒で抜ければ通る |
| 25〜50 | ±4〜8° | **層分割が必須**。Meta 3D Photos の実効域（減衰クランプ付き） |
| 50〜 | ±10°〜 | 破綻域。対象外 |

**それでも被害が小さい理由：**
- **寄り（ドリーイン）はこの制限を受けない。** 前進運動は視差が放射状の拡大になり、
  遮蔽解除の穴がほとんど出ない。**「迫ってくる」の主成分はこれ**なので、振りは味付けの ±3° で足りる
- **穴埋めがこちらでは安い。** 文献は写真前提（隠れた部分は機械学習で幻覚生成するしかない）。
  こちらは**描けばいい**うえ、MANGA PLATE の `▶▶ OBAN レイヤー` が既に層を割っている

**逆に漫画が不利な点：** 文献が名指しする最悪の構図が漫画の標準構図だった——
**くっきりした線画＋後ろのスクリーントーン**。トーンは規則的な網目なので引き伸ばしが一発で見える。
だから決定1は LAB B で実測する。
**吹き出し・描き文字は必ず独立レイヤー**（単層深度に混ぜると文字の輪郭と空間が繋がって歪む）。

### ② 輪郭線 — 背面法が業界標準な理由は「裏面」ではなく「手で塗れること」

GUILTY GEAR Xrd 等は**頂点カラーの Alpha に押し出し量を手でペイント**している
（0.0＝線を消す／0.5＝標準／1.0＝最大。緑＝距離補正、青＝Zオフセット）。
全自動のスクリーン空間輪郭線は主役キャラでは**制御性の無さゆえに棄却**されている。

> **→ こちらは前面投影UV＝画面なので、頂点カラーの代わりに「線幅マスク」を
> 絵と同じキャンバスに描けばいい。** 同じブラシ・同じ座標系・同じ Undo。
> 棄却理由のうち「塗り分け不可」はこの構造では成立しない。
> もう1つの棄却理由「浅い角度で線幅が破綻」も、振れ幅が ±3° 前後なら実質消える。

入り抜きは **JFA（Jump Flood Algorithm）でスクリーン空間の距離場を作り、
端点と曲率でしきい値を連続的に落とす**。かすれは距離場境界へのノイズ変調。
**ノイズはスクリーン座標で乗せると画面に貼りついて滑る**ので、板のUVで乗せる。

### ③ 深度の精度 — 16bit PNG は原理的に使えない

Canvas 2D / createImageBitmap / WebCodecs の**どの経路でも仕様として8bitに丸められる**。
→ RGB 3ch パッキング一択（実効24bit）。**§5 の必須ルールに落としてある。**

### ④ パペット — 持つべき変形は4種、うち必須は2種

| 持つもの | 位置づけ | 優先 |
|---|---|---|
| **格子ワープ** | Live2D のワープデフォーマ。**実務で最頻用**。顔の向き・揺れ・全身の可塑変形をこれ1つで | 必須 |
| **回転ピボット** | 回転デフォーマ。首・肘・肩 | 必須 |
| 頂点直接編集 | 目・口の開閉など**局所だけ**。広範囲に使うと管理が破綻する | 後で |
| パス変形 | Inochi2D の PathDeform。リボン・長い髪 | 要らなければ捨てる |

**嘘パースの作り方**（Live2D 実務）＝ 格子の分割を細かくし、手前側の頂点間隔を広げ、
先端を球状に膨らませる（Spherize）。**XY と Z を同時に効かせたほうが片方より確実に効く。**

**重ね順**は3エンジンとも「描画順そのものをアニメーションさせて」解いている
（Live2D＝0〜1000の整数、Inochi2D＝float の `zsort`）。
→ こちらは **depth 値にキーを打てるようにしておけば足りる**（OBAN の `zpar`/DEPTH と同じ数値）。
ちらつき対策は上腕・前腕・手首に細分化して切り替えをずらす。

### ⑤ インテリアマッピング — 次に効く3つ

- **接空間 IM**（TBN で視線を接空間へ）… 斜め壁・曲面ビルが**同じシェーダで通る**
- **奥壁判定を外して無限深度＋フォグ** … 白黒ならトーン濃度でそのまま出せる
- **建築物以外**：アニメ調3Dで**瞳の虹彩**に視差IMを当てる例が増えている。
  平坦なポリゴンのまま「奥行きのある瞳」。口内も同様。**漫画のキメ顔に効く可能性が高い**
- （L字など不定形の部屋が要るなら Grid Interior Mapping ＝ 視線方向ごとのUVを事前ベイク。
  Houdini 前提なので、そこまで要るかは要判断）

---

## 4. ラボ5本（＝次にやること）

**1ラボ＝単一HTML＝1つの問い。順番は決めない。** どれかで「これだ」が出たら SPEC に昇格。
直列フェーズ計画にはしない（ユーザーの進め方の好み）。

| | 名前 | 問い | 置き場所（提案） | 依存 |
|---|---|---|---|---|
| **A** | DEPTH BRUSH | **深度を「描く」のは気持ちいいか？**（決定2の答え）<br>画1枚に深度をブラシで描いて、寄り＋±3°で振る | `ANIxOBNxCOM/depth-brush-lab.html` | **なし。今すぐ** |
| **B** | SKIRT vs LAYER | **漫画のトーンは何度で崩れるか？**（決定1の実測）<br>同じ絵を「単層＋スカート」と「2層」で並べて振り比べる | `ANIxOBNxCOM/depth-skirt-lab.html` | A |
| **C** | LINE | **生成線は手描き線と混ぜて成立するか？**<br>深度エッジ＋法線エッジ＋線幅マスク＋JFAの入り抜き | `ANIxOBNxCOM/depth-line-lab.html` | A |
| **D** | GURURI ROOM | **白黒の部屋は奥行きが読めるか？**<br>部屋の中から見回して描く。接空間対応・無限深度フォグ | `Gen-EX…/Interior Mapping/`（既存2本の続き） | **なし。今すぐ** |
| **E** | WARP LATTICE | **拳が迫るのは XY と Z のどっちが効くか？**<br>1枚の格子に嘘パースと深度を同時に載せる | `ANIxOBNxCOM/depth-warp-lab.html` | A |

**A と D は依存が無く、どちらからでも始められる。** A はゼロから1本目、D は既存の続きで手応えが早い。

### LAB A の完了条件（迷ったらこれ）

1. 画像を D&D → 前面投影の格子メッシュに貼る
2. ブラシで深度を描く（盛る／彫る／ならす／平らに戻す）＋ **視差クランプのスライダー**
3. 寄り（ドリーイン）＋ ±3° の振りでプレビュー
4. 連番PNG書き出し（＋ `*.depth.png`）
5. **「気持ちいいか」を判断できる状態**になっていること ← これが本体

---

## 5. 実装時の必須ルール（踏むと静かに壊れる）

| | ルール |
|---|---|
| **NEAREST** | パッキングした深度テクスチャに **LINEAR 補間を掛けると壊れる**（桁の違うチャンネルが混ざって深度が飛ぶ）。テクスチャ生成時に必ず `NEAREST` を指定する。**既定は LINEAR なので書き忘れる** |
| **可逆のみ** | 深度PNGを JPEG／非可逆WebP で保存してはいけない（DCTが桁の跳躍を潰す）。WebP は品質1.0で可逆 |
| **視差クランプ** | 拳が手前・背景が遠い大パース（`Zbg/Zfg > 5`）では**1°振るだけで巨大な視差が出る**。深度レンジの非線形圧縮（対数／シグモイド）は後付け機能ではなく**最初から要る** |
| **のりしろ** | 振ると画面端に未描画が出る。**メッシュを3〜5%拡大**（Meta は入力を5〜10%クロップ）。COMPOSER の `bleed ×1.2` と同じ発想 |
| **file://** | `fetch` でローカルの GLB/PNG は読めない。読み込みは **D&D / file input（Blob URL）だけ** |
| **module** | Three.js は importmap ＋ `type="module"`。**既存の非module な単一HTML群とスコープが混ざらない**。ラボ群だけ module でいく割り切り |
| **check.js** | `tools/check.js` の「未参照関数」検査は module スコープで誤検出しうる。**`FILES` に足す前に一度確かめる** |
| **VRT** | econte / manga-plate は後から網を張れずにいる（ROADMAP §0-3）。**ラボが本番に昇格した時点で `window.__HARNESS__` を入れる**。SPEC_08 に Three.js テンプレあり（`ready()` が GLTF・`compileAsync` を待つ） |
| **CRLF** | 既存の単一HTMLを一括置換で書き換えるときは改行コードに注意（メモリ `crlf-single-html-edits`） |

---

## 6. 既存資産の場所

**参照実装（Three.js の書き方はここを見る。新規に調べない）**
- `Gen-EX…/Interior Mapping/interior_mapping_proto01.html` — **解析的レイボックス交差**の
  インテリアマッピング。15部屋メガアトラス（5×3ブロック／1部屋＝3×2の6面）、窓ごとに `hash` で
  部屋を抽選（面ごとに別シード）、`side:FrontSide`、COPY/PASTE PARAMS。**LAB D の土台**
- `Gen-EX…/Interior Mapping/room_atlas_editor.html` — 6面アトラスの作画＋PNG書き出し
- `_Claude/SKILL/OBJECT_RIG_SKILL/OBJECT_RIG_v1.html` — TransformControls＋KFキャプチャ
- `Gen-EX…/Camera Map Fx/CAMERA_MAP_FX_v5.html` — OrbitControls＋カメラKF

**関連スキル**（`.claude/skills/` — このフォルダ配下でのみ有効）
`composer-timeline-kit`（KF・ショートカット・WA）／`floating-panel-kit`／`animator-brush-ops`。
グローバル側に `camera-rig-orbit-capture` / `object-rig-gizmo-capture` / `kinetic-stage-design-system`。

**別プロジェクトだが関係するもの**
- `Gen-EX…/Model Maker Z/` — SFメカのプロシージャル生成（UIモックのみ・Three.js未実装）。
  **DEPTH PLATE とは問題が正反対なので混ぜない**（裏面を持つ／自由に回す／ハードサーフェス）。
  ただし**同じカットに同居する継ぎ目**はある：レリーフの板が舞台、メカのプロップだけが本物の3D。
  法線エッジ検出はハードサーフェスに完璧に効く。**いま再開する話ではない**
- ぐるりペイント https://tools.neco-sara.com/tools/gururi-paint/ — 360°に直接描くツール。
  **LAB D の UX の下敷き**（6面を1枚ずつ描くのではなく、中に立って見回しながら描く）

---

## 7. 既存パイプラインとの接続（実装は後。原則だけ）

```
MANGA PLATE ──→ DEPTH PLATE ──→ OBAN（連番として置く）
（コマを割る／     （層に厚みを      ／ COMPOSER（カット全体に詰める）
  層に分ける）      与える）
```

- 入口として最も自然なのは MANGA PLATE の **`▶▶ OBAN レイヤー`** の分割結果
- **ANIMATOR ⇄ DEPTH PLATE** は既存の `tdr_exchange` / `tdr_live` にそのまま相乗りできる
  （SPEC_09 v2 のルート10 と同じやり方＝**既存3本の改修ゼロ**）
- カメラは **TAKE `{kf:[{x,y,z,dwell,ease}]}`**（SPEC_01）。振りは任意フィールド `rx`/`ry` を追加
  ＝旧リーダーは無視するだけ。**3つ目のキーフレーム形式を発明しない**
- 階層は PROJECT_v2 の `parent`(tid)、3コマ切替は `hold:true` / `duration=step`
- **入出力を変えたら `PIPELINE.md` を先に更新する**（各 CLAUDE.md より先）

---

## 8. 新チャットの入り方

1. この1枚を読む（`_Research/DEPTH_PLATE/` の原文は §3 で足りるので開かない）
2. **LAB A か LAB D のどちらをやるかユーザーに聞く**
3. 作る。`node tools/check.js` は**ラボを `FILES` に足す前に**素の状態で1回通しておく
4. できたら §4 の「問い」に答えられたかを一緒に確認する
   （パラメータを足したら**動作チェック表**を付ける＝ユーザー共通ルール／`param-check` スキル）

**SPEC_19 を書くのは「これだ」が出てから。** いま書くと確実に外す。
