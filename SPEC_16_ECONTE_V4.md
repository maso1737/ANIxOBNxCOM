# SPEC_16 — ECONTE V4「枠ごとの画」

> **状態: §1〜§3（V4コア）／§5-B（B1〜B6）／E1（Undo）／§5-C（C1〜C10）すべて実装済み（2026-08-18〜19）。**
> 実装メモは `ECONTE_HANDOVER.md` の「V4「枠ごとの画」」節。
> **残り: §5-D（iPad）だけ。**
> C5 / C6 は 2026-08-19 のユーザー追記で**方針が確定してから**実装した（下の §5-C5 / §5-C6 を見ること）。
> §2a は **推しの (b)＋入れ子のときだけ隠す** を採用した。ただし §3-3（GRIDのセルは自分の枠だけ）と
> 併せると「A のセルに描けるが TIMELINE では B が総取りして出ない」領域ができる。
> §3-3 を優先し、その領域を **A のセルに破線＋枠名で出す**（`drawTakeoverGuide`）ことで食い違いを可視化した。
> ここは実際に使ってみて決め直せる（合成側を GRID に合わせる切り替えは1か所）。

`econte.html` の**ペイントの持ち方をもう一段変える**仕様。
**着手前に SPEC_15（V3）と ECONTE_HANDOVER.md の V3-P1 / V3-A を読むこと。**

起点は 2026-08-18 のユーザー観察（GRID で T.U./T.B. を試した結果）:

> GRIDでT.U. T.B.テストしてみたところ、同時にまたがって大小ペイントが入る感じです。
> それぞれ大きいとき小さいときは別の画扱いで、かつGRIDではまたがってペイントした場合
> 同じブラシサイズで一気に作業できればというイメージでした。
> （つまり元は同じCUTだけど複製されて配置されているかんじで、REF扱いの直接ペイントはしないので
> 表示としてだけ。シートはそのまま各サムネになり、タイムラインのときはアップのほうの解像度が
> 上に重なって乗っている感じだと実際のT.U.対応でもあるかんじです。ペイントはそれぞれの要素として可能で。）

**戻り先タグ: `econte-v2-g3`（`32f5862`）。** V3-P1 の実装は master 上にある。

---

## 0. いま何が起きているか（V3-P1 の結果）

V3-P1 で色を「カット1枚・ベイク空間」にした。**紙の上に1枚**なので:

- ✅ T.U./PAN すると色も一緒に寄る（当初の要望どおり。フェードは消えた）
- ❌ **A枠に描いた線が B枠のセルにも出る**（同じ紙なので当然）。
  B のセルには「A で引いた太い線」＋「B で引いた正しい太さの線」が同時に見える
  ＝ ユーザーの言う「**大小ペイントが同時に入る**」

`SPEC_15 P1-7` で「失うもの」として合意済みの項目だが、**実際に使うと成立しない**ことが分かった。
原因は合意の取り方ではなく、**「A と B は同じ画か、別の画か」を詰めていなかった**こと。

---

## 1. ユーザーの求めている仕組み

**枠（`cam[k]`）ごとに1つの「画」がある。** カットが複製されて並んでいるイメージ。

| | 扱い |
|---|---|
| 写真（REF / `baseC`） | **全枠で共有・表示のみ**。直接は描かない |
| ペイント（線・色） | **枠ごとに独立**。A の画と B の画は別物 |
| SHEET | 各サムネはそのまま |
| TIMELINE | **寄り（B）のほうが上に重なって乗る**＝実際の T.U. のセル差し替えと同じ |
| GRID | またいで描いても**同じブラシサイズ**で一気に描ける |

---

## 2. 機構として成立するか（評価）

**成立する。しかも今より素直になる。** 理由:

1. **V3-P1 の土台がそのまま使える。** ベイク空間の層・`plateScale()`・`camSrcRect()`・
   `drawPlateLayer()`・`rebakeFromCam` での載せ替え・濃/モードの全体設定は
   「1枚 → N枚」に増えるだけで、**旧 V2 の「出力枠にベタ置き」には戻らない**。
   フェードが復活しない（＝当初の要望を壊さない）のはこの1点による
2. **メモリが減る。** いまは `drawC` が**ベイク寸法**（最寄り枠が1280pxになる密度＝T.U.3倍なら3840px幅、
   最大4K面積）で1枚。枠ごとにすると **各枠は出力1280×720で足りる**ので、
   T.U.3倍のカットで `3840×2160 → 1280×720 × 2枚` ＝ **約1/3**
3. **ブラシの物差しが自明になる。** 各枠の層が「その枠で1280px」なので、
   `strokeScaleFor()` の枠ごと補正が要らなくなる（どの枠でも `brush` px がそのまま出力px）

### 2a. ★決めないと進めない1点 — 「A の画は B の中でも見えるか」【決定: (b)＋入れ子のときだけ】

| | 案 | T.U.（B ⊂ A） | PAN（A と B が並ぶ） | GRID の見え方 |
|---|---|---|---|---|
| (a) | **重ねるだけ**（B が上） | A の線が B の下に透ける | 継ぎ目なし | **いまの不満がそのまま残る** |
| (b) | **B の枠の中は B が総取り**（下を隠す） | 寄ると B だけになる＝セル差し替えそのもの | **枠の境目で線が切れる** | B のセルが完全にきれいになる ✅ |
| (c) | GRIDだけ(b)・TIMELINEは(a) | — | — | 描く画面と出る画面が食い違う（SPEC_13 §9b の原則に反する） |

**推し = (b)、ただし「後の枠が前の枠を含むときだけ」隠す。**
含む＝T.U./T.B. なので継ぎ目が起きない。並ぶ＝PAN のときは (a) に落とす。
**この判定は矩形の包含だけで機械的に決まる**ので、ユーザーに選ばせる項目にしない。

> (b) を採ると、ヒキで見たときは「A の絵の真ん中に B のパッチが貼ってある」状態になる。
> これは**実際にその画がそう写る**ということなので、嘘ではない。

### 2b. 残る宿題

- **PAN の途中のコマ**は A のパッチと B のパッチの間を通る。塗っていない所は写真だけになる
  （描いていないのだから正しい）。ただし**枠の縁で線が切れて見える**ので、
  のりしろ（`bakeExpand`）を枠ごとの層にも持たせるか、要検討
- **STUDIO の COLLAGE（紙の全面）** は「全枠のパッチを紙の上に並べた合成」で出す。
  V3-P1 の `compositeTo()` がそのまま使える

---

## 3. 実装の当たり（P1 → P4 の差分）

### 3-1. データ構造

```
削除: cut.drawC / drawBlob / dirtyDraw          ← カット1枚・ベイク寸法
      cut.plateC / plateBlob / dirtyPlate       ← カット1枚・ベイク×0.4
新設: cut.fr[k] = {                             ← **枠ごと**（cam[k] と1対1）
        line, lineBlob, dirtyLine,              //  1280×720（出力と等倍）
        plate, plateBlob, dirtyPlate            //   512×288（出力の0.4倍）
      }
```

- `cam[]` の増減と**必ずセットで動かす**（V3-P1 で `snapshotCam` から外した `colors` の扱いを戻す）
- `baseC`（写真）は**カット1枚のまま**。ここは共有で正しい

### 3-2. 描画

```
1コマ描く = 下地 → baseC(枠で切り出し) → 各枠のパッチを紙の上に置く（k昇順・包含なら下を隠す） → …
```
`drawCamFrame()` / `compositeTo()` の合成順は変えない。**「色1枚を置く」が「パッチをN枚置く」になるだけ。**

### 3-3. GRID

- セル = `cam[].key` の枠（いまと同じ）。**セルは自分の枠のパッチだけを描く**（(b) の帰結）
- またぎ塗りは**枠ごとのパッチに、それぞれの座標で**落とす。
  ブラシは全セル共通の `brush` px（枠ごとの補正が消えるので自然にそろう）

### 3-4. 移行

V3-P1 の `plateC`（カット1枚）→ 各枠のパッチへ、**枠の矩形で切り出して**配る。
`drawC` も同様。SPEC_15 P1-6 と同じ「見えていたものがそのまま正になる」形にする。

---

## 4. 済み（V3・2026-08-18）

`ECONTE_HANDOVER.md` の **V3-P1 / V3-A** に詳細。要点だけ:

- **V3-P1**: 色をカット1枚・ベイク空間へ（本仕様の土台）／濃・描画モードは全体設定 `gPaint`
- **性能**: 「ペイント反応鈍く、絵が抜ける」の原因は**座標ではなく描き直しのコスト**だった。
  座標は GRID / SINGLE とも実測でズレ 0.3% 未満。
  `previewSegOnCell()`（引いている間は区間だけ重ねる）＋ rAF で1フレーム1回に間引き。
  **GRID線の JS占有率 75.2% → 0.4%**
- **バグ4件**: GRIDの CLEAR BASE が戻せない／Ctrl・Shift の複数選択／◆の改行／尺の下限1コマ
  - ◆の改行は最終案 = **カット尻の◆を「そのカットの最後のコマ」に置く**（次のカットの頭は動かさない）

---

## 5. 残っている依頼（2026-08-18 のメモ由来）

### B. 手癖・操作（V4 と同じ回で入れる。SPEC_15 P3-2〜P3-4 を含む）

| | 内容 | 補足 |
|---|---|---|
| B1 | **描く先・ブラシサイズを画面移動で変えない**（SPEC_15 P3-2 / P3-3） | `setEditMode` / `setView` の `setPaintTarget` を消す。ブラシ上限は1つ |
| B2 | **econte だけ FILL（クリック判定の塗りつぶし）を廃止** | **初めから投げ縄塗り・長押しで投げ縄透明消し。Wクリック切替は無し**。ラフとスピード優先の道具なので ANIMATOR とは意図的に変える |
| B3 | ボタンの点灯 | B2 の2状態（投げ縄塗り／透明消し）が見た目で分かること |
| B4 | **指操作メイン・ペンは描画特化**（GRID に限らず全画面） | ペンはペイントと投げ縄選択のみ。キー移動・枠の微調整だけ例外 |
| B5 | パレット既定を**縦3** | |
| B6 | CUTS の表示は**目のアイコンだけ**で ON/OFF | |

> **スキルはそのままでよい。** `animator-brush-ops` は ANIMATOR が正準。
> B2 は econte だけの意図的な逸脱なので、**スキルは触らず ECONTE_HANDOVER に逸脱として記録する**。

### C. 機能追加

| | 内容 |
|---|---|
| C1 | 中央キャンバス（STUDIOプレビュー）の**ホイール拡縮／ホイール押下ドラッグ移動** |
| C2 | **SINGLE もホイール押下ドラッグで移動**（いま出来ない） |
| C3 | GRID の余白を**ホイール押下ドラッグで手のひら移動** |
| C4 | ref_board リンクの**差し替え／消す**（貼る→選ぶ→組み込む→飛ぶ は実装済み） |
| C5 | 画ブレ → **実装済み。キーは打たない**（下の §5-C5 で確定） |
| C6 | イーズ AE 準拠＋キーアイコン COMPOSER 合わせ → **実装済み。区間ではなく「キーごと」に確定**（下の §5-C6） |
| C7 | シートの `Ctrl+Shift+↑/↓` で最上位／最下位へ |
| C8 | GRID の一括: ERASE Wクリック全消し・BG GRAY も複数選択で同期。**投げ縄塗りは選択に関係なくまたいで塗る** |
| C9 | MOVE の選択枠色をグレーに（配置後はほぼ動かさないので目立たせない） |
| C10 | 書き出したカラースクリプト画像の**読み戻し**＋画像を1枚＝1カットで取り込む → **実装済み**（下の §5-C10）。**動画の読み込みはユーザー判断で見送り** |

### D. iPad（PC を仕上げてから。iPad用仕様はスキル化を先に検討）

| | 内容 |
|---|---|
| D1 | 写真アプリから**複数ドラッグ＆ドロップ**で MOVE に読み込み |
| D2 | MOVE の3本指タップがビュー移動とブッキング |
| D3 | **スポイトになるのが早すぎる**（長押し判定を遅く。undo中も発動している疑い） |
| D4 | Wタップの投げ縄切替がほぼ反応しない → **B2 で切替自体が無くなるので解消見込み** |
| D5 | **C.SCRIPT が1回しかDLできない／MP4が0%で止まる**（もう一度タップでキャンセルも） |
| D6 | パラメータのスクラブ・ボタンON/OFFも**指専用**にしてペンは描画に振り切る |

### バグ（未着手）

| | 症状 | 分かっていること |
|---|---|---|
| E1 | **GRIDで Ctrl+Z が12回くらいで効かなくなる** | `COLOR_LOG_MAX = 12` の上限そのもの。1件が plate の before/after 2枚（最大4.7MB）なので浅く持っている。**変更矩形だけ持てば桁で深くできる**。V4 で Undo の持ち方に触るので**同じ回でやる**のが良い |

---

## 6. 関連

- `SPEC_15_ECONTE_V3.md` — V3（**P1 / P2 / P3 すべて実装済み**）。**本仕様は P1 の続き**。
  P2（フォトバッシュ＝浮いた選択）は V4 の後に実装した。V4 の枠ごとパッチへの読み替えは SPEC_15 **P2-6**
- `SPEC_13_ECONTE_V2.md` §9 — 画面の役割。**着手前に読む**
- `LP_motion-graphics/TOOL_MECHANICS/SPACES_LAB/spaces-lab.html` — 色の置き場所を触って確かめる教材。
  **V4 の「枠ごとのパッチ」もここで先に見られるようにすると判断が速い**
- `PIPELINE.md` — 保存形式が変わるので V4 実装時に更新

---

## 5-C5. 画ブレ【確定・実装済み 2026-08-19】

起草時は「MOVEでPANと同様にセット／シートにON/OFF／TLのキー行は2行」だったが、
ユーザーの追記で **キーは打たないほうが便利** に落ち着いた:

> 強めていく、弱めてブレがなくなる の調整さえできれば PAN などほどキーでやらないほうが便利かもです。

### 持ち方

```
cut.shake = { a0, a1, freq }     // カットに1組だけ（枠ごとではない）
  a0 = 頭の強さ / a1 = 尻の強さ（0〜100）… カット内で直線に変わる
  freq = 速さ（1秒あたりの揺れの回数。既定 8）
```

| やりたいこと | 設定 |
|---|---|
| 一定に揺らす | a0 = a1 |
| 強めていく | a0 = 0 → a1 = 強 |
| 弱めてブレがなくなる | a0 = 強 → a1 = 0 |
| なし | a0 = a1 = 0（`cut.shake` を消す） |

### 決めごと

1. **揺れ幅は「いま写っている枠の幅」に対する割合**（`SHAKE_MAX = 0.045`）。
   px で持つと T.U. で寄った瞬間だけガタガタになる。
2. **乱数を持たない。** `shakeSeed(cut)`（id のハッシュ）＋値ノイズで、
   **コマ番号から一意に決まる**。再生し直しても書き出しても同じ揺れになるし、保存も要らない。
3. **揺らすのは出力だけ**（`drawCamFrame`）。GRIDのセル・SHEETのサムネ・C.SCRIPT は
   `drawCellFrame` を通るので**静止したまま**（一覧が動くと選べない）。
4. 中央プレビューで直接塗るときは、**表示と同じ揺れを通した座標**に落とす
   （`camToBake(..., true)`。止めて描いている限り決定論なのでズレない）。
5. UI は **BOARD で枠を選んだときのバーの ∿**（＋枠 の隣）＝「PAN を仕込むのと同じ流れ」。
   SHEET の行には **∿** を出して「使っているカット」が一覧で分かるようにする。

**入れていない**: 回転ブレ。`camSrcRect` が軸揃えの矩形しか扱わないので、
入れるなら合成側に回転描画を足すことになる（要望が出てから）。

---

## 5-C6. イーズ【確定・実装済み 2026-08-19】

ユーザーの追記で2点が確定した:

> 初めのスタートがイーズアウトで加速、終わるほうがイーズインで減速
> キー自体のクリックやタップでイーズの選択が出ると、どっちのキーがどのイーズなのかがわかりやすい

つまり **「区間で選ぶ」ではなく「キーごと」**。起草時の §5-C6 の書き方（区間で選ぶ）はここで**破棄**する。

### 持ち方（COMPOSER が正準）

```
cam[k].eo   = この◆から出る側の効き（0〜100）＝ AE のイーズアウト ＝ 加速
cam[k].ei   = この◆へ入る側の効き（0〜100）＝ AE のイーズイン   ＝ 減速
cam[k].hold = この◆で止まる（次の◆まで動かない）
```

区間 k→k+1 のカーブ ＝ `cubic-bezier(cam[k].eo/100, 0, 1 - cam[k+1].ei/100, 1)`。
`bezierEaseT()` は **composer.html からそのまま転載**（式を変えたら両方直す）。

### 見た目

◆の**形**で一目で分かる。COMPOSER の `.tl-kf-d` と同じ語彙:

| 形 | 意味 | 色 |
|---|---|---|
| 菱形 | 緩急なし | 既存のまま（gold / 先頭・最終は ice） |
| 角丸 | 効き中（1〜98） | `#5AE9FF` |
| 丸 | 効き最大（99〜100） | `#FF4FA8` |
| 灰の四角 | タメ（HOLD） | `#8892a0` |

> econte は**先頭/最終キーがもともと ice** なので、そのままだと「ice の菱形」と
> 「ice の角丸」が紛らわしい。効きのあるキーには**外側の光る輪**を足して区別している。

### 操作

- **◆をクリック** → そのキーの緩急（入＝減速 / 出＝加速 / タメ）。
  先頭の◆には「入」を出さない／最終の◆には「出」とタメを出さない（意味が無いため）。
  **ドラッグ（尺の配分）とは「動かさずに離したか」で切り分ける**ので、同じ◆に2つの役目を持たせられる。
- **区間バーをクリック** → 現場の名前で区間まるごと（早撃ち用。中身は左右キーの eo/ei を書くだけ）。
  プリセットに合わない組み合わせは `出 66% ／ 入 100%` と数字で出す（**嘘の名前を付けない**）。

### 旧データ

`migrateEase()` が「区間に1つの名前」をキーへ開く。**旧 IN/OUT の名前の逆転もここで直る**:

| 旧（区間） | 新 |
|---|---|
| LINEAR | eo 0 / ei 0 |
| EASE | 左 eo 66 ／ 右 ei 66 |
| IN（遅い出だし） | 左 eo 80 |
| OUT（遅い止まり） | 右 ei 80 |
| Q.PAN | 右 ei 45 |
| FAIRING | 右 ei 100 |
| HOLD | 左 hold |

区間で共有されるキー（B は A→B の右キーであり B→C の左キーでもある）は**強いほうを残す**。

---

## 5-C10. 読み戻し／リファレンスの取り込み【確定・実装済み 2026-08-19】

ユーザーの関心は「書き出したものを戻す」より **「参照の画像を、参照やトレースに使えるか」** だった。
動画の読み込みは本人の判断で見送り（「ひとまず動画は自分で必要なとこをスクショして入れればよさそう」）。

### 前提：econte に「参照レイヤー」は要らない

**BOARD の写真がそのまま参照**で、CUT枠で切り出せば `baseC` になり、V4 では
**REF（濃度・上下を切り替えられる下敷き）として全枠で共有**される。
`animator-ref-overlay`（ANIMATOR が正準）は「1枚の参照を浮かせて動かす」仕組みで前提が違うので**持ち込まない**。
C10 がやるのは **写真を置いて 16:9 の枠を自動で合わせる**、それだけ。

### 入口

| 操作 | 結果 |
|---|---|
| BOARD へ D&D ／ `+ IMPORT` ／ Ctrl+V | **今までどおり写真**（紙のネームを並べる） |
| SHEET へ D&D ／ `+ カット` | **1枚 = 1カット（16:9）** として取り込む |

**落とす場所で意味が決まる**ようにしてあるので、モード切替のUIを持たない。

### 決めごと

1. **枠のほうを固定して、画像をその中に収める**（升目 1400×787.5 に contain）。
   逆にすると縦長の画像で枠が横へ伸び、**隣に置いた参照画像まで焼き込む**。
2. 16:9 の画像は枠とぴったり重なる（スクショ・一覧PNGのセルは 1:1）。
   16:9 でなければ余白が出て ⚠ が付く——**端を切っていない印**として正しい。
3. `baseAlpha = 1`。取り込んだ絵はそれ自体が画なので薄くしない
   （トレースしたければ REF のスライダーで落とす。BOARD から切り出した通常のカットは従来どおり 0.5）。
4. **取り込み全体で Undo 1手**（`gLogMute`）。
5. 尺は既定（2秒）。「24fps を REF として」の fps は静止画では効かないので使っていない。

### カラースクリプトPNG の読み戻し

`csGeometry()` が **`exportColorScript` の寸法式を逆に解く**:

```
W = MG*2 + COLS*CW + (COLS-1)*GAP          GAP    = 0 | 14   （列間 ON/OFF）
H = MG*2 + HEAD + rows*(BAR+CH+CAP+PAL+RG) PAL    = 0 | 20   （色帯 ON/OFF）
                                           ROWGAP = 8 | 92   （行間 ON/OFF）
   MG=28 CW=480 CH=270 BAR=5 CAP=30 HEAD=46
```

整数解が出た候補を **実際の画素**（余白と列間が真っ黒か）で絞る。
最終行の**空き升目**は、**下にキャプション（`C1-A` のピンク文字）が有るか**で落とす
——絵は真っ黒になり得る（地色0のカット）が、キャプションは必ず描かれるため。

> ⚠ **`exportColorScript` の数値を変えたら `csGeometry` の定数も直す。** 片方だけ変えると外れる。

**実測**: 書き出した PNG を読み戻したセルは、書き出す前の `drawCellFrame` と**画素差 0%**。
同寸法の白画像／ふつうの写真／黒いが寸法違いの画像は、いずれも格子扱いしない（誤判定なし）。

### やっていないこと：動画 → 連番写真

見送り。将来やるなら **動画からコマを抜いて `importAsCuts()`（画像の配列を受け取る）に流すだけ**で済む。
`crv`（claude-real-video）で外側で抜いても同じ結果になるので、実際に使う場面を見てから決める。

なお「プロジェクトそのものを戻す」用途は **PROJECT ZIP（`ver:4`）が本命**
（写真＋枠列＋パッチ＋音＋マーカーが全部入る）。画像からの復元は
「ZIP を無くしたが PNG は残っている」場合の保険という位置づけ。
