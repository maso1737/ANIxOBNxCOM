# SPEC_19 — ECONTE V5「画の単位をプレートに」＋ GRID 一本化

作成 2026-09-13。対象 `econte.html`（V4 = SPEC_16 実装済みの状態が起点）。
**実装者向け。着手前に `ECONTE_HANDOVER.md` の「V4 枠ごとの画」表と `ROADMAP.md` §3 を読むこと。**

---

## 0. いま何が起きているか（診断）

V4 で「画は枠（`cam[k]`）ごとに1枚」（`cut.fr[k]` = 1280×720 を `cam[k]` の矩形に貼る）にした。
これで **T.U. の B セルに A の太い線が出る**問題は消えたが、代わりに3つ出ている。

| # | 症状 | 原因（コード上の場所） |
|---|---|---|
| S1 | **STUDIO／動画書き出しで、PAN の途中コマに画面の淵から「ペイントの切れ目」が出る** | PAN は §2a(a)「重ねるだけ」なので、A のパッチと B のパッチが**別々の1280×720**として紙に貼られる。GRID の A セルで枠いっぱいまで塗った線は、A の矩形の縁でパッチごと終わる。途中コマは A と B の縁が両方とも画面内に入る＝縁が見える（`drawPatchStack` `econte.html:1758`、`drawCamFrame` `:3941`） |
| S2 | **SINGLE で枠の外（余白・A と B の隙間）に描いた線が消える** | `frameAtBake()`（`:1720`）は枠外の点を「いちばん広い枠」に振るが、`bakeToPatch()` はクリップしないので **パッチ寸法の外の座標**に描く＝canvas 外で捨てられる。ユーザーには「描けるのに残らない」に見える |
| S3 | **SINGLE（紙全面）と GRID（枠1つ）で「画の単位」が違う**ので、同じ絵を描いても見え方が一致しない。ボタンも STUDIO / EDIT / GRID / SINGLE の4つで、EDIT＝GRID の重複がある | `setEditMode` `:5167` ／ `#btn-mode-single` `#btn-mode-grid` `:1000` |

矛盾の根は1つ：**「枠」は撮影の単位であって、画の単位ではない**。
アニメの実務でも PAN は1枚の長い紙、T.U. だけがセル差し替え（別の紙）になる。
V4 は「全部別の紙」にしたので PAN で紙が割れた。

---

## 1. 決めごと（ユーザー提案 2026-09-13 を仕様化）

### 1-1. 画の単位＝**プレート**（同じ倍率の枠群の合併）

```
cut.pl[j] = {
  frames: [k, ...],          // この紙に乗る枠（cam の index・昇順・連続）
  rect:   {x,y,w,h},         // 紙（ベイク空間）の上での矩形
  scale:  number,            // 1枠の幅を 1280px にする密度 = CONTE_W / cam[frames[0]].w（bakeRect 単位）
  line, lineBlob, dirtyLine, // px = rect × scale（line は出力等倍）
  plate, plateBlob, dirtyPlate  // 色。line の PLATE_K(0.4) 倍
}
```

**グルーピング規則（機械的・ユーザーに選ばせない）**
- 隣り合う枠 k, k+1 の幅の比が **1/1.25 〜 1.25** なら同じプレート（＝PAN / 微ズーム付き PAN）
- それ以外（T.U. / T.B.）は新しいプレート
- 枠が1つのカット（FIX）はプレート1つ
- **いちばん広い倍率のプレートは `rect` = 紙全面（`bakeRect`）**。これで S2 が消える（紙のどこに描いても受け皿がある）
- それ以外のプレートは `rect` = 枠群の union に **のりしろ 4%（枠幅基準）** を足して紙内でクリップ
- 面積上限は既存 `MAX_AREA`（3840×2160）でクランプ（横 PAN 3枠 = 3840×720 は上限内）

**隠す規則**は V4 の `patchHides()` をプレート単位に読み替える：
後のプレートが前のプレートの **枠に包含される**（T.U./T.B.）ときだけ、その矩形の下を隠す。
PAN は同一プレートなので **継ぎ目という概念自体が無くなる**（S1 の解決）。

**ブラシの物差しは V4 のまま**：どのプレートも「1枠の幅 = 1280px」で作るので、`brush` の数値 = 出力 px。
`patchK()` の定数（line=1 / plate=0.4）はそのまま。**枠ごと補正は復活させない**。

> 残る縁は T.U. の B プレートの縁だけ（SPEC_16 §2a で「実際にそう写る」と合意済み）。
> ズーム付き PAN で幅比が 1.25 を超えると別プレートになり縁が出る。まれなので許容。
> 実機で頻発するなら比を 1.5 に緩めるだけで済むように `PLATE_SPLIT_RATIO` 定数にしておく。

### 1-2. GRID のセル＝プレート・**統一解像度**で並べる

- **セルの表示倍率は全セル共通** `s` = `GC_BASE × gGridZoom / 1280`（出力1pxあたりの画面px）
- セル幅 = `rect.w × scale × s`、高さも同様。FIX は今と同じ 200×112（zoom 1）。横 PAN は横に伸び、縦 PAN は縦に伸びる
- **同じブラシ数値が全セルで同じ太さに見える**（統一解像度の目的）。拡大は既存 `gGridZoom` のまま
- 配置は **シェルフ詰め（順序厳守）**。参考画像（TS3 カラースクリプト）の「PAN は横一連」と同じ絵になる
  - 単位 = 16:9 セル1段の高さ `H` と、その **半分幅** `U = H×16/9/2` の格子に量子化
  - 各セルは `ceil(w/U)` 列 × `ceil(h/H)` 行を占める（縦 PAN は2〜3行）
  - スカイライン法：列ごとの「埋まっている高さ」を持ち、セルは **y が最小になる x（同点なら左）** に置く。
    ただし **y は直前のセルの上端より上に行かない**（読み順＝左→右、上→下 を崩さない。`grid-auto-flow: dense` は順序を乱すので使わない）
  - 空いた穴はそのまま（ガタツキ許容）。**`.gc` は絶対配置**（`#grid-wrap` に left/top を書く）
- **行のカラー帯（`.gr-pal`）は廃止し、セル帯（`.gc-pal`）にする**：各セルの下、キャプションとの間に `extractPalette()` の6色を横帯で出す。
  行を明示的に組む理由（`renderGrid` のコメント）が消えるので DOM は `#grid-wrap > .gc` の1階層になる。HUD の「色帯」トグルはそのまま流用
- セル内に **枠ガイド**を薄く出す（`drawCamGuide` の縮小版：A=水色 / 以降=桃、ラベル付き。画には焼かない）
- キャプション：`C12 A→B`（プレート内の枠名を並べる）。尺バー = プレート内の枠の `cellFrames()` 合計
- **`cam[].key`（★）は GRID の表示から外す**（PAN の中間枠は「帯の途中」なので選ぶ意味が無くなる）。データは互換のため残す。UI の★は消す＝ボタンが1つ減る
- `updateCellRes()` の「可視セルだけ高解像度」はそのまま。セル canvas の px はプレート px を `s` で割った寸法（上限 `GC_MAX_RES` は幅ではなく**面積**で見る＝横長セルで頭打ちしない）
- **またぎ塗り（`gridStrokeStart/Move/Up`）は残す**が、PAN が1セルになるので発火が減る。T.U. の A/B セル間では今までどおり働く
- C.SCRIPT 一覧 PNG（`exportColorScript` `:8214`）は **GRID と同じレイアウト関数を通す**（セル幅がプレートに比例した絵が出る）

### 1-3. SINGLE を GRID に吸収 ＝ **FOCUS**

ボタンは **STUDIO / GRID の2つ**にする。`#btn-view-edit`（EDIT）は GRID に改名、`#btn-mode-single` / `#btn-mode-grid` は撤去。
キー：`1` = STUDIO / `2` = GRID / `X` で行き来（現状維持）。`3` `4` は空ける。

| 入口 | 動き |
|---|---|
| セルの **Wタップ／ダブルクリック／✎** | そのプレートが画面いっぱい（**FOCUS**）。周囲の GRID は消す（暗くしない＝iPad の描画コスト） |
| FOCUS 中の **ESC／⤢ボタン／ピンチインで縮小下限** | GRID に戻る。戻り先のセルへ `scrollIntoView` |
| STUDIO の ✎ ／ SHEET のダブルクリック（旧 `openEdit`） | そのカットの **プレート0 を FOCUS** で開く（＝旧 SINGLE と同じ「紙全面」） |

- 実装は **旧 SINGLE の `editCv` をそのまま使う**。違いは `renderEditCanvas()`（`:5308`）が描くものが「紙（`compositeTo`）」から「**プレート矩形の切り出し**」になるだけ。
  プレート0 は紙全面なので、旧 SINGLE と同じ画が出る（機能の喪失なし）
- `editCv` の寸法 = プレートの px（`rect × scale`）。`toCanvasCoord` は `el.width/height` を見る作りなので変更不要
- **V2-C のズーム／パン・トレース透かし・WARP（`gFloat` / `floatLift` `:6104` / `floatCommit` `:6359`）・投げ縄は無改修で FOCUS に乗る**（描く先が「プレート」に変わるだけ。`floatKindNow()` の規則も不変）
- FOCUS 中の Undo は EDIT 共通の `gColorLog`（V4 E1 で1本化済み）。**行き先を増やさない**
- GRID のセル上で直接 WARP は **やらない**（FOCUS が1タップ先にあるので足さない。ボタンを増やさない原則）
- `gEditMode` は `'grid' | 'focus'` の2値に。`inSingle()` `:5885` は `inFocus()` に改名して同じ場所で使う

### 1-4. STUDIO 中央プレビューのペイントは現状維持

SPEC_13 §9a の「再生しながら描く」は残す。描く先は `plateAtBake()`（§2-2）で決まるので変更は経路の関数名だけ。

---

## 2. 実装の当たり

### 2-1. データ・移行（`meta.ver` 4 → 5）

- `ensureFr()` `:1657` → `ensurePl(cut)`：`cam[]` から §1-1 の規則でプレート列を組む。**`cam` を増減・移動した後は必ず通す**
  - 枠を動かして **グルーピングが変わる**（PAN が T.U. になる等）ときは、プレートを分割／合併する。
    分割 = 旧プレートから新 rect を切り出す（`splitBakeToPatches` `:1465` と同型）／合併 = 各プレートを新 rect に貼る。
    どちらも「見えていたものがそのまま正になる」（SPEC_16 §3-4 と同じ方針）
  - `syncPatchRects()` `:1815` の役目（枠を動かしたら絵が追従）は `rect` をプレートに持たせて同じ形で残す
  - `seedPatch()` `:1831`（枠を足したら見えていたもので埋める）はプレートが新設されるときだけ発火
- `snapshotCam()` は `pl` の並びごと控える（V4 で `fr` に対してやっているのと同じ）
- IndexedDB：`cut.pl[] = {frames, rect, scale, line, plate}`。ZIP：`cuts/<id>.p<j>.line.png` / `.plate.png`
- **移行 ver4 → 5**：`fr[k]` をグルーピングし、各プレートの canvas に `fr[k].line` を `fr[k].rect` の位置へ **等倍で** `drawImage`（同一プレート内は同倍率なのでリサンプル無し）。ver3 以前は既存の経路で一度 ver4 に上げてから
- LRU：`cellCache` はプレート単位（`cut.cellCache[j]`）。作る瞬間は今と同じ「追い出す直前だけ」

### 2-2. 描画・ストローク（関数名の読み替え表）

| V4（枠） | V5（プレート） | 変更点 |
|---|---|---|
| `camBakeRect(cut,k)` | `plBakeRect(cut,j)` | `pl[j].rect` をベイク px に |
| `patchHides(cut,k)` | `plHides(cut,j)` | 前プレートの**枠**との包含で判定 |
| `frameAtBake(cut,bx,by)` | `plateAtBake(cut,bx,by)` | 後ろから探す。**必ずどれかに入る**（プレート0 が紙全面）ので fallback 不要 |
| `bakeToPatch(cut,k,p,kind)` | `bakeToPlate(cut,j,p,kind)` | 同型 |
| `drawPatchStack(ctx,cut,src,dw,dh,kind,only,raw)` | 同名・`only` はプレート index | 合成順 k昇順 → j昇順 |
| `drawCellFrame(ctx,cut,k,…)` | `drawCellPlate(ctx,cut,j,…)` | src = `pl[j].rect`（枠の切り出しではなくプレート全面）。SHEET のサムネと C.SCRIPT も同じ関数（V4 と同じ「一致」の原則） |
| `strokeSeg` → `strokePatchSegBake(cut,k,…)` `:5586` | `strokePlateSegBake(cut,j,…)` | 区間がプレートをまたいだら両方に引く（今と同じ）。**ストローク進行の状態はベイク空間に1つ**（ROADMAP §3-1 の原則は不変） |
| `txTouch(cut,k,kind,…)` `:5503` | `txTouch(cut,j,kind,…)` | key の意味だけ |
| `drawTakeoverGuide` `:4800` | 同名 | T.U. の A セルにだけ出る（PAN では出なくなる） |

`drawCamFrame()` `:3941` は `drawPatchStack` の中身が変わるだけで**呼び側は無改修**。動画書き出し（`exportVideo` `:8104`）も同様。

### 2-3. 進行中の「ブラシ登録」との関係

ROADMAP §3 の注意点1・3（区間が2パッチに二重に描かれる／半透明ブラシはオフスクリーンが枠ごとに要る）は
**プレート化で PAN については消える**（同一プレートなので跨がない）。T.U. の A/B 間だけ残る。

**順序の推奨：プレート化（本 SPEC P0）→ ブラシ登録。** ブラシを先に枠 API に乗せると `k`→`j` の付け替えが二度手間になる。
BRUSH LAB の契約4点（`brush-lab.html` の CLAUDE.md 参照）は `k` を `j` に読み替えてそのまま有効。

### 2-4. iPad（SPEC_16 §5-D / SPEC_18 P3）

- FOCUS の出入りは **Wタップ／ピンチ**（ボタンを増やさない）。`bindDoubleTap` の「複数選択のタップ中は開かない」ガードは維持
- 統一解像度のセルは横に長いものが増える。**指の横スクロール**は自前（`gridTouchDown/Move/Up`）のまま、`#grid-wrap` の幅は `max(セル右端)` にする
- 4本指タップ再生・◆の長押しは SPEC_18 P3 側の話なのでここでは触らない

---

## 3. フェーズ

| P | 内容 | 見た目の変化 | 確認 |
|---|---|---|---|
| **P0** | `pl[]` 新設・移行・`drawPatchStack` / ストローク経路のプレート化。GRID は暫定でプレート0 だけ出す | STUDIO／動画の **PAN 途中コマから縁が消える**。それ以外は同じ画 | `verify:econte` で **C3 PAN の中間コマだけ差分**・他は一致 |
| **P1** | GRID：セル＝プレート、統一解像度シェルフ配置、セル帯、★撤去、C.SCRIPT 同レイアウト | PAN が横一連、縦 PAN が2段 | 新フィクスチャ C6（縦 PAN）・C7（PAN→T.U.）を追加して承認 |
| **P2** | FOCUS：SINGLE 撤去・ボタン2つ・キー整理・`openEdit` の付け替え | ボタンが2つ減る | 動作チェック表（§4） |
| P3 | ブラシ登録（別進行）をプレート API に乗せる | — | ROADMAP §3 |

P0 と P1 の間で一度 iPad 実機に当てる（横長セルのスクロールと `updateCellRes` の面積上限）。

---

## 4. 動作チェック表（完成報告に必ず付ける・⚠️未確認 のまま完了と言わない）

| パラメータ／操作 | 期待される変化 | 有効になる条件 | 結果 |
|---|---|---|---|
| PAN カットの A セルで右端まで塗る → STUDIO 再生 | 途中コマに縁が出ない | PAN（幅比 ≤1.25） | ⚠️ |
| T.U. カットの B セルに塗る → A セル | A セルに B の領域が破線で出る（V4 と同じ） | T.U.（包含） | ⚠️ |
| FOCUS（プレート0）で余白に描く → GRID | 消えずにセルに出る（S2） | 常時 | ⚠️ |
| `brush` = 6 で FIX セルと PAN セルに描く | 画面上の太さが同じ | 常時（統一解像度） | ⚠️ |
| `gGridZoom` 最小⇔最大 | 全セルが同率で拡縮、列数が変わる、PAN セルは横長のまま | 常時 | ⚠️ |
| 縦 PAN のセル | 2〜3 行分の高さ。順序が崩れない | 縦 PAN あり | ⚠️ |
| 枠を動かして PAN → T.U. に変える | プレートが分割され、見えていた絵が保たれる | 幅比が 1.25 を跨ぐ | ⚠️ |
| Wタップ／✎ → FOCUS → ESC | 元のセルへ戻る | GRID | ⚠️ |
| FOCUS 中の WARP・投げ縄・Ctrl+Z | V4 の SINGLE と同じに動く | FOCUS | ⚠️ |
| STUDIO 中央プレビューに描く | プレートへ乗り、GRID に反映 | ✎ TOOLS 表示中 | ⚠️ |
| C.SCRIPT 書き出し | GRID と同じ並び・同じ画 | 常時 | ⚠️ |
| 動画書き出し | PAN の途中に縁が出ない | PAN | ⚠️ |
| ver4 プロジェクトの読み込み | 絵が1px もズレない（等倍貼り） | 旧データ | ⚠️ |
| iPad：横長セルを指で横スクロール／ピンチ | ペンは描画のみ、指はスクロール | `pointer: coarse` | ⚠️ |

---

## 5. 関連

- `SPEC_16_ECONTE_V4.md` §2a（隠す規則）／§3-3（GRID セル）— 本 SPEC で「枠」を「プレート」に読み替える
- `SPEC_13_ECONTE_V2.md` §9a（画面の役割）— SINGLE の行は FOCUS に置き換わる。STUDIO 中央のペイントは残す
- `ROADMAP.md` §3（ブラシ追加）— 順序は本 SPEC P0 が先
- `ECONTE_HANDOVER.md` 「V4 枠ごとの画」「GRIDの作り」— 実装後に同じ表形式で V5 を追記すること
- `verify/verify.econte.config.json` — C6 / C7 を足す
