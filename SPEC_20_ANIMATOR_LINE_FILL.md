# SPEC_20 — ANIMATOR「線と塗りの2レーン」＋ REF レーン ＋ 変形（econte WARP 移植）

作成 2026-09-18。対象 `animator.html`（2026-09-15「参照まわりを1本化」までが起点）。
**実装者向け。着手前に `ANIMATOR_HANDOVER.md` の「レイヤー構成」「主要な state」「autosave の容量実測」を読むこと。**
発注者との合意 2026-09-18／2026-09-19（本文 §1 の「決めごと」がその写し。§5 に確定事項）。
**UI は animator の現行デザインで統一する**（§1-9。新 OBAN のデザインシステムは持ち込まない＝2026-09-19 発注者判断）。

---

## 0. いま何が起きているか（診断）

| # | いまの形 | 困りごと |
|---|---|---|
| S1 | コマの絵は `frames[].drawData`（RGBA 1枚）で、**線も塗りも同じバッファ**に焼かれている（`syncFrameFromCanvas` `animator.html:4391`） | AE 用に線と塗りを別素材で出せない。塗りが線を汚す・線が塗りを汚す。塗りの縁に線のアンチエイリアスの白い隙間が出る |
| S2 | REF は3種（参照 ANIMATOR＝ティック同期・PASTE 画像＝静止・FRAME＝描ける下絵）を**フローティング REF パネルの行**でしか見られない | 「どの参照がいつ出ているか」「尺がどこで尽きるか」が時間軸の上で見えない。表示間隔（コマ打ち）も時間オフセットも無い |
| S3 | 描いた絵の**選択→拡縮/回転/コピペ**が無い（HANDOVER「将来フェーズ・見送り中」） | econte には `箱+rot+warp` が実装済み（`floatLift` `econte.html:6104` / `floatCommit` `:6359`・実測 3ms 台）なのに animator に無い |

土台として使える既存の仕組みが3つある。**新しく作らず、これを伸ばす。**

- 描く先の切替は FRAME LAYER の EDIT が既にやっている：`activeDrawCtx()`（`:5598`）が `frameLayerCtx` か `drawCtx` を返すだけ。
- 参照は `color`・尺（`ticks.length`）・`x/y/scale`・`locked` を持ち、`renderRefLayer(tick)`（`:2756`）が1本で描いている。
- タイムラインの横軸は `tickToPx()`（`:4600`）で、ワークエリアの黄色帯もこれで置いている。REF の帯も同じ物差しで置ける。

---

## 1. 決めごと

### 1-1. データ：コマ ＝ `line` ＋ `fill`

```js
frame = {
  id, kind, duration, hidden,
  line: Uint8ClampedArray | null,   // 旧 drawData。W×H×4
  fill: Uint8ClampedArray | null,   // ★最初の塗りまで null（メモリを増やさない）
  thumbCanvas,                      // 合成サムネ（従来どおり1枚）
  history: [{ lane:'line'|'fill'|'both', line?, fill? }], histIdx,   // ★エントリにレーン印
  strokeCount, ...
}
state.lane  = 'line' | 'fill'                                  // いま描く先
state.lanes = { line:{visible:true, locked:false, opacity:1},
                fill:{visible:true, locked:false, opacity:1} } // レーン全体の属性（セルごとではない）
```

- **タイムラインは1本、レーンが2つ。** 線と塗りは必ず同じコマ打ちなので、独立した2本にしない（同期ズレの管理が増えるだけ）。
- `fill` は遅延確保。線の段階のコマは今と同じメモリ量。塗り済みのコマだけ生 9MB → 18MB（2048×1152）。
  IndexedDB は圧縮が効くので 0.42MB → 0.7MB 程度の見込み（HANDOVER「autosave の容量実測」の比で）。
- 旧プロジェクト（`drawData` のみ）は **丸ごと `line` に入れる。分離しない**（§5-1）。

### 1-2. 描画：2枚の canvas、描く先は `activeDrawCtx()` の分岐が1つ増えるだけ

`#canvas-wrap` のスタック（上から）：**2026-09-23 P0 で確定**

```
guide-layer / draw-layer（＝線レーン・id は据え置き） / onion-next / onion-prev / fill-layer（★新＝塗レーン） / refimg-layer / frame-layer / ref-layer / bg-layer
```

起草時の案から2点変えた（§7 に理由）：
- **線レーンの id は `draw-layer` のまま**（改名しない）。JS は `lineCanvas`/`lineCtx` を `drawCanvas`/`drawCtx` の別名として足しただけ。
- **オニオンは塗の上・線の下**。塗の下に置くと、塗り終えたコマではオニオンが塗りに隠れて前後の線が読めなくなる。

- 線は塗りの上。セルの見た目そのまま。**合成はブラウザのコンポジタが GPU でやる**ので、ストローク中の JS は今と同じ「1枚に描く」だけ。
  ストロークごとに自前で1枚へ合成し直す方式は**取らない**（重くなる唯一の分岐なので禁止）。
- `activeDrawCtx()` ＝ `frameLayer.editMode ? frameLayerCtx : (state.lane === 'fill' ? fillCtx : lineCtx)`。
  `floodFill` `lassoFillPolygon` `pushHistory` `syncFrameFromCanvas` `loadFrameToCanvas` は**この1関数経由に揃える**（§2-2 の表）。
- レーンの `visible` / `opacity` は canvas の CSS（`display` / `opacity`）で効かせる。FRAME LAYER の `applyFrameLayerStyle()`（`:2524`）と同じ作法。

**道具 × レーンの規則**（「効かない＝バグ？」を作らない）

| 道具 | 線レーン | 塗レーン |
|---|---|---|
| PEN | `inkColor` | **`fillColor` で描く**（塗り残しの補修用。§5-2） |
| ERASE | ○ | ○ |
| FILL / 投げ縄塗り（FILL 長押し） | **押した瞬間に塗レーンへ移る**（トースト「塗レーン」）。~~FILL ボタンの「塗」バッジ~~は作らない（押せば必ず移るので、いま線レーンにいる状態で FILL を持つことが無い） | ○ |
| SEL（変形・§1-7） | 現在レーン（HUD の「線+塗」で両方） | 同左 |
| 🔒 ロック中 | 選べるが描けない。ストローク開始で「🔒 線レーンはロック中」トースト。FILL の自動移動先がロック中でも移動はする（描けない理由はトーストで出る） | 同左 |

- **オニオンスキンは線レーンだけ**（§5-3）。`drawOnionSide()`（`:5159`）が拾うのを `f.line` にする。`onionTint` のキャッシュキーも `line` 配列へ。
  設定パネルに「オニオンに塗りを含める」（既定 OFF）。
- サムネ・タイムラプス（`tlComposeShot` `:7795`）・VIDEO は合成（`fill` の上に `line`・レーン不透明度は**掛けない**＝出力は常に 100%）。

### 1-3. 塗りが線を参照する（ここが分離の最大の実利）

`floodFill()`（`:3919`）は今「自分のバッファの色の連続」で広がる。塗レーンでは次に変える。

- **壁 ＝ 線レーンの α ≥ `FILL_WALL_A`（既定 128）**。塗レーンの既存色は従来どおり「領域」（同色の連続）。
- 広げた結果を **`FILL_UNDER_PX`（既定 1・設定 0〜3）だけ膨らませて**から塗る。線が上に載るので膨らみは見えず、
  線のアンチエイリアスの縁に出ていた白い隙間が消える。AE で線と塗りを別に重ねても同じ結果になる（塗りが線の下まで届いている）。
- 投げ縄塗りは膨らませない（多角形の縁は線ではないので）。
- トーン（`isToneInk` `:2162`）は塗レーンの塗り。従来どおり網点位置だけ着色。
- 隙間閉じ（線が途切れていても漏れない）は**このSPECではやらない**。要るなら別SPEC。

### 1-4. タイムライン：レーン付きストリップ

```
       ┌──────────────────────────── time-track（既存のルーラー / ワークエリア / カーソル）
 ▸REF 2│ ■C12_rough ×2 ─────────┤   ▪PASTE 1 ══════════   ▪下絵 ══════════      ← §1-5 の帯
 線 ●🔒 100│[thumb][thumb][ thumb  ][空][thumb]…                                   ← 従来サムネ（合成）
 塗 ●🔒 100│[ ▬  ][    ][   ▬    ][  ][ ▬  ]…                                     ← 細い帯 14px
```

- **レーン見出しは左端の固定列**（`#lane-head`）。行ごとに `名前 ● 🔒 100`。
  `●`＝表示、`🔒`＝ロック、数字＝不透明度で**左右ドラッグの数値**（既存の `#frame-in` / `#frame-pos` の `ew-resize` スクラブと同じ手つき）。
  **ドラッグ中は数字のテキストだけ差し替え、離したら `updateStripUI()`**（毎フレーム作り直すと掴みが切れる）。
- **セル**：既存 `.fc` の中を上下に割る。上＝従来の `.fc-thumb`（合成サムネ）、下＝`.fc-fill`（14px の帯。`f.fill` があれば `--acid` の 60%、無ければ枠線だけ）。
  空ブロック `.fc-empty` は割らない。
- **レーンをタップ ＝ そのコマを選ぶ ＋ `state.lane` がそのレーンになる。** 現在レーン側は `--acid` で縁取り、反対側は 60% に落とす。
  Shift 複数選択・ドラッグ並べ替え・コマ打ちハンドルは**上下どちらを掴んでも従来どおり**（レーンは「どこに描くか」だけを変える）。
- **HUD**：`#stage-info` に `LINE` / `FILL` を大きく出す（レーン色）。カーソルの色もレーン色。
- **キー**：`SHORTCUT_ACTIONS` に「レーン切替」を登録（既定キーは実装時に空きから採り、`gKeymap` で再割当可）。
- 塗レーンを「帯ではなくサムネ」にしたい場合は設定パネルのトグル（既定 OFF。iPad の縦を食わないため）。

### 1-5. REF レーン：参照ごとの「帯」

`time-track` と `frame-strip` の間に `#ref-lane` を1行。**サムネは持たない。帯の div だけ。**
横スクロールは `#time-track-inner` と同じ `translateX` で追従させる。

| 参照 | 帯 | 長さ | 操作 |
|---|---|---|---|
| 参照 ANIMATOR（`refAnimators[]`） | `r.color` の帯・名前・`×N` | `tickToPx(offset)` 〜 `tickToPx(offset + ticks.length × step)` | タップ＝選択（`refSel`）／横ドラッグ＝`offset`／右端の `×N` をタップ＝`step`（`fc-dur` と同じ変更UI） |
| PASTE 画像（`refImages[]`） | ice の細い帯（4px） | 全長 | タップ＝選択 |
| FRAME 下絵 | 白の細い帯（4px） | 全長 | タップ＝選択 |
| 1コマだけの参照（MANGA PLATE のコマ枠など） | 細い帯 | 全長（静止画扱い。既存規則） | タップ＝選択 |

新設フィールド：`r.offset`（tick・既定 0）、`r.step`（≥1・既定 1）。保存は `saveRefs()`（`:2906`）に同梱。

- `renderRefLayer(t)` と `animTickImg(r)`（`:3395`）は**同じ式**で添字を出す：`k = floor((t - r.offset) / r.step)`、`k < 0` または `k ≥ ticks.length` は非表示。
  静止画（`ticks.length === 1`）は両方無視して全ティック。**2つの関数の規則を揃える**のは HANDOVER で踏んだ落とし穴（`refCenterSel` が黙る）。
- `effectiveTotalTicks()`（`:4576`）は `offset + ticks.length × step` の最大を見る。
- `▸REF n` で折り畳み（`localStorage['animator_reflane_v1']`）。参照 0 件なら自動で畳む＝使っていないとき高さを食わない。
- 帯の色：参照 ANIMATOR の `REF_COLORS`（`:2616`）は「どの参照か」を示す**役割固定色**なので据え置き。選択枠だけ `--acid`。

### 1-6. フローティング REF パネルは「何を・どう」、レーンは「いつ・どれが」

同じ `refSel` を共有する2つの見え方。**パネルの機能は減らさない**：
+ PASTE / + JSON・SEQ / + SAVED / 行ごとの ● VIS・🔒・濃度・✕ / MOVE / 中央 / FRAME の EDIT・CLR・濃度。
レーンで帯をタップするとパネルの行がハイライトし、パネルの行をタップすると帯が縁取られる。
**変形（§1-7）はパネルに入れない**（「参照を変形するのか絵を変形するのか」が毎回曖昧になる）。

### 1-7. 変形：econte の `箱+rot+warp` を道具側に移植

econte の `gFloat`（`floatLift` `:6104` → 箱ハンドル → `floatCommit` `:6359`、`flPaint` `:6085`、`toggleSelWarp` `:6470`、`gSelClip` `:6401`）をそのまま持ってくる。
形の持ち方は econte と同じ **`箱(x,y,w,h) + rot + warp{u,v}`（箱ローカルの正規化座標）**。ECONTE_HANDOVER の変形表 #1〜#8 が根拠。

animator 向けに変えるのは次だけ：

| 項目 | econte | animator |
|---|---|---|
| 入口 | SEL ツール | **`SEL` ツールボタンを新設**。ドラッグ＝投げ縄、Shift+ドラッグ＝矩形。**SEL の Wクリック＝WARP on/off**（econte と同じ手つき・SPEC_16 §5-B3） |
| 持ち上げ元 | `line` / `base` | **現在レーン**。HUD の「線+塗」トグルで両レーンを同じ変形で動かす（セルの一部をまるごと動かすとき） |
| 貼り方 | `imageSmoothingEnabled = true` | **`state.selAA`（既定 false）**＝ニアレスト。回転でギザギザになるのが「アンチ無しのまま」。HUD で AA on/off |
| PASTE の置き場 | 貼り先の紙の中央 | **同じ座標**（コマは全部同じ寸法なので。中割りの流用は同位置が欲しい） |
| Undo | 持ち上げ〜確定で1手 | 同じ。`history` のエントリは `lane:'both'` で `line`/`fill` 両方を持つ |
| 描画中の扱い | 持ち上げ中は描けない | 同じ。持ち上げ中だけ `#float-layer`（新設・`draw-line` の上）に描き、確定で焼いて消す |
| コピー | `gSelClip`（見えている形を焼いてから控える） | 同じ。別コマの**同じレーン**へ貼れる。「線+塗」で取ったものは両レーンに貼る |

HUD（持ち上げ中だけ出る小パネル）は**現行の `#zoom-ctl` と同じ作り**（`#stage` の上に絶対配置・小ボタンを並べるだけ）：`AA` / `線+塗` / `WARP` / `COPY` / `PASTE` / `✓ 確定` / `✕ 取消`。
Esc の優先順位：**変形の取消 ＞ 投げ縄の取消 ＞ TL 再生の停止 ＞ フレーム選択解除**（既存の順の先頭に2つ足す）。

### 1-8. 書き出し・連携

| 出口 | 変更 |
|---|---|
| **SEQ PNG**（`exportSequencePNG` `:7207`） | 押すと4択を**既存 `showModal({list})`**（`+ SAVED` で使っているもの）で選ばせる：**合体 / 線 / 塗 / 線+塗（別フォルダ）**。前回の選択を `localStorage['animator_seq_mode_v1']`。ZIP は `frames/`（合体・従来）または `line/` ＋ `fill/`。ファイル名は従来の `frame_00000.png` |
| **PNG**（単枚・`exportPNG` `:7162`） | 合体のまま。Shift＝BG透過も従来どおり |
| **VIDEO** | 合体のまま |
| **EXPORT JSON**（ファイル・`buildProjectJSON` `:6781`） | `cells[].image` は合体（従来）。**`cells[].layers = { line: dataURL, fill: dataURL \| null }` を追加**。`applyProjectJSON`（`:6850`）は `layers` があれば分離して戻し、無ければ `image` → `line` |
| **PROJ 保存箱（スナップショット）** | EXPORT JSON と同じ形（`layers` 込み） |
| **→ COMPOSER / LIVE / 共有DB `tdr_exchange`**（`broadcastProjectNow` `:7120`） | **合体 1枚のみ。`layers` は載せない**（3倍に膨らむのを避ける）。COMPOSER / OBAN / MANGA PLATE は**改修ゼロ**（SPEC_07 の大原則を守る） |

`buildProjectJSON()` にフラグ `{ forExchange:true }` を足し、共有DB／LIVE 向けだけ `layers` を落とす。
`PIPELINE.md` の animator の出口に「SEQ PNG の4択」と「EXPORT JSON `layers`」を追記すること。

### 1-9. UI 規約 ＝ **animator の現行デザインのまま**

発注者判断 2026-09-19（前日の「新 OBAN の規約で」から変更）。**新 OBAN のデザインシステムは持ち込まない。**

- **テーマ機構は入れない。** `THEMES` / `body[data-theme]` / `applyTheme()` / `CVC` 橋は**作らない**。
  色は既存の `:root`（`animator.html:13`）の変数をそのまま使う。主アクセントは **`--acid`（`#FF4FA8`）**。
  新しい hex を足すときも `:root` に1つ足して変数経由にする（リテラル直書きはしない）。
- **新設 UI は既存の語彙で作る**：
  | 作るもの | 真似る先 |
  |---|---|
  | レーン見出し列の `●` / `🔒` | `.fc-vis-btn`（`:1697` 付近）と REF 行の `● VIS` / `🔒` |
  | レーン不透明度の左右ドラッグ | `#frame-in` / `#frame-pos` の `ew-resize` スクラブ |
  | 変形 HUD | `#zoom-ctl`（`#stage` 上の絶対配置・小ボタン列） |
  | SEQ PNG の4択 | 既存 `showModal({list})`（`+ SAVED` の一覧） |
  | REF 帯の `×N` 変更 | `.fc-dur`（コマ打ちのクリック変更） |
  | 新しいパネルが要る場合 | `#ref-panel` の骨格（`#…-head` ドラッグ・`.panel-min` の `–`・`✕`・`#…-resize`）とスキル `floating-panel-kit` |
- **確認ダイアログは `modalConfirm` / `modalAlert`**（native `confirm` / `alert` を足さない。ROADMAP §5）。
- `scrollIntoView` は使わない（レイアウトが崩れた過去がある）。
- ⚙ 設定パネルに足す項目は既存の行フォーマットに合わせる。

> 新 OBAN 規約への寄せ（テーマ3種・44px ヘッダ・`.qd-*`）は**このSPECの対象外**。
> やるとしても別SPECで、5本まとめて1度に決める（1本だけ違う見た目になるのが一番困る）。

---

## 2. 実装の当たり

### 2-1. データ・移行（`CFG.DB_VERSION` 5 → 6・`animator.html:2018`）

- `STORE_FRAMES` の値：`{drawData, duration, width, height, savedAt}` → `{line, fill, duration, width, height, savedAt}`。
  **読むときは `drawData` があって `line` が無ければ `line` として扱う**（`loadProject` `:7564`）。書くときは常に新形式（`flushSave` `:7415`・差分保存は従来どおり）。
- `onupgradeneeded` でストアは増やさない（既存レコードはそのまま。読み側の読み替えだけで足りる）。
- `saveRefs()` の各参照に `offset` / `step` を追加。無ければ 0 / 1。
- `frameClipboard`（`:3099`）・`pushTimelineHistory()`（`:4221`）のスナップショットは `drawData` → `line` ＋ `fill`（参照同一性の比較は両方見る）。
- `resampleDrawData` / `recanvasDrawData`（`:3666` / `:3680`・CANVAS SIZE 変更）は `line` と `fill` の両方に掛ける。

### 2-2. 描画（関数の読み替え表）

| 関数 | いま | これから |
|---|---|---|
| `activeDrawCtx()` `:5598` | `editMode ? frameLayerCtx : drawCtx` | `editMode ? frameLayerCtx : (lane==='fill' ? fillCtx : lineCtx)` |
| `floodFill()` `:3919` | 自分のバッファで色の連続 | 塗レーン：壁＝線 α、領域＝塗の色。結果を `FILL_UNDER_PX` 膨張。線レーンでは呼ばれない（道具が塗レーンへ移る） |
| `syncFrameFromCanvas()` `:4391` | `drawCtx` → `drawData` | `lineCtx` → `line`、`fillCtx` → `fill`（**触ったレーンだけ** `getImageData`。もう片方は据え置き） |
| `loadFrameToCanvas()` `:4403` | `drawData` → `drawCtx` | `line` → `lineCtx`、`fill` → `fillCtx`（null なら clear） |
| `pushHistory()` `:4141` | `snapshot()` 1枚 | エントリに `lane` を付け、そのレーンの ImageData だけ積む。`stepUndo/Redo` は `lane` を見て戻す先を選ぶ |
| `regenThumb()` `:3700` | `drawData` | `fill` の上に `line` を描く |
| `frameToPNGDataURL()` `:6758` | `drawData` | 引数 `lane:'both'\|'line'\|'fill'`。既定 `both` |
| `drawOnionSide()` `:5159` / `onionTint()` `:5207` | `drawData` | `line`（設定で `both`） |
| `tlComposeShot()` `:7795` | draw 1枚 | `fill` → `line` の順 |
| `markFrameDirty()` `:7720` | — | 変更なし（レーンに関係なくコマ単位で dirty） |

### 2-3. タイムライン

- `buildStrip()`（`:4631`）：`.fc` の中に `.fc-thumb`（既存）と `.fc-fill`（新）を作る。`data-lane` を持たせ、`pointerdown` で `state.lane` を更新してから既存の選択処理へ流す。
- `updateStripUI()`（`:5015`）：`.fc-fill` の「あり/なし」と現在レーンの縁取りを更新。
- `#lane-head`：`#strip-area` の左に固定列。`#time-track-inner` の `left:28px` は見出し列の幅に合わせて広げる（帯とセルの原点を揃える）。
- `#ref-lane`：`updateTimeTrack()`（`:5058`）の中で帯を作り直す（呼ばれる頻度は既に低い。ストローク中には走らない）。

### 2-4. 変形の移植元（econte）

`FL_WARP_N` `econte.html:5880`（4×4 固定）／`flLocalWarp` `:5982`／`flPaint` `:6085`／`floatLift` `:6104`／`floatCommit` `:6359`／`gSelClip` `:6401`／`toggleSelWarp` `:6470`。
`flPaint` の `imageSmoothingEnabled` を `state.selAA` にする以外は**数式を変えない**（三角形の 0.45px 膨らませ・反対側の隅をワールドで固定、はそのまま）。
持ち上げ元の紙は `floatSourcePaper()` の読み替え：現在レーンの canvas（`線+塗` なら2枚を同じ `gFloat` で持つ）。

### 2-5. 重さの見積り（守るべき数字）

| 場面 | 見込み | 根拠 |
|---|---|---|
| ストローク中 | **今と同じ** | 描く先が1枚なのは変わらない。合成はコンポジタ |
| コマ切替 | `putImageData` が最大2回（塗りが無ければ1回） | `loadFrameToCanvas` |
| 塗り済み 72 コマの RAM | +9MB × 塗り済み枚数 | `fill` は遅延確保 |
| IndexedDB | 1コマ 0.42MB → 0.7MB 前後 | HANDOVER 実測の比 |
| REF レーン | div 数 = 参照数 | サムネ無し |
| 変形プレビュー | 3.1〜3.4ms／確定 ≤173ms 1回 | ECONTE_HANDOVER #8 |

---

## 3. フェーズ

| P | 内容 | 完了の条件 |
|---|---|---|
| **P0** ✅ 2026-09-23 | データ（§1-1）・2枚 canvas（§1-2）・道具×レーン規則・塗りの壁判定と潜り（§1-3）・DB v6 と読み替え（§2-1）・§2-2 の表。UI は**トップバーに `線/塗` トグル1つだけ** | 旧プロジェクトが線レーンに入って1pxも変わらない／塗レーンで FILL すると線の下に 1px 潜る／Ctrl+Z がレーンを跨がない／`verify:animator` 0.000% → **4つとも実測で満たした（§7）** |
| **P1** ✅ 2026-09-23 | タイムラインのレーン化（§1-4）・HUD・キー | 見出し列の ● 🔒 数字が効く／レーンをタップで描く先が変わる／**既存の見た目から浮いていない**（§1-9） → **満たした（§7-5）** |
| **P2** ✅ 2026-09-23 | REF レーン（§1-5）・`offset` / `step`・折り畳み | 帯の位置と `renderRefLayer` の表示が一致／中央ボタンが t≥1 でも効く（`animTickImg` 同規則） → **満たした（§7-6）** |
| **P3** ✅ 2026-09-23 | 書き出し 4択・EXPORT JSON `layers`（§1-8）・`PIPELINE.md` 更新 | 線 ZIP と塗 ZIP を AE で重ねて合体 ZIP と一致／COMPOSER に届く JSON のサイズが今と同じ → **満たした（§7-7）** |
| **P4** ✅ 2026-09-23 | 変形（§1-7）：SEL ツール・投げ縄/矩形・箱+rot+warp・AA off・線+塗・COPY/PASTE | econte と同じ操作で動く／AA off で回転後も線が 2値のまま／Undo 1手 → **満たした（§7-8）** |

2026-09-23 に P0〜P4 を通しで実装した（発注者「旧 animator をこのまま極める」）。
残っているのは §7-9 の「やっていないこと」だけ。

---

## 4. 動作チェック表（完成報告に必ず付ける・⚠️未確認 のまま完了と言わない）

| パラメータ / 操作 | 期待される変化 | 有効になる条件 | 結果 |
|---|---|---|---|
| レーン切替（見出し列の名前・セルの上下タップ・L キー） | HUD が 線/塗 に変わり、描いた線がそのレーンの canvas に乗る | 常時 | ✅確認済（コマ2のサムネ実クリック → コマ2・線／コマ1の帯 → コマ1・塗／L キーで往復。P0 のトップバー `線`/`塗` は撤去＝§7-5） |
| 線レーンで FILL を持つ | 塗レーンへ自動で移りトースト | 常時 | ✅確認済（FILL ボタンの実クリック → `lane=fill`） |
| 塗レーンで PEN | `fillColor` で描ける | 塗レーン | ✅条件付き（主線=黒のとき `fillColor`。下書/指示インクを選んでいるときはその色のまま） |
| 塗レーンで FILL | 線 α≥128 を壁にして広がり、1px 膨らむ | 塗レーン・`FILL_UNDER_PX` | ✅確認済（6px 線の四角：中だけ塗られ外は α0、線の内側1px目に塗 α255。線レーンは1pxも変わらない。33ms @2048×1152） |
| 塗りの潜り幅 0〜3（⚙ LINE / FILL） | 潜る幅が変わる（0 で従来どおり隙間が出る） | 塗レーンのバケツ | ✅確認済（設定欄で 3 → 5px 線の内側に 3px 潜る・外へは漏れない。0/1/3 も実測） |
| レーン ●（見出し列） | その canvas が消える／戻る。書き出しには出る | 常時 | ✅確認済（塗の ● 実クリックで display none・✕ 表示、もう一度で戻る） |
| レーン 🔒（見出し列） | 描けずトースト。選択・表示・濃度は触れる | 常時 | ✅確認済（ロック中の実ストロークは1pxも乗らず「🔒 線レーンはロック中」。全消去・SEL の持ち上げもロック中のレーンは触らない） |
| レーン濃度（見出し列の数字を左右ドラッグ・Wクリックで100） | canvas の opacity。書き出しには乗らない | 常時 | ✅確認済（左へ約40px ドラッグで 100→60・CSS 0.6、Wクリックで 100 に戻る） |
| Ctrl+Z / Y | 触ったレーンだけ戻る | 常時 | ✅確認済（線→線→塗 の順に積んで Undo×3：塗だけ→線2本目→線1本目。Redo×3 で全部戻る。全消去は Undo 1回で両レーン戻る） |
| オニオン（⚙ オニオンに塗りも含める） | 既定は線だけ、ON で塗りも | ONION ON | ✅確認済（既定 1247＝線だけ、ON で 34472＝合体。設定ボタンの実クリックで ON/OFF） |
| REF 帯の横ドラッグ（Wクリックで先頭へ） | `offset` が変わり canvas の参照がずれる | 参照 ANIMATOR・🔒 OFF | ✅確認済（実ドラッグで +2コマ、ロック中は動かずトースト、実 Wクリックで 0 に戻る。再読込で復元） |
| REF 帯の `×N`（×1→×2→×3→×4） | 1枚の表示が N コマに伸びる | 参照 ANIMATOR（2コマ以上） | ✅確認済（ずらし3・×2 で t=3,4 が1枚目、t=9,10 が4枚目、以降は出ない＝キャンバスの画素で実測。帯とタイムラインも伸びる） |
| REF 帯・行ラベルのタップ ⇄ パネル行 | 双方ハイライト・中央ボタンの対象が変わる | 常時 | ✅確認済（行ラベルのクリックでパネル側も選択。中央ボタンは参照が出ている t=2 で原寸中央へ） |
| `▸ REF n`（時間メモリの左端） | 折り畳み。0件・閉じているときは高さ0 | 常時 | ✅確認済（閉じると `--ref-lane-h` 0px・開くと行数×14px。下絵に描くと FRAME 行が増え、消すと消える。REF パネルを閉じると帯が薄くなる） |
| SEQ PNG 4択 | ZIP のフォルダ構成が変わる。線+塗を重ねて合体と一致 | 常時 | ✅確認済（別フォルダ＝`line/`4＋`fill/`4 で番号が揃い、塗の上に線を重ねるとアプリの合体と 0 バイト差。合体は従来の名前と `frames/`、塗りだけは `_fill.zip`・前回の選択を記憶） |
| EXPORT JSON → IMPORT JSON | 線と塗が分離したまま戻る | 塗りのあるコマ（`layers` あり） | ✅確認済（書き出し→読み込みで線・塗の画素数が一致。線だけのコマは `layers` を持たず従来の形） |
| → COMPOSER / LIVE | 合体 1枚・`layers` を載せない | 常時 | ✅条件付き（送る中身を作る `buildProjectPayload()` のキーは従来どおり `id,kind,duration,hidden,image`。COMPOSER 実機での受信は未確認） |
| 旧プロジェクトの読込 | 線レーンに入り 1px も変わらない（VRT） | `drawData` のみ | ✅確認済（v5 形式のレコードを DB に直接書いて再読込 → 線レーンに全量・塗 null。VRT 6コマ 0.000%） |
| SEL ドラッグ / Shift+ドラッグ / Ctrl+ドラッグ | 投げ縄 / 四角で持ち上がる／元を残してコピー | SEL（A） | ✅確認済（ポインタイベントで四角選択 → 線レーンだけ持ち上がり、画面に抜き跡・コマのデータは持ち上げ前のまま。空の範囲・ロック中は持ち上がらずトースト） |
| SEL Wクリック / A2連打 / HUD WARP | WARP 格子 on/off・形は残る | 常時（持ち上げ中は格子が出る） | ✅確認済（実 Wクリックと A2連打でどちらも切替、ボタン表示が SEL⇄WARP。内側の点を掴んで変形 → 確定） |
| HUD `AA` | off でアンチ無しのまま、on で滑らか | SEL | ✅確認済（OFF：移動・拡縮・30°回転・自由変形・WARP のどれを確定しても半透明 0・色 1色。ON：回転で半透明 604 画素） |
| HUD `線+塗` | 両レーンが同じ変形で動く | 持ち上げる前に選ぶ（浮いている間はグレー） | ✅確認済（両レーンを拡縮して確定＝履歴1手 `both`、どちらも半透明0） |
| COPY → 別コマで PASTE（Ctrl+C / Ctrl+V） | 同じ座標に浮いて置ける | SEL | ✅確認済（コマ1で COPY → コマ2で PASTE が同じ 379,279 に浮く。確定後 Ctrl+Z で元に戻る） |
| Esc / Enter / Ctrl+Z / 他の操作 | Esc＝取り消し、Enter・枠の外・別の道具・コマ移動＝確定、Ctrl+Z＝持ち上げる前へ | 浮いている間 | ✅確認済（Esc で画素数が元どおり、移動を Enter で確定 → Ctrl+Z 1回で元の位置。PEN に持ち替え・コマ移動で確定） |
| 新設 UI の見た目 | 既存の `--acid` 桃・既存ボタンの形から浮いていない（§1-9） | 常時 | ✅確認済（見出し列＝`.fc-vis-btn` の作り、REF 帯＝時間メモリの作り、HUD＝`#zoom-ctl` の作り、設定欄＝トーン設定の行。スクリーンショットで確認） |
| iPad（`pointer:coarse`） | 見出し列の的が指で押せる・帯のドラッグがペン/指で効く | iPad | ⚠️未確認（実機なし）。768×1024 の縦幅で段ずれ・横スクロールが無いことだけ確認。coarse では帯 18px・HUD 32px に広げてある |
| `tools/check.js` / `npm run verify:animator` | 構文・配線・VRT 0.000% | 常時 | ✅確認済（P0〜P4 の各段で animator は check 全通過・VRT 6コマ 0.000%。最長フレームは 189〜196ms＝変更前 192〜206ms と同じ幅） |

---

## 5. 仮置きを確定として記録（2026-09-18・発注者「その方向でOK」）

1. **旧プロジェクトは丸ごと線レーン。**分離しない。「色で分ける」移行ツールは作らない（要るなら別SPEC）。
2. **塗レーンの PEN は `fillColor`。**塗り残しの補修用。線レーンの PEN は `inkColor` のまま。
3. **オニオンは線レーンだけ。**設定で塗を含められる（既定 OFF）。
4. **REF の表示間隔は `×N`（step）と `offset` の2つ。**それ以上のリタイム（伸縮カーブ）は持たない。
5. **UI は animator の現行デザインで統一**（§1-9・2026-09-19 変更）。テーマ機構は入れない。
   前日いったん「新 OBAN の規約で」としたが撤回済み＝**`--acc` / `THEMES` / `.qd-*` / `CVC` を足さない**。

---

## 6. 関連

- `ANIMATOR_HANDOVER.md` — レイヤー構成／主要 state／autosave 容量実測／「線のコピペ＆選択移動/回転/スケール（見送り中）」→ **本SPECの P4 で引き取る**
- `ECONTE_HANDOVER.md` — 変形表 #1〜#8（`箱+rot+warp` の根拠と実測）
- `SPEC_15_ECONTE_V3.md` §P2-7 — 変形の元仕様
- `SPEC_16_ECONTE_V4.md` §5-B3 — SEL Wクリックの手つき
- `SPEC_07_ANIMATOR_OBAN_BRIDGE.md` — 下流は改修ゼロの大原則（§1-8 で守る）
- `PIPELINE.md` — P3 で更新
- `OBAN_BUILDER_UI_HANDOVER.md` — **参考だけ**。§4「ドラッグ中はテキストだけ・離したら全再描画」の考え方は借りるが、配色・パネル骨格は**持ち込まない**（§1-9）
- スキル `animator-ref-overlay` / `animator-brush-ops` / `floating-panel-kit` — 実装を変えたら references も同じコミットで直す

## 7. P0 実装メモ（2026-09-23）

### 7-1. 起草から変えたこと

| # | 起草時 | 実装 | 理由 |
|---|---|---|---|
| 1 | 線 canvas を `draw-line` に改名 | **`draw-layer` のまま**。`lineCanvas`/`lineCtx` は別名 | id は `verify/`・スキル・ポインタ処理が参照している。改名しても得るものが無い |
| 2 | オニオンは塗の下 | **塗の上・線の下** | 塗り終えたコマでオニオンが塗りに隠れて前後の線が読めなくなる |
| 3 | 線レーンの FILL ボタンに「塗」バッジ | 作らない | FILL を押すと必ず塗レーンへ移るので、線レーンで FILL を持っている状態が無い |
| 4 | 履歴は `[{lane, line?, fill?}]` | 同じ形に **`lane:'base'`（コマを開いた時点の両レーン）** と **`'empty'`（そのレーンがまだ無かった印）** を足した | 下の 7-2 |

### 7-2. 履歴（Undo）の仕組み ★ここを崩すとレーンを跨いで壊れる

- `history[0]` は `{lane:'base', line, fill|'empty'}`。**`HISTORY_MAX` を超えても base は捨てない**（`splice(1,1)` で2番目から捨てる）。
  base を捨てると「そのレーンの1つ前の姿」が見つからず、Undo で何も戻らなくなる。
- Undo は「いま戻すエントリのレーン」を、**手前に遡って同じレーンが最後に記録された姿**（`laneImgAt()`）で上書きする。
  線→線→塗 と積んだあと Undo すると、塗の1つ前は base の `fill:'empty'` まで遡る＝clear。線は触らない。
- 全消去は `pushHistory(['line','fill'])` で `lane:'both'` の1手。Undo 1回で両レーン戻る。
- `syncFrameFromCanvas(f)` を**レーン省略で呼ぶと、塗は「既に持っているときだけ」吸い上げる**。
  ここで塗を毎回作ると、線だけのコマも 9MB → 18MB になる（遅延確保が崩れる）。塗を作るのは `pushHistory('fill')` 経由だけ。

### 7-3. 実測

| 項目 | 値 |
|---|---|
| 塗レーンのバケツ（2048×1152・壁判定＋1px 潜り込み） | 33ms |
| 潜り幅 `fillUnderPx` 0 / 1 / 3 | 線の内側 0 / 1 / 3px に塗 α255。6px 線の外へは漏れない |
| 保存→再読込 | 線 1247・塗 33432（間引き計数）が一致。`lane` も復元 |
| v5 形式のレコード | 合体 34472 がそのまま線レーンへ・塗 null |
| EXPORT JSON | `cells[0]` のキーは `id,kind,duration,hidden,image` で従来と同じ。画像は塗→線の合体 |
| VRT `verify:animator` | 6コマ 0.000% |
| 起動の最長フレーム | 変更後 177 / 195ms、変更前 206 / 192ms（同じ幅） |

### 7-4. 気づいたが P0 では直していないもの

- **`drawLine()` に小数の座標を渡すと返ってこない**（`bresenham` が整数刻みで終点に届かない）。変更前からある。
  実際の描画経路は `toCanvasCoord()` が整数を返すので踏まない。**P4 の変形で座標を計算して線を引くときに踏みうる**ので、
  そのときは呼ぶ側で `Math.round` するか `bresenham` の入口で丸める（丸めは整数入力には効かない＝VRT は変わらない見込み）。
- サムネ（80×45）はニアレスト縮小なので、細い線（7px 前後）は消えて塗りだけが見えることがある。変更前から同じ。

### 7-5. P1（タイムラインのレーン化）で決めたこと

- **トップバーの `線`/`塗` は撤去した**。P0 で足したらトップバーが 1095px → 1169px に伸び、
  **iPad mini 横（1133px）で HOME がはみ出した**。同じ操作（切替・ロック）は見出し列が持つので、入口は見出し列と L キー、表示は HUD。
- 帯の高さは `--fill-band`（14px・`pointer:coarse` で 18px）。**`#app` のタイムライン行をその分だけ伸ばした**のでサムネの高さは変わらない
  （ステージが 14px 低くなる）。
- 見出し列の段はセルの段にそろえた（線の段＝サムネ、塗の段＝帯、下＝コマ番号の 18px）。実測でずれ 0〜1px。
- セルは押した段でレーンが決まる（サムネ＝線／帯＝塗）。Shift・Alt のときはレーンを変えない（複数選択・分割の邪魔をしない）。
- 帯の色は「塗レーンに絵があるか」で付く。判定は `fillHasInk()`（配列を WeakMap で覚えるので編集のたびに1回だけ走査）。
- **やっていないもの**：塗レーンをサムネで見る設定（§1-4 の任意項目）、カーソルの色をレーン色にする（OS のカーソルなので色を変えられない）。

### 7-6. P2（REF レーン）で決めたこと

- **表示コマの式は `refTickIndex(r, t)` の1本**（`k = floor((t - offset) / step)`）。`renderRefLayer` と `animTickImg` がこれを呼ぶ。
  尽きる位置は `refEndTick(r)`、タイムラインの延長（`maxRefTicks`）もこれを使う。
- 開閉ボタン `▸ REF n` は**時間メモリの左端の空いている角**に置いた（見出し列の真上）。REF レーン自体は参照ごとに1行（14px）で、
  **閉じている・参照が0件のときは高さ0**（`--ref-lane-h`）。高さが変わるとステージが縮むので、FIT 中は `ResizeObserver` で合わせ直す。
- 行の順は **参照 ANIMATOR → 貼り付け画像 → 下絵 FRAME**（時間を持つものを上）。FRAME は「下絵に絵があるときだけ」出す
  （`frameLayerHasInk()`。下絵の編集は全部 `scheduleFrameLayerSave()` を通るので、そこで印を落とす）。
- 帯のWクリック判定は **参照そのもの（`r._laneTap`）に持たせる**。1回目のタップで選択→帯が作り直されるので、
  DOM に持たせると2回目が新しい帯に落ちて判定が消える（実装中に踏んだ）。
- 帯のドラッグ中は帯の位置とキャンバスだけ更新し、離したら `refTimingChanged()`（尺・パネル・保存）。

### 7-7. P3（書き出し）で決めたこと

- **`forExchange` フラグは作らなかった**。共有DB／LIVE へ送る中身はもともと別関数 `buildProjectPayload()` が作っていて、
  `layers` を足したのはファイル書き出しと PROJ 保存箱が使う `buildProjectJSON()` だけ。
- `layers` は **塗レーンに絵があるコマだけ**付ける（線だけのコマは従来と同じ形・同じ大きさ）。
- SEQ PNG は 4択を既存の `showModal({list})` で出す。**空のコマ・非表示のコマも透明1枚を書く**（番号の欠けを作らない）。
  合体はファイル名も `frames/` も従来のまま。

### 7-8. P4（SEL）で決めたこと

- 形の持ち方と表示用の貼り方（`flPaint`）は econte の式をそのまま移した。**確定だけ別**：
  AA OFF（既定）は `flBakeNearest()` で1pxずつ逆写像し、元の画素をそのまま拾う。canvas に描かせると
  三角形のクリップの縁に半透明が出て、色も1段ずれ、あとでバケツの「同じ色の連続」が途切れるため。
  実測：移動・拡縮・30°回転・自由変形・WARP のどれでも確定後 **半透明 0・色 1色**。AA ON は canvas で滑らかに（回転で半透明 604 画素）。
- **浮いている間は `syncFrameFromCanvas` がそのコマを吸い上げない**。だから autosave も取り消し（`paintLanesFrom`）も
  「持ち上げる前」の絵で済み、確定は `pushHistory` 1回＝Ctrl+Z 1回で持ち上げる前に戻る。
- 他の操作へ移るときは `floatSettle()` で確定する（別の道具・コマ移動・レーン切替・再生・書き出し・全消去・サイズ変更・コマ削除）。
  浮いている間の Ctrl+Z は取り消し、Redo は確定。作画タイムラプスは浮いている間は撮らない（抜き跡が写るため）。
- 焼き込み先は**持ち上げたレーン**（econte の「いまの描く先」ではない。レーンを切り替えると先に確定する）。
- ペーストは同じ座標。空ブロックで貼ると絵のコマに昇格させる。SEL は FRAME 編集中は使えない。
- キーは A（2連打で WARP）。Ctrl+C は浮いている形があればそれを、無ければ従来どおりコマのコピー。
  Ctrl+V は SEL を持っていてコピーがあれば形を、無ければ従来どおりコマのペースト。
- 検証は合成ポインタイベントで行った（実マウスの Shift+ドラッグを送れないため）。合成イベントでは `setPointerCapture` が
  例外を出すので、テスト中だけ無害化した。アプリ側の処理はマウスと同じ経路。

### 7-9. やっていないこと（P0〜P4 のあと）

- 塗レーンをサムネで見る設定（§1-4 の任意項目）
- カーソルの色をレーン色にする（OS のカーソル）
- iPad 実機での確認（指で SEL は使えない。描くのと同じくペン専用。HUD のボタンは指で押せる）
- COMPOSER 実機での受信確認（送る中身の形は従来と同じことをキーで確認済み）

## 8. 使ってみての調整（2026-09-23・発注者の要望13件）

| # | 要望 | 実装 |
|---|---|---|
| 1 | ペンと消しゴムを持ったら基本は線レーン、ペンは主線 | PEN / ERASE を選ぶと線レーンへ。**FILL / SEL から PEN に戻ったら主線へ戻す**（FILL でパレット色を使うとインクが下書へ移る作りのため） |
| 2 | 筆圧の Wクリックがペンと消しゴムで連動する | `state.pressureErase` を新設。PEN の Wクリック／P2連打＝ペンだけ、ERASE の Wクリック／E2連打＝消しゴムだけ。判定は `toolPressureOn()` |
| 3 | 線を投げ縄消しで消したい。ほかの FILL は線では要らない | 投げ縄消し（L-DEL）を選ぶと**今のレーンのまま**。それ以外の FILL は塗レーンへ移る。線レーンでバケツ・投げ縄塗りを押したら理由を出して何もしない（`fillIsLassoErase()`） |
| 4 | 投げ縄塗りで主線を選んだら黒 | `lassoFillPolygon` は主線のとき `#000000`。バケツは従来どおり |
| 5 | iPad でツールバーのスクロールが BG バーを奪う | `.tb-bg-track` に `touch-action:none`＋非 passive の `touchstart` で止める。指の的を左右 10px 広げた（見た目は同じ） |
| 6 | ツールバーの ↩ ↪ は要らない | 撤去（Ctrl+Z / Y と指2本・3本タップが入口）。`updateUndoButtons()` は名前だけ残した |
| 7 | 線・塗レーンの濃度は要らない | 見出し列の数字を撤去。濃度は常に100%（旧保存の値は読まない）。見出し列の幅は 112px に保ち、空いた分を REF の名前欄へ回した |
| 8 | 画面全体スポイトの色がパレットに入らない | 原因＝**下書／指示を選んでいるときはインクにだけ入り、パレットには入らない**作りだった（主線のときだけ入っていた）。どのインクでも選択中の色チップへ入るように揃えた（`pickToPalette()`）。先頭の明度チップを選んでいるときは固定なので入れず、理由を出す |
| 9 | REF の並びをタイムラインとパネルで揃えたい・上下を入れ替えたい | **種類をまたいで1本の並び**（`z` が大きいほど手前）。パネル・REF レーン・キャンバスの重なりが同じ順。パネルの ▲▼、REF レーンの見出しを上下ドラッグで入れ替え。参照 ANIMATOR と画像は同じ `ref-layer` に奥→手前で描く（`refimg-layer` は使わない）。下絵 FRAME は参照より手前の別の層なので一番上に固定 |
| 10 | 画像にもイン点／アウト点 | 画像に `tin`（イン）/`tout`（アウト・null＝最後まで）。**既定は全尺**。帯の端をドラッグで切る・中を横ドラッグで範囲ごと移動・Wクリックで全尺へ。帯の見た目は細線のまま、掴める高さは 12px |
| 11 | 名前を短く付け直したい（種類は分かるように） | 名前の前に種類の札（IMG / ANI / SEQ / 下絵）。パネルの ✎ か、REF レーンの見出しを Wクリックで、その場で名前を変える（Enter／外を触る＝確定、Esc＝取り消し。24文字まで） |

- 旧保存の並び：`z` が無いものは「画像が手前・ANIMATOR が奥」だった見え方のまま振る（`ensureRefZ()`）。
- **画像も ANIMATOR と同じ層に入ったので、画像は下絵 FRAME の下になった**（以前は FRAME の上）。参照→下絵→塗→線の順。
- 再生・スクラブで参照を描き直す条件は `refHasTiming()`（ANIMATOR があるか、範囲を切った画像があるか）。
- Wクリック判定は見出し（`_lblTap`）も帯（`_laneTap`）も**参照そのものに持たせる**（1回目で DOM が作り直されるため）。
- 起動の性能計測：この日の後半は push 済みの版も同じく 442〜493ms（予算 300ms 超）で、差は無かった＝PC 側の状態による。見た目の回帰は 6コマ 0.000%。
