# GUIDE — ANIMATOR × MANGA PLATE × OBAN / COMPOSER をやってみる手順

「送る」「取り込む」ボタンを押していく実践ガイド。仕組みの説明は
[SPEC_09_MANGA_PLATE.md](SPEC_09_MANGA_PLATE.md) と [PIPELINE.md](PIPELINE.md) にあるので、
ここでは**手順だけ**を追える形にした。

## 0. 先に知っておく3つ

- **同じブラウザの中だけ**で繋がる（別のPC・別のブラウザには届かない）。作業中は関係するタブを開いたままにしておくのが一番安定する
- 送る側で **LIVE** を1回押しておくと、以後は保存するたびに自動で相手へ届く（毎回ボタンを押し直さなくていい）
- 迷ったら、受け取る側の一覧（**+ FROM SAVED** / **▶ANIMATOR**）を開いて「今なにが届いているか」を確認する

---

## 1. ANIMATOR → MANGA PLATE（描いた絵をコマに入れる）

1. `animator.html` を開いて絵を描く（1コマでも複数コマでもOK。複数コマだと MANGA PLATE 側で「コマ送り」に化ける）
2. 上部の **LIVE ○** を1回クリック → **LIVE ●** になれば準備完了
   （すでに **→ COMPOSER** を一度押していれば、それでも共有倉庫に絵が置かれるので省略できる）
3. `manga-plate.html` を開く（すでに開いていればそのタブに切り替えるだけ）
4. コマ割りができていなければ左の **╱ DIV** で分割 → 絵を入れたいコマを選ぶ
5. ◆Link パネルの **◀ ANIMATOR 取込** を押す → 一覧が出るので、いま描いたプロジェクトを選ぶ
6. 選んだコマにぴったり収まって絵が入る

**確認**: ◆Items のコマ欄に IMG の素材が増えていれば成功。以後 ANIMATOR で保存するたびに自動更新される。

---

## 2. MANGA PLATE → ANIMATOR（コマの中を描く／描き足す）

2つのやり方がある。

### 2-A. コマの中身をゼロから描く（下敷きを送って描く）

1. `manga-plate` で描きたいコマを選択
2. ◆Link の **▶ ANIMATOR REF** を押す → 確認ダイアログで「ANIMATOR を新しいタブで開きますか？」→ はい
3. ANIMATOR 側で **REF**（ショートカット `R`）を開く → **REF ANIMATORS** の一覧に、送ったコマが下敷きとして出ているはず。
   出ていなければ **+ FROM SAVED** から拾う
4. 下敷きの上にレイヤーを重ねて描く
5. 描けたら `manga-plate` に戻り ◆Link の **◀ ANIMATOR 取込** → 送った位置にピッタリ絵が収まる

以後は ANIMATOR で保存するたびに自動更新（LIVE）。

### 2-B. パララックスで動かすと足りなくなる「はみ出し」を描き足す（のりしろ付き）

§3・§4 で「レイヤー送り」した後、OBAN/COMPOSER で層を動かすとコマ枠の外側が見えてしまうことがある。
そこを描き足すための往復。

1. `manga-plate` で対象コマを選択
2. ◆Link の **▶ 描き足し（のりしろ付き）** を押す
3. 「どの層に入れますか？」→ **奥 / 中間 / 手前** から、直したい層を選ぶ
4. ANIMATOR が開く。**桃色の実線＝コマの枠**、**水色の破線＝のりしろの外側の限界**。この間を描き足す
5. 描けたら `manga-plate` へ戻り **◀ ANIMATOR 取込** → 選んでおいた層にそのまま収まる

---

## 3. MANGA PLATE → OBAN（モーションコミックにする）

### 3-A. かんたん版：ページをまるごと1枚絵として送る

1. `manga-plate` でページを仕上げる（コマ割り・絵・トーン・文字まで）
2. ◆Link の **▶ OBAN** を押す → OBAN が新規タブで開く
3. OBAN 側の **⚙ SETTINGS ▸ FILE／連携** を開き、**▶ANIMATOR**（連番の取り込み）を押す
   → さっき送ったページを選ぶ → 1枚のパネルになる

### 3-B. コマ内パララックス版：奥・中間・手前を分けたまま送る

コマの中の素材（トーン・集中線・流線など）に、あらかじめ ◆Items で **奥 / 中間 / 手前** のタグを付けておく
（このタグは**送るときだけ効く**。紙の見た目は変わらない）。

1. `manga-plate` でパララックスさせたいコマを**1つ**選ぶ
2. 素材ごとに ◆Items の行の **前 / 中 / 奥** バッジ、または ◆Item の「奥行き」チップで奥行きを決める
3. のりしろ（既定115%）と ぱかぱか層の解像度（既定70%）はそのままでOK。重ければ下げる
4. ◆Link の **▶▶ OBAN レイヤー** を押す → OBAN が新規タブで開く
5. OBAN 側の **⚙ SETTINGS ▸ FILE／連携 ▸ ▶ANIMATOR** で、さっき送ったコマを選ぶ
   → 1コマが「フレーム＋子パネル（奥/中間/手前）」として入る
6. OBAN の **TAKE** でカメラを振ると、層ごとに違う速さで動く＝パララックスする
7. **寄っても層を分けたい**ときは、そのフレームのインスペクタで **Z** を上げる（既定は届いた時点で 1.00）

集中線・流線に **ぱかぱか**（◆Item で ON）を付けておくと、その層だけ 24fps のループアニメになって届く。

---

## 4. MANGA PLATE → COMPOSER（合成・カメラワークを詰める）

### 4-A. かんたん版：ページをまるごと1トラックとして送る

1. `manga-plate` でページを仕上げる
2. ◆Link の **▶ COMPOSER** を押す → COMPOSER が新規タブで開き、1トラックとして絵が入る

### 4-B. レイヤー版：層ごとに別トラックで送る

1. §3-B と同じ要領で、コマを選び ◆Items で **奥 / 中間 / 手前** を付けておく
2. ◆Link の **▶▶ COMPOSER レイヤー** を押す → COMPOSER が新規タブで開く
3. **地 → 奥 → 中間 → 手前 → 枠の上** が奥から手前の順で別トラックに並ぶ
4. COMPOSER のカメラ（XY・Z）を動かすと、トラックごとに違う速さで動く＝奥行きが出る

---

## つまずきやすいところ

| 症状 | 見るところ |
|---|---|
| 一覧に出てこない | 送った側（ANIMATOR / MANGA PLATE）で **LIVE** を1回押したか確認。送った直後は少し待つ |
| 別ブラウザ・別PCで繋がらない | この連携は**同一ブラウザ内だけ**が仕様。ファイルで渡したいときは EXPORT JSON / PNG を使う |
| タブを閉じてしまった | もう一度そのアプリを開き直せばよい。共有倉庫の中身は消えないが、**自動追従だけは切れる**ので、開き直したら送信側で LIVE を押し直す |
| COMPOSER のレイヤー絵がガタつく | ぱかぱか層の解像度を **100%** に上げると滑らかになる（データは重くなる） |
| パララックスで動かすと絵が欠ける | のりしろを上げる、または **▶ 描き足し（のりしろ付き）** で外側を描き足す |
| 寄っても層が分かれない | OBAN のフレームの **Z**（既定0）を上げる。MANGA PLATE のレイヤー送りは 1.00 で届く |
