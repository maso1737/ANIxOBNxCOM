# 申し送り — MANGA PLATE ⇄ OBAN の読み文字

更新 2026-08-29。**P0（送り）・P1（受け）・P2（戻り）すべて実装済み。往復がつながった。**
実装メモと実測は `SPEC_09_MANGA_PLATE.md` **§v2-9（送り）／§v2-11（戻り）** と
`OBAN_BUILDER_HANDOVER.md`（V2-D テキスト）にある。ここは**残っている選択肢だけ**。

---

## いまの往復（要点だけ）

- MANGA PLATE `▶ OBAN` … 読み文字を**画像に焼かず** `rec.plate.texts[]` で渡す
  （`▶ ANIMATOR REF` / `▶ COMPOSER` は従来どおり焼く）
- OBAN 取り込み … **1ページ＝1フレーム**に包み、絵を子パネル・読み文字をフレーム子テキストへ。
  そのとき `srcId`（MANGA PLATE の item id）を控える
- OBAN で文字を確定 … **文字列だけ**が MANGA PLATE へ戻り、id 一致の文字を書き換える。
  **位置・サイズ・色・行間は MANGA PLATE が正**。Ctrl+Z で戻せる
- 届いたかは **ack** で判る（閉じていれば「戻せませんでした」と出る）
- **EN字幕（`type:'sub'`）には一切触っていない**

## 残っている選択肢（発注者が「要る」と言ったら着手／いまは意図的に無し）

1. **OBAN で足した文字を原稿にも作る**
   → 位置の逆変換が必須になり、「文字だけ戻す」の約束が崩れる。やるなら §v2-11 の決めごとを1つ外す判断から
2. **OBAN で消した文字を原稿からも消す**
   → 事故るのが原稿側なので、**確認ダイアログ必須**にする
3. **位置・サイズも戻す**（OBAN でレイアウトを詰めたいとき）
   → 用紙座標へ逆変換すると誤差が乗る。往復するほど版面がにじむ点を承知の上で

## 触るときの注意

- `oban-builder.html` を変えたら **`cd verify && npm run verify:oban`**（SPEC_08）
- 入出力を変えたら **`PIPELINE.md` を更新**
- `drawVText` は **builder と viewer の2か所**にある
- 複製したテキストは `srcId` を継がない（継ぐと2つが原稿の同じ1文字を奪い合う）
